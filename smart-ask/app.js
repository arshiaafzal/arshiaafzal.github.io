function renderSession(e="smart"){const t="smart"===e?smartLines:smartLines.map((([e,t,n])=>"ROUTER"===e?["ROUTER","opus \xb7 forced baseline","opus"]:"SONNET"===e?["OPUS",t,"opus"]:[e,t,n]));terminal.innerHTML=t.map((([e,t,n],s)=>'<div class="terminal-line '+n+'" style="animation-delay:'+55*s+'ms"><span class="label">'+e+'</span><span class="value">'+t+"</span></div>")).join("");const n="smart"===e;$("#footer-model").textContent=n?"claude-sonnet-4-6":"claude-opus-4-1",$("#turn-cost").textContent=n?"$0.0848 this turn":"$0.1404 this turn",$("#total-cost").textContent=n?"$0.1273 total":"$0.2108 total",$("#cost-bar").style.width=n?100-evidence.lookup.value+"%":"100%",$("#cost-delta").textContent=n?"\u221239.6%":"BASELINE"}function renderCalculator(){const e=Object.fromEntries(Object.entries(calcInputs).map((([e,t])=>[e,t.value]))),t=calculateEstimate(e);$("#share-output").textContent=e.sonnetShare+"%",$("#routed-cost").textContent=money(t.routedCost),$("#saving-cost").textContent=money(t.saving),$("#saving-percent").textContent=t.savingPercent.toFixed(1)+"%",$("#formula").innerHTML=money(t.opusWork)+" Opus work<br>+ "+money(t.sonnetWork)+" Sonnet work<br>+ "+money(t.classifierCost)+" classifier<br>+ "+money(t.handoffCost)+" handoffs"}function renderCompression(){const e=Number(range.value);$(".compact-context").style.width=e+"%";const t=Math.round(evidence.handoff.compactTokens+(e-15)/85*(evidence.handoff.fullTokens-evidence.handoff.compactTokens));$("#compress-output").textContent=t.toLocaleString()+" tokens",$(".compact-context b").textContent=t.toLocaleString()+" input tokens"}import{evidence,sessionSteps}from"./evidence.js";import{calculateEstimate,presets}from"./calculator.js";const $=(e,t=document)=>t.querySelector(e),$$=(e,t=document)=>[...t.querySelectorAll(e)],money=e=>"$"+e.toFixed(2),terminal=$("#terminal-lines"),smartLines=[["USER","Fix the failing slugify tests.",""],["ROUTER","sonnet \xb7 0.96 confidence","router"],["SONNET","Reads test_slugify.py","sonnet"],["ROUTER","sonnet \xb7 0.98 confidence","router"],["SONNET","Applies one-line delimiter fix","sonnet"],["RESULT","3 tests passed","success"]];$$("[data-mode]").forEach((e=>{e.addEventListener("click",(()=>{$$("[data-mode]").forEach((t=>{const n=t===e;t.classList.toggle("active",n),t.setAttribute("aria-pressed",String(n))})),renderSession(e.dataset.mode)}))})),renderSession();const calcInputs={baseline:$("#baseline"),sonnetShare:$("#sonnet-share"),handoffs:$("#handoffs"),overhead:$("#overhead")};Object.values(calcInputs).forEach((e=>e.addEventListener("input",(()=>{$$("[data-preset]").forEach((e=>e.classList.remove("active"))),renderCalculator()})))),$$("[data-preset]").forEach((e=>e.addEventListener("click",(()=>{const t=presets[e.dataset.preset];Object.entries(t).forEach((([e,t])=>{calcInputs[e].value=t})),$$("[data-preset]").forEach((t=>t.classList.toggle("active",t===e))),renderCalculator()})))),renderCalculator();const range=$("#compress-range");range.addEventListener("input",renderCompression),renderCompression();const installCommand=["git clone git@github.com:arshiaafzal/smart-ask.git","cd smart-ask","cp scripts/claude-smart-ask.local.env.example scripts/claude-smart-ask.local.env","# Add ANTHROPIC_API_KEY, then:","./scripts/claude-smart-ask"].join("\n");$$(".copy-button").forEach((e=>e.addEventListener("click",(async()=>{const t="copy-install"===e.id?installCommand:e.dataset.copy;try{await navigator.clipboard.writeText(t);const n=e.textContent;e.textContent="Copied \u2713",setTimeout((()=>{e.textContent=n}),1800)}catch{e.textContent="Select & copy"}}))));const menuButton=$(".menu-toggle");if(menuButton.addEventListener("click",(()=>{const e="true"!==menuButton.getAttribute("aria-expanded");menuButton.setAttribute("aria-expanded",String(e)),$("#nav-links").classList.toggle("open",e)})),$$(".nav-links a").forEach((e=>e.addEventListener("click",(()=>{menuButton.setAttribute("aria-expanded","false"),$("#nav-links").classList.remove("open")})))),"IntersectionObserver"in window&&!matchMedia("(prefers-reduced-motion: reduce)").matches){const e=new IntersectionObserver((t=>{t.forEach((t=>{t.isIntersecting&&(t.target.classList.add("visible"),e.unobserve(t.target))}))}),{threshold:.1});$$(".reveal").forEach((t=>e.observe(t)))}else $$(".reveal").forEach((e=>e.classList.add("visible")));document.documentElement.dataset.sessionCost=sessionSteps.reduce(((e,t)=>e+t.smart),0).toFixed(4);