<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://arshiaafzal.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://arshiaafzal.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-03T17:40:05+00:00</updated><id>https://arshiaafzal.github.io/feed.xml</id><title type="html">blank</title><subtitle>Arshia Afzal personal page. </subtitle><entry><title type="html">Raven (Part-1)</title><link href="https://arshiaafzal.github.io/blog/2026/raven-part1/" rel="alternate" type="text/html" title="Raven (Part-1)"/><published>2026-04-12T00:00:00+00:00</published><updated>2026-04-12T00:00:00+00:00</updated><id>https://arshiaafzal.github.io/blog/2026/raven-part1</id><content type="html" xml:base="https://arshiaafzal.github.io/blog/2026/raven-part1/"><![CDATA[<div style="background:#f8f9fa; border-left:3px solid #adb5bd; padding:0.6rem 1rem; margin-bottom:0.75rem; border-radius:0 4px 4px 0; font-size:0.9rem;"> This post is cross-posted at <a href="https://goombalab.github.io/blog/2026/raven-part1/" target="_blank">Goomblab Blog</a>. </div> <div style="background:#f8f9fa; border-left:3px solid #adb5bd; padding:0.6rem 1rem; margin-bottom:1.5rem; border-radius:0 4px 4px 0; font-size:0.9rem;"> <strong>Raven Series</strong> &nbsp;—&nbsp; <strong>Part 1: Memory as a set of Slots</strong> &nbsp;|&nbsp; <a href="/blog/2026/raven-part2/">Part 2: Architecture and Results →</a> </div> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/raven_walnut-480.webp 480w,/assets/img/raven_walnut-800.webp 800w,/assets/img/raven_walnut-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/raven_walnut.png" class="rounded mx-auto d-block" width="55%" height="auto" title="Mischievous raven and walnut treasure" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>🐦‍⬛ Ravens are among the most intelligent birds known — famous for caching food across hundreds of locations and retrieving each item with remarkable precision. Just like our new model, which stores information selectively and can retrieve it cleanly when needed!</p> <h2 id="the-recall-gap">The Recall Gap</h2> <p>Mamba <d-cite key="mamba"></d-cite> showed that attention <d-cite key="attention"></d-cite> is not strictly necessary for strong language modeling. A simple recurrent model could match, and sometimes outperform, Transformers on language modeling at a fraction of the cost. But there is no free lunch. Soon after, several works exposed a critical limitation: fixed-size memory models struggle on recall benchmarks, which is not surprising, since they have only a finite amount of room to store information. While it’s tempting to blame their finite capacity, the reality is more nuanced.</p> <p>Consider Needle-In-The-Haystack (NIAH). The model is first given a lookup key, then a long dictionary, and finally asked to retrieve the matching value. This is a form of <strong>constant-memory recall</strong>: in principle, it requires only $O(1)$ memory, since the model only needs to retain the key and its corresponding value as it scans the sequence (unlike <strong>scaling-memory recall</strong> tasks like KV-Retrieval, which require $O(T)$ memory because the query comes after the dictionary).</p> <p>Despite this theoretical $O(1)$ requirement, recurrent models still lag behind Transformers on NIAH. This failure isn’t just a quirk of synthetic benchmarks; it mirrors real-world bottlenecks. In multiple-choice questions, a recurrent model may identify the correct answer early on but fail to preserve that single letter in its state until the output <d-cite key="gna"></d-cite>. Similarly, in math and code, recurrent models struggle to track a single variable or a few intermediate values over long distances.</p> <p>The problem is not a lack of space: the model has enough capacity to store the information. Rather, it lacks the structure needed to organize its state, so new data interferes with what is already stored instead of maintaining its <strong>persistence</strong>.</p> <h2 id="from-global-states-to-partitioned-slots-">From Global States to Partitioned Slots 👕</h2> <blockquote> <p><em>Can we design a recurrent model with strong constant-memory recall that approaches theoretical limits?</em></p> </blockquote> <p>To build intuition, think of the hidden state as a closet. Good organization is not just about how much space you have, but about how that space is structured. When a new item arrives, you usually do two things:</p> <p><strong>1. <em>Choose where it should go.</em></strong> (Routing) <br/> <strong>2. <em>Clear just enough space there to fit it.</em></strong> (Squeezing / Decay)</p> <p>Memory in recurrent models faces the same dual requirement. A model should decide which part of its state should store the new information, and it should update that part without unnecessarily altering the rest. This motivates the idea of <strong>slots</strong>: subsets of the hidden state that can be updated independently. With this structure, a new token can modify only the relevant slot(s), while other stored information remains undisturbed.</p> <h3 id="ssms-write-blindly-and-decay-uniformly">SSMs write blindly and decay uniformly</h3> <p>State space models (SSMs) <d-cite key="mamba,mamba2"></d-cite> and linear attention <d-cite key="linearattn"></d-cite> all share the same fundamental memory operation — a matrix-valued hidden state $S_t \in \mathbb{R}^{M\times d}$ updated by a <strong>linear time-dependent</strong> recurrence:</p> \[S_t = \underbrace{S_{t-1}A_t}_{\text{Decay}} + \underbrace{v_t k_t^\top}_{\text{Write}}, \qquad o_t = \underbrace{S_t q_t}_{\text{Read}}\] <p>SSMs are strong at the <strong>reorganization</strong> step. The forget gate $A_t$ decays old content to make room for new information. But they do not control <em>where</em> information is written.</p> <p>In the diagonal case ($A_t=\text{diag}(a_t)$), the update simplifies to $S_t = S_{t-1} \odot a_t + v_t k_t^\top$. Here, the scalar $a_t$ applies uniform decay across the entire state. Because the write $v_t k_t^\top$ lands across all $M$ rows, every new token perturbs everything already stored. There is little isolation between memory contents, so interference accumulates.</p> <div style="margin: 1.5rem auto 1rem; display: flex; justify-content: center; width: 100%;"> <iframe id="ssm-recurrent-matrix-update" src="/assets/html/ssm_recurrent_matrix_update.html" title="SSM recurrent matrix update visualization" loading="lazy" scrolling="no" style="display: block; width: 70%; max-width: 1400px; height: 0; border: 0; border-radius: 0rem; background: transparent; overflow: hidden;"></iframe> </div> <h3 id="swa-writes-precisely-but-forcefully-evicts">SWA writes precisely but forcefully evicts</h3> <p>Sliding window attention (SWA) takes the opposite approach. It maintains a fixed cache and, at each step, $\color{red}{\text{drops}}$ the oldest token to $\color{green}{\text{append}}$ the newest one. Written as a matrix update using a one-hot selector $e_t$:</p> \[[S^{k}_t, S^v_t] = \underbrace{(\mathbf{1} - e_t)}_{\color{red}{\text{Remove oldest}}}\odot[S^{k}_{t-1}, S^v_{t-1}] + \underbrace{e_t}_{\color{green}{\text{Write new}}} [k_t^\top, v_t^\top]\] <p>SWA is precise about where it writes: it updates exactly one slot ($e_t$). But it doesn’t reorganize memory well. Instead of compressing or preserving old content, it removes entries outright to make room for new ones.</p> <div style="margin: 1.5rem auto 1rem; display: flex; justify-content: center; width: 100%;"> <iframe id="swa-recurrent-matrix-update" src="/assets/html/swa_recurrent_matrix_update.html" title="SWA recurrent matrix update visualization" loading="lazy" scrolling="no" style="display: block; width: 70%; max-width: 1400px; height: 0; border: 0; border-radius: 0rem; background: transparent; overflow: hidden;"></iframe> </div> <h3 id="the-dual-requirement">The Dual Requirement</h3> <p>SSMs and SWA fail in complementary ways. SSMs update memory too diffusely; SWA updates memory too rigidly.</p> <p>Bridging the recall gap requires a mechanism that can do both: treat subsets of the hidden state as independent slots, and decide which ones should be updated versus preserved. This dual requirement—routing and decay—is the foundation of our framework.</p> <h2 id="routing-slot-memories-rsm">Routing Slot Memories (RSM)</h2> <p>Bridging the gap between “blind writes” and “forced eviction” requires a unifying framework. Instead of writing everywhere (SSM) or writing to a fixed rotation (SWA), an <strong>RSM</strong> uses a <strong>learned sparse router</strong> $r_t \in \mathbb{R}^M$.</p> <p>This router looks at the current token and determines which slots should change and which should remain untouched:</p> \[S_t = \underbrace{(\mathbf{1} - r_t) \odot S_{t-1}}_{\text{Persistence}} + \underbrace{r_t \odot \bigl(D_t\,S_{t-1}\,A_t + U_t\bigr)}_{\text{Selective Update}}\] <p>By making $r_t$ sparse, the RSM treats the state as a collection of independent subspaces:</p> <ul> <li><strong>The Persistence Term:</strong> If the router does not select a slot ($r_t = 0$), that memory remains mathematically identical to the previous step. It is “shielded.”</li> <li><strong>The Update Term:</strong> Only the “active” slots undergo the decay ($D_t, A_t$) and write ($U_t$) process.</li> </ul> <p>By simply changing the behavior of the router $r_t$, this single equation provides a unified view of several prominent sequence model families:</p> <div style="margin: 1.5rem auto 1rem; display: flex; justify-content: center; width: 100%; overflow-x: auto;"> <iframe id="comparison-table" src="/assets/html/comparison_table.html" title="Comparison table" loading="lazy" scrolling="no" style="display: block; width: 100%; max-width: 100%; height: 0; border: 0; border-radius: 0rem; background: transparent; overflow: hidden;"></iframe> </div> <p>A dense router ($r_t = \mathbf{1}$) recovers an SSM, where every token updates the full state. A one-hot cyclic router ($r_t = e_t$) recovers SWA, where each token updates a single slot in a fixed rotation.</p> <p>The question then becomes concrete: can a model learn, purely from data, which slot each token should occupy, and preserve that assignment over time?</p> <p>Our answer is <strong>Raven</strong>. By using a <strong>learned sparse router</strong>, Raven treats its hidden state like a Mixture-of-Experts <d-cite key="moe"></d-cite> for memory. It can place critical information into dedicated slots and avoid corrupting those slots with irrelevant filler tokens.</p> <div style="margin: 1.5rem auto 1rem; display: flex; justify-content: center; width: 100%;"> <iframe id="raven-recurrent-matrix-update" src="/assets/html/raven_recurrent_matrix_update.html" title="Raven recurrent matrix update visualization" loading="lazy" scrolling="no" style="display: block; width: 70%; max-width: 1400px; height: 0; border: 0; border-radius: 0rem; background: transparent; overflow: hidden;"></iframe> </div> <p>In <strong>Part 2</strong>, we’ll move from theory to practice: we’ll look at the specific architecture of the Raven block, the “counterintuitive” design decisions that made it work, and how this organized memory allows it to recall information $16\times$ beyond its training length.</p> <script>
  (() => {
    const iframe = document.getElementById('raven-recurrent-matrix-update');
    if (!iframe) return;

    const resizeIframe = () => {
      try {
        const doc = iframe.contentDocument || iframe.contentWindow?.document;
        if (!doc || !doc.body || !doc.documentElement) return;

        const height = Math.max(
          doc.body.scrollHeight,
          doc.documentElement.scrollHeight,
          doc.body.offsetHeight,
          doc.documentElement.offsetHeight
        );

        iframe.style.height = `${height}px`;
      } catch (error) {
        // Ignore cross-document timing issues during initial load.
      }
    };

    iframe.addEventListener('load', () => {
      resizeIframe();
      window.setTimeout(resizeIframe, 0);
      window.setTimeout(resizeIframe, 250);
      window.setTimeout(resizeIframe, 1000);
    });

    window.addEventListener('resize', resizeIframe);
  })();
</script> <script>
  (() => {
    const iframe = document.getElementById('swa-recurrent-matrix-update');
    if (!iframe) return;

    const resizeIframe = () => {
      try {
        const doc = iframe.contentDocument || iframe.contentWindow?.document;
        if (!doc || !doc.body || !doc.documentElement) return;

        const height = Math.max(
          doc.body.scrollHeight,
          doc.documentElement.scrollHeight,
          doc.body.offsetHeight,
          doc.documentElement.offsetHeight
        );

        iframe.style.height = `${height}px`;
      } catch (error) {
        // Ignore cross-document timing issues during initial load.
      }
    };

    iframe.addEventListener('load', () => {
      resizeIframe();
      window.setTimeout(resizeIframe, 0);
      window.setTimeout(resizeIframe, 250);
      window.setTimeout(resizeIframe, 1000);
    });

    window.addEventListener('resize', resizeIframe);
  })();
</script> <script>
  (() => {
    const iframe = document.getElementById('ssm-recurrent-matrix-update');
    if (!iframe) return;

    const resizeIframe = () => {
      try {
        const doc = iframe.contentDocument || iframe.contentWindow?.document;
        if (!doc || !doc.body || !doc.documentElement) return;

        const height = Math.max(
          doc.body.scrollHeight,
          doc.documentElement.scrollHeight,
          doc.body.offsetHeight,
          doc.documentElement.offsetHeight
        );

        iframe.style.height = `${height}px`;
      } catch (error) {
        // Ignore cross-document timing issues during initial load.
      }
    };

    iframe.addEventListener('load', () => {
      resizeIframe();
      window.setTimeout(resizeIframe, 0);
      window.setTimeout(resizeIframe, 250);
      window.setTimeout(resizeIframe, 1000);
    });

    window.addEventListener('resize', resizeIframe);
  })();
</script> <script>
  (() => {
    const iframe = document.getElementById('comparison-table');
    if (!iframe) return;

    const resizeIframe = () => {
      try {
        const doc = iframe.contentDocument || iframe.contentWindow?.document;
        if (!doc || !doc.body || !doc.documentElement) return;

        const height = Math.max(
          doc.body.scrollHeight,
          doc.documentElement.scrollHeight,
          doc.body.offsetHeight,
          doc.documentElement.offsetHeight
        );

        iframe.style.height = `${height}px`;
      } catch (error) {
        // Ignore cross-document timing issues during initial load.
      }
    };

    iframe.addEventListener('load', () => {
      resizeIframe();
      window.setTimeout(resizeIframe, 0);
      window.setTimeout(resizeIframe, 250);
      window.setTimeout(resizeIframe, 1000);
    });

    window.addEventListener('resize', resizeIframe);
  })();
</script>]]></content><author><name>Arshia Afzal$^*$</name></author><summary type="html"><![CDATA[Memory as a set of Slots]]></summary></entry><entry><title type="html">Raven (Part-2)</title><link href="https://arshiaafzal.github.io/blog/2026/raven-part2/" rel="alternate" type="text/html" title="Raven (Part-2)"/><published>2026-04-12T00:00:00+00:00</published><updated>2026-04-12T00:00:00+00:00</updated><id>https://arshiaafzal.github.io/blog/2026/raven-part2</id><content type="html" xml:base="https://arshiaafzal.github.io/blog/2026/raven-part2/"><![CDATA[<div style="background:#f8f9fa; border-left:3px solid #adb5bd; padding:0.6rem 1rem; margin-bottom:0.75rem; border-radius:0 4px 4px 0; font-size:0.9rem;"> This post is cross-posted at <a href="https://goombalab.github.io/blog/2026/raven-part2/" target="_blank">Goomblab Blog</a>. </div> <div style="background:#f8f9fa; border-left:3px solid #adb5bd; padding:0.6rem 1rem; margin-bottom:1.5rem; border-radius:0 4px 4px 0; font-size:0.9rem;"> <strong>Raven Series</strong> &nbsp;—&nbsp; <a href="/blog/2026/raven-part1/">← Part 1: Memory as a set of Slots</a> &nbsp;|&nbsp; <strong>Part 2: Architecture and Results</strong> </div> <h2 id="from-framework-to-model">From Framework to Model</h2> <p>In Part 1, we introduced Routing Slot Memories (RSM), a framework that combines the selective writing of SWA with the gradual forgetting of SSMs. The next step was to turn that framework into a concrete model.</p> <p>RSM is deliberately general. To instantiate Raven, one needs to choose how the router $r_t$ is computed and how the decay term $a_t$ is parameterized. We wanted a design expressive enough to learn meaningful slot assignments, but simple enough to train stably at scale.</p> <p>Our recurrence uses separate key and value states, mirroring SWA, but replaces hard deletion with learned decay:</p> <div style="background:#eff6ff; border-radius:10px; padding:0.5rem 1.5rem; margin:0.5rem 0;"> $$ S^k_t = (1-\exp(a_tr_t)) \odot S^k_{t-1} + \exp(a_tr_t)^\top k_t $$ $$ S^v_t = (1-\exp(a_tr_t)) \odot S^v_{t-1} + \exp(a_tr_t)^\top v_t $$ </div> <p>Unlike standard SSMs <d-cite key="mamba,mamba2"></d-cite> that write to all memory slots at once ($r_t = \mathbf{1}$), Raven writes only to the slots selected by $r_t$. And unlike SWA, which fully overwrites the evicted slot, Raven decays the previous content of the selected slot rather than removing it outright.</p> <p>For routing, we draw inspiration from the DeepSeek Mixture-of-Experts (MoE) family <d-cite key="deepseek_moe"></d-cite>. Each token is projected to a score vector, and only the top-$K$ entries remain active:</p> <div style="background:#eff6ff; border-radius:10px; padding:0.5rem 1.5rem; margin:0.5rem 0;"> $$ \begin{aligned} m_t &amp;= \sigma(Wx_t), \\ g_t &amp;= \mathrm{KeepTop}_K(m_t) = \begin{cases} m_t[i], &amp; \text{if } i \in \mathrm{TopK}(m_t),\\ 0, &amp; \text{otherwise}. \end{cases} \end{aligned} $$ </div> <p>By isolating only the highest-scoring slots, the memory dynamically specializes based on the content of the token. Finally, to keep the effective write magnitude stable, these gated scores are normalized into the final routing weights:</p> <div style="background:#eff6ff; border-radius:10px; padding:0.5rem 1.5rem; margin:0.5rem 0;"> $$ r_t = \frac{g_t}{\alpha\sum_{i=1}^M g_t[i]}. $$ </div> <p>Here, $\alpha$ controls the effective write scale, similarly to the role of temperature-like scaling in GLA <d-cite key="gla"></d-cite>, we used $\alpha=1$ for 400M model size and $\alpha=4$ for 800M model size.</p> <h2 id="design-decisions">Design Decisions</h2> <p>With the recurrence fixed, we turned to the block design.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/raven_arch-480.webp 480w,/assets/img/raven_arch-800.webp 800w,/assets/img/raven_arch-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/raven_arch.png" class="rounded mx-auto d-block" width="60%" height="auto" style=" max-width: 60%; " title="block diagram" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Raven block design vs. other linear models block design.</figcaption> </figure> <p><strong>Dropping the short convolution.</strong> Most linear transformer blocks include a short depthwise convolution before the recurrence — a holdover that helps capture local context. We removed it. The reasoning: SWA is mathematically equivalent to an input-dependent convolution, and Raven’s RSM already subsumes SWA as a special case. Adding a separate convolution on top would be redundant. </p> <p><strong>On norms.</strong> Following common practice in both Transformers and linear attention/SSMs, we apply QK-RMSNorm to queries and keys to stabilize training and avoid gradient explosions, following common practice in SSMs. One interesting exception appears in hybrid Raven, where Raven layers are interleaved with attention. In that setting, removing these norms improves length generalization, especially on recall tasks. We hypothesize that the norms suppress position-related information that the NoPE attention layers rely on. This was one of the more unexpected ablation results.</p> <details><summary>Side Note on RoPE</summary> <p>In early experiments, we applied Rotary Position Embedding (RoPE) <d-cite key="rope"></d-cite> to queries and keys. This substantially hurt Raven’s length generalization, which is one of its main strengths, so we removed it from the final design.</p> </details> <p><strong>No load balancing.</strong> In Mixture-of-Experts models, load-balancing losses are commonly used to spread tokens more evenly across experts. We do not use such a loss in Raven, since its memory is most useful when allocation is uneven. Slots that specialize in retrieval-critical tokens, such as passkeys, variable names, or rare facts, should receive more of those tokens rather than be pushed toward uniform usage. For this reason, we allow the router to specialize freely, and use Gumbel noise during training only to encourage exploration and avoid early collapse. This intuition is supported by our experiments: retrieval-critical tokens naturally cluster into dedicated slots, while other slots remain available for ordinary context.</p> <h2 id="empirical-results">Empirical Results</h2> <p>We now turn to the empirical evaluation. Our main question was whether Raven improves in-context retrieval without sacrificing general language modeling.</p> <h3 id="retrieval-ability">Retrieval Ability</h3> <p>We begin with retrieval. We compare Raven against <em>Mamba-2</em> <d-cite key="mamba2"></d-cite>, <em>GDN</em> <d-cite key="gated_deltanet"></d-cite>, and <em>GLA</em> <d-cite key="gla"></d-cite> on the SSM side, and SWA-like models including <em>SWA+RoPE</em> and <em>GSA</em> <d-cite key="gsa"></d-cite> on the other — and threw Needle-in-a-Haystack tasks at all of them.</p> <p>The baseline behavior is consistent with their memory mechanism: Mamba-2 and GDN write every token to every slot, so over a long sequence, each slot becomes a blurry average of everything it has ever seen. By 8K tokens — already $4\times$ their training length — the passkey signal is too diluted to recover.</p> <div style="margin: 2rem auto; display: flex; flex-direction: column; gap: 0.4rem; align-items: center; max-width: 100%;"> <div style="width: 100%; display: flex; justify-content: center; overflow-y: hidden;"> <iframe id="recall-table" src="/assets/html/recall_table.html" title="Benchmark table" loading="lazy" scrolling="no" style="display: block; width: 100%; height: 420px; border: 0; background: transparent;" onload="(function(f){try{const d=f.contentWindow.document;const resize=function(){f.style.height='0px';const h=Math.max(d.body?d.body.scrollHeight:0,d.documentElement?d.documentElement.scrollHeight:0);f.style.height=(h+2)+'px';};resize();setTimeout(resize,120);setTimeout(resize,500);}catch(e){}})(this)"></iframe> </div> <div style="width: 100%; font-size: 0.85rem; color: #475569; line-height: 1.6; text-align: left;"> <strong style="color: #1e293b; display: block; margin-bottom: 0.5rem;">Table 2: In-context recall benchmarks and NIAH accuracy vs. context length and cache size.</strong> We report accuracy (%) on SWDE/FDA/SQuAD and on single NIAH-1/2/3 across context lengths. Rec. mem. and Conv. mem. denote the millions of cached state elements used during decoding. The common baseline of \(12.5M\) recurrent elements corresponds to a 24-layer model with a total state size of \(0.57M\) per layer (derived from \(2\times\) factors in architectures like GSA/SWA or larger single tensors in Mamba-2/GDN). </div> </div> <p>Raven behaved differently. It held near-perfect accuracy ($\geq \mathbf{99\%}$) all the way to 16K tokens, and remained the <em>only model</em> at the 400M scale to keep strong performance ($&gt; \mathbf{91\%}$) at 32K — that’s $\mathbf{16\times}$ its training length.<br/> <strong>In contrast, Raven is dramatically better at <em>NIAH-1</em> than all other models.</strong><br/> This behavior is consistent with the routing mechanism: the passkey can be written into a dedicated slot and remain recoverable over long spans, rather than being diluted by subsequent tokens.</p> <div style="margin: 1.5rem -8% 0; width: 116%;"> <iframe id="niah-plot" src="/assets/html/niah_plot.html" title="NIAH-1 accuracy interactive plot" loading="lazy" scrolling="no" style="display: block; width: 100%; height: 490px; border: 0; border-radius: 0.5rem; background: transparent;" onload="(function(f){try{var d=f.contentWindow.document;var r=function(){f.style.height='0px';var h=Math.max(d.body?d.body.scrollHeight:0,d.documentElement?d.documentElement.scrollHeight:0);f.style.height=(h+4)+'px';};r();setTimeout(r,150);setTimeout(r,600);}catch(e){}})(this)"></iframe> </div> <h3 id="language-modeling">Language Modeling</h3> <p>A natural question is whether this improvement in retrieval comes at the expense of general language modeling. We had deliberately made the memory uneven — would that hurt perplexity or zero-shot benchmarks?</p> <p>Across standard evaluations, <strong>Raven matched or surpassed</strong> <em>Mamba-2</em> <d-cite key="mamba2"></d-cite>, <em>GLA</em> <d-cite key="gla"></d-cite>, and <em>GDN</em> <d-cite key="gated_deltanet"></d-cite>, as well as strong Transformer baselines like <em>FoX</em>, at both <strong>400M and 800M parameter scales</strong>. It even achieved the <strong>best Lambada performance at 400M</strong>. These results suggest that selective memory does not compromise general language modeling, and may even help by routing tokens to more appropriate slots.</p> <div style="margin: 2rem auto; display: flex; flex-direction: column; gap: 0.4rem; align-items: center; max-width: 100%;"> <div style="width: 100%; display: flex; justify-content: center; overflow-y: hidden;"> <iframe id="lm-table" src="/assets/html/lm_table.html" title="Benchmark table" loading="lazy" scrolling="no" style="display: block; width: 100%; height: 420px; border: 0; background: transparent;" onload="(function(f){try{const d=f.contentWindow.document;const resize=function(){f.style.height='0px';const h=Math.max(d.body?d.body.scrollHeight:0,d.documentElement?d.documentElement.scrollHeight:0);f.style.height=(h+2)+'px';};resize();setTimeout(resize,120);setTimeout(resize,500);}catch(e){}})(this)"></iframe> </div> <div style="width: 100%; font-size: 0.85rem; color: #475569; line-height: 1.6; text-align: left;"> <strong style="color: #1e293b; display: block; margin-bottom: 0.5rem;">Table 3: Language modeling and zero-shot evaluation results. </strong> Perplexity on Lambada (LMB.) and zero-shot accuracy on Lambada, PIQA, HellaSwag, WinoGrande, ARC-e, and ARC-c for Transformer, SSM, and Raven models at 400M and 800M parameter scales. Raven matches strong linear baselines overall while maintaining strong downstream performance. </div> </div> <h3 id="hybrid-raven">Hybrid Raven</h3> <p>We also experimented with a hybrid variant — interleaving Raven RSM layers with standard attention layers. The results here were striking. The hybrid Raven achieved near-perfect NIAH-1 accuracy up to <strong>32K tokens</strong> and strong NIAH-2 performance up to <strong>16K tokens</strong>, while other SSM hybrids like GDN+Attn <d-cite key="gated_deltanet"></d-cite> and Mamba-2+Attn <d-cite key="mamba2"></d-cite> started breaking down at 4K and 2K respectively. The combination of NoPE attention — good at precise short-range retrieval — with Raven’s persistent, content-addressed slots turned out to be unusually complementary.</p> <div style="margin: 2rem 0; width: 100%; display: flex; flex-direction: column; gap: 0.75rem; align-items: stretch;"> <div style="width: 100%; overflow: hidden;"> <iframe id="hybrid-table" src="/assets/html/hybrid_table.html" title="Benchmark table" loading="lazy" scrolling="no" style="display: block; width: 100%; max-width: 100%; height: 420px; border: 0; background: transparent;" onload="(function(f){try{const d=f.contentWindow.document;const resize=function(){f.style.height='0px';const h=Math.max(d.body?d.body.scrollHeight:0,d.documentElement?d.documentElement.scrollHeight:0);f.style.height=(h+2)+'px';};resize();setTimeout(resize,120);setTimeout(resize,500);}catch(e){}})(this)"></iframe> </div> <div style="width: 100%; font-size: 0.85rem; color: #475569; line-height: 1.6; text-align: left;"> <strong style="color: #1e293b; display: block; margin-bottom: 0.5rem;">Table 4: Hybrid Models Retrieval Ability.</strong> Recall ability of hybrid-raven vs other hybrid architechtures. </div> </div> <p>In the 800M hybrid setting, Raven is the best model for in-context recall and length extrapolation — while GDN and SWA+RoPE drop to <strong>0%</strong> accuracy beyond their training length, Raven retains around <strong>80%</strong> accuracy on NIAH-2 at 64K tokens, which is <strong>16×</strong> its training sequence length.</p> <div style="margin: 1.5rem -8% 0; width: 116%;"> <iframe id="niah-hybrid-plot" src="/assets/html/niah_hybrid_plot.html" title="NIAH-2 and NIAH-3 hybrid accuracy interactive plot" loading="lazy" scrolling="no" style="display: block; width: 100%; height: 490px; border: 0; border-radius: 0.5rem; background: transparent;" onload="(function(f){try{var d=f.contentWindow.document;var r=function(){f.style.height='0px';var h=Math.max(d.body?d.body.scrollHeight:0,d.documentElement?d.documentElement.scrollHeight:0);f.style.height=(h+4)+'px';};r();setTimeout(r,150);setTimeout(r,600);}catch(e){}})(this)"></iframe> </div> <h2 id="length-generalization">Length Generalization</h2> <p>One surprising outcome was Raven’s strong length generalization. During evaluation, we found that Raven remained effective far beyond its training length, beyond what had previously been observed in SSM-based models. This behavior was not explicitly designed for, which motivated us to look for a mechanistic explanation. In developing that explanation, we found it useful to think in terms of <strong>Effective Sequence Length (ESL)</strong> (ESL), a concept that came out of our discussion with <a href="https://r-buitrago.github.io/">Ricardo</a>.</p> <div style="margin: 0.6rem auto 2rem; display: flex; flex-direction: column; gap: 0.4rem; align-items: center; max-width: 100%;"> <div style="width: 100%; display: flex; justify-content: center; overflow-y: hidden;"> <iframe id="esl-table" src="/assets/html/esl_figure.html" title="Benchmark table" loading="lazy" scrolling="no" style="display: block; width: 100%; height: 600px; border: 0; background: transparent;" onload="(function(f){try{const d=f.contentWindow.document;const resize=function(){f.style.height='0px';const h=Math.max(d.body?d.body.scrollHeight:0,d.documentElement?d.documentElement.scrollHeight:0);f.style.height=(h+2)+'px';};resize();setTimeout(resize,120);setTimeout(resize,500);}catch(e){}})(this)"></iframe> </div> <div style="width: 100%; font-size: 0.85rem; color: #475569; line-height: 1.6; text-align: left;"> <strong style="color: #1e293b; display: block; margin-bottom: 0.5rem;">Figure: Effective Sequence Length.</strong> Normalized effective sequence length for a NIAH-1 sample at sequence length 16K. SWA stores each token in exactly one slot (FIFO). Raven shows the hidden state $S_t$ for layer 1, head 1 (256 slots, Top$_{32}$). SSM stores each token in all slots with decay. Slots are reordered by usage frequency; results correspond to 400M parameter models. </div> </div> <p>In a standard SSM, every slot receives every token, so the effective sequence length of each slot is $T$. In SWA, each slot receives exactly $\frac{T}{M}$ tokens, yielding uniform allocation across slots. In Raven, routing produces a more heterogeneous pattern: some slots receive many tokens, while others receive only a small subset, including rare retrieval-critical tokens. As a result, different slots operate over different effective sequence lengths, which may help explain Raven’s improved length generalization.</p> <p>To make this concrete, we visualize Raven’s hidden state on a synthetic NIAH task:</p> <div style="margin: 1.5rem auto; display: flex; flex-direction: column; gap: 0.4rem; align-items: center; max-width: 100%;"> <div style="width: 100%;"> <iframe id="raven-memory-dynamics" src="/assets/html/raven_memory_dynamics.html?v=12" title="Raven Memory Dynamics — interactive slot allocation visualization" loading="lazy" scrolling="no" tabindex="-1" style="display: block; width: 100%; height: 220px; border: 0; background: transparent;" onload="(function(f){try{const minH=220,maxH=520;f.style.height=minH+'px';const r=function(){const d=f.contentWindow&amp;&f.contentWindow.document;if(!d)return;const h=Math.max(d.body?d.body.scrollHeight:0,d.documentElement?d.documentElement.scrollHeight:0);if(h&gt;0){const target=Math.min(maxH,Math.max(minH,h+4));f.style.height=target+'px';}};r();[80,220,500,900,1400].forEach(function(ms){setTimeout(r,ms);});}catch(e){}})(this)"></iframe> </div> <div style="width: 100%; font-size: 0.85rem; color: #475569; line-height: 1.8; text-align: left;"> <strong style="color: #1e293b; display: block; margin-bottom: 0.4rem;">Figure: Raven Memory Dynamics &mdash; Layer 23, Head 4.</strong> The box is Raven's recurrent memory: 512 slots arranged left-to-right, each displayed as a vertical line. The memory starts completely empty (gray) and fills token by token as the prompt is processed. <span style="color:#dc1010; font-weight:600;">&#9646; Red slots</span> are claimed by the passkey tokens (<em>RecallSSM1378</em>) — the router directs them into a dedicated region and keeps them there. <span style="color:#1a8fe0; font-weight:600;">&#9646; Blue slots</span> receive both passkey and context writes and act as shared memory. <span style="color:#12b554; font-weight:600;">&#9646; Green slots</span> absorb the surrounding context tokens only. Brightness grows each time a slot is revisited — darker means written once, fully saturated means written many times. When <strong>Raven:</strong> begins generating the answer, it reads from the red region and the retrieval slots brighten further, showing the model recovering exactly what it stored. </div> </div> <p>The passkey — shown in $\color{red}{red}$ — is routed into a dedicated region of the hidden state and remains separated from most ordinary tokens. Ordinary tokens shown in $\color{green}{green}$ flow through the general-purpose slots without touching it. Some slots shown in $\color{blue}{blue}$ serve both roles. Crucially, different heads allocate different amounts of memory to retrieval-critical tokens — the model discovered this specialization entirely on its own, without any explicit supervision.</p> <p>This is what <strong>“organizing memory like a closet”</strong> looks like from the inside.</p> <h2 id="final-notes-and-future">Final Notes and Future</h2> <p>Raven began from a simple goal: improving recall in linear models. More broadly, it suggests that learned memory can benefit from structured, content-based allocation rather than uniform updates across slots. In Raven, a relatively simple routing mechanism is enough to improve recall while remaining competitive on language modeling, with the additional benefit of strong length generalization.</p> <p>There is plenty left to explore. How far can the length generalization be pushed? Can the routing mechanism be made even more expressive? What happens at truly large scales? We don’t have all the answers yet — but we’re working on it.</p>]]></content><author><name>Arshia Afzal$^*$ 🎙️</name></author><summary type="html"><![CDATA[Architecture and Results]]></summary></entry><entry><title type="html">The Attention Zoo: Linear &amp;amp; Softmax Models Unified FUCK</title><link href="https://arshiaafzal.github.io/SSM_Story/" rel="alternate" type="text/html" title="The Attention Zoo: Linear &amp;amp; Softmax Models Unified FUCK"/><published>2026-04-06T00:00:00+00:00</published><updated>2026-04-06T00:00:00+00:00</updated><id>https://arshiaafzal.github.io/attention-zoo</id><content type="html" xml:base="https://arshiaafzal.github.io/SSM_Story/"><![CDATA[<style>#az-root *{box-sizing:border-box}#az-root{--c-bg:#f8fafc;--c-card:#fff;--c-border:#e2e8f0;--c-text:#0f172a;--c-muted:#64748b;--c-subtle:#f1f5f9;--c-linear:#6366f1;--c-softmax:#d97706;--c-delta:#059669;--c-diag:#7c3aed;--c-scalar:#dc2626;--c-none:#475569;--radius:12px;--trans:.25s cubic-bezier(.4,0,.2,1);font-family:'Inter','Segoe UI',system-ui,sans-serif;background:var(--c-bg);border-radius:20px;padding:2rem 1.75rem;margin:2rem -1rem;color:var(--c-text);box-shadow:0 4px 24px rgba(0,0,0,.07),0 1px 4px rgba(0,0,0,.04)}.az-label{font-size:.7rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--c-muted);display:flex;align-items:center;gap:.5rem;margin-bottom:.65rem}.az-label::after{content:'';flex:1;height:1px;background:var(--c-border)}.az-pills{display:flex;flex-wrap:wrap;gap:.45rem;margin-bottom:1.1rem}.az-pill{padding:.38rem 1rem;border-radius:100px;border:1.5px solid var(--c-border);background:#fff;color:var(--c-muted);font-size:.82rem;font-weight:600;cursor:pointer;transition:all var(--trans);user-select:none}.az-pill:hover{border-color:#94a3b8;color:var(--c-text)}.az-pill.active{background:var(--pill-c);border-color:var(--pill-c);color:#fff;box-shadow:0 0 0 3px color-mix(in srgb,var(--pill-c) 25%,transparent)}[data-type="linear"]{--pill-c:var(--c-linear)}[data-type="softmax"]{--pill-c:var(--c-softmax)}[data-decay="delta"]{--pill-c:var(--c-delta)}[data-decay="diagonal"]{--pill-c:var(--c-diag)}[data-decay="scalar"]{--pill-c:var(--c-scalar)}[data-decay="none"]{--pill-c:var(--c-none)}[data-type="all"],[data-decay="all"]{--pill-c:#334155}.az-hr{height:1px;background:var(--c-border);margin:1.25rem 0}#az-grid{display:flex;flex-direction:column;gap:1.75rem}.az-card{background:var(--c-card);border:1px solid var(--c-border);border-radius:var(--radius);overflow:hidden;box-shadow:0 2px 12px rgba(0,0,0,.05);animation:az-in .4s cubic-bezier(.34,1.56,.64,1) both;border-top:3px solid var(--card-accent,#6366f1)}@keyframes az-in{from{opacity:0;transform:translateY(16px) scale(.98)}to{opacity:1;transform:none}}.az-card-header{padding:1.1rem 1.5rem .9rem;display:flex;align-items:flex-start;justify-content:space-between;gap:1rem;border-bottom:1px solid var(--c-border);background:var(--c-subtle)}.az-card-name{font-size:1.3rem;font-weight:800;letter-spacing:-.02em;color:#0f172a}.az-card-full{font-size:.8rem;color:var(--c-muted);margin:.1rem 0 0}.az-paper-meta{margin:.25rem 0 .45rem;line-height:1.45}.az-paper-title{display:block;font-size:.71rem;font-style:italic;color:#475569}.az-paper-authors{display:block;font-size:.68rem;font-weight:600;color:var(--c-muted);margin-top:.1rem}.az-paper-btn{display:inline-flex;align-items:center;gap:.3rem;font-size:.72rem;font-weight:600;color:var(--c-muted);text-decoration:none;padding:.22rem .65rem;border:1px solid var(--c-border);border-radius:6px;background:#fff;transition:all .2s;flex-shrink:0}.az-paper-btn:hover{color:#0f172a;border-color:#94a3b8;text-decoration:none}.az-card-badges{display:flex;flex-direction:column;align-items:flex-end;gap:.3rem;flex-shrink:0}.az-badge{font-size:.67rem;font-weight:700;padding:.18rem .58rem;border-radius:100px;letter-spacing:.05em;text-transform:uppercase}.b-linear{background:#eef2ff;color:#4338ca;border:1px solid #c7d2fe}.b-softmax{background:#fffbeb;color:#b45309;border:1px solid #fde68a}.b-delta{background:#ecfdf5;color:#065f46;border:1px solid #a7f3d0}.b-diagonal{background:#f5f3ff;color:#5b21b6;border:1px solid #ddd6fe}.b-scalar{background:#fef2f2;color:#991b1b;border:1px solid #fecaca}.b-none{background:#f8fafc;color:#334155;border:1px solid #cbd5e1}.az-card-body{display:grid;grid-template-columns:60% 40%;min-height:280px}@media(max-width:680px){.az-card-body{grid-template-columns:1fr}}.az-desc{padding:1.2rem 1.4rem;border-right:1px solid var(--c-border);font-size:.8rem;line-height:1.75;color:#334155}.az-desc p{margin:0 0 .75rem}.az-desc p:last-child{margin:0}.az-desc strong{color:#0f172a}.az-arch{padding:.75rem 1rem;display:flex;align-items:center;justify-content:center;background:#fff}.az-arch-img{width:100%;max-width:400px;display:block;height:auto;border-radius:8px;box-shadow:0 1px 8px rgba(0,0,0,.07)}.az-eq-row{display:flex;flex-wrap:wrap;border-top:1px solid var(--c-border);background:var(--c-subtle);border-radius:0 0 var(--radius) var(--radius)}.az-eq-cell{flex:1;min-width:260px;padding:.7rem 1.3rem;border-right:1px solid var(--c-border)}.az-eq-cell:last-child{border-right:0}.az-eq-lbl{font-size:.62rem;font-weight:700;letter-spacing:.1em;text-transform:uppercase;color:var(--c-muted);margin-bottom:.25rem;display:flex;align-items:center;gap:.3rem}.az-eq-lbl::before{content:'';display:inline-block;width:5px;height:5px;border-radius:50%;background:var(--card-accent,#6366f1);flex-shrink:0}
.az-eq-val{font-size:.9rem;overflow-x:auto}.az-eq-extra{margin-top:.45rem;display:flex;align-items:baseline;justify-content:space-between;gap:.6rem}.az-opt-tag{font-size:.58rem;font-weight:700;letter-spacing:.07em;text-transform:uppercase;color:#94a3b8;border:1px solid #cbd5e1;border-radius:3px;padding:1px 5px;white-space:nowrap}.az-aside{margin-top:.9rem;padding:.6rem .9rem;border-left:3px solid #cbd5e1;background:#f8fafc;border-radius:0 4px 4px 0;font-size:.75rem;color:#64748b;line-height:1.6}.az-aside-lbl{display:inline-block;font-weight:700;font-size:.62rem;letter-spacing:.08em;text-transform:uppercase;color:#94a3b8;margin-bottom:.25rem}#az-empty{text-align:center;padding:3rem 1rem;color:var(--c-muted);display:none}</style> <hr/> <h2 id="intro">Intro</h2> <p>Modern sequence models share a deep mathematical skeleton: a <strong>key-value memory</strong> written to at each step and read by a query. The differences lie in <em>how</em> that memory decays — and this page makes those differences interactive and visual.</p> <p>Filter by <strong>attention kernel</strong> and <strong>memory decay type</strong> below. Each model card shows the architecture diagram for that model.</p> <hr/> <h2 id="interactive-explorer">Interactive Explorer</h2> <div id="az-root"> <div class="az-label">Attention Type</div> <div class="az-pills" id="az-attn"> <button class="az-pill active" data-type="all">All</button> <button class="az-pill" data-type="linear">Linear</button> <button class="az-pill" data-type="softmax">Softmax</button> </div> <div class="az-label">Decay Type <span style="font-weight:400;font-size:.65rem;letter-spacing:0;text-transform:none;margin-left:.25rem">(exact match — selects unique models)</span></div> <div class="az-pills" id="az-decay"> <button class="az-pill active" data-decay="all">All</button> <button class="az-pill" data-decay="none">None</button> <button class="az-pill" data-decay="delta">Delta-Rule</button> <button class="az-pill" data-decay="diagonal">Diagonal</button> <button class="az-pill" data-decay="scalar">Scalar</button> </div> <div class="az-hr"></div> <div id="az-grid"></div> <div id="az-empty"> <div style="font-size:2rem;margin-bottom:.5rem">🔬</div> <p>No models match this combination. Try a different filter.</p> </div> </div> <script>
const MODELS = [
  {
    id:'la', name:'LinAtt', full:'Linear Attention',
    paperTitle:'Transformers are RNNs: Fast Autoregressive Transformers with Linear Attention',
    paperAuthors:'Angelos Katharopoulos, Apoorv Vyas, Nikolaos Pappas, François Fleuret',
    paper:'https://arxiv.org/pdf/2006.16236',
    attn:'linear', decays:['none'], accent:'#64748b',
    imgRef:'la-custom',
    desc:`<p><strong>LinAtt</strong> makes attention cheaper by removing the softmax. Standard attention computes \\(\\operatorname{Softmax}(QK^\\top)V\\); dropping the softmax gives \\((QK^\\top)V\\), which can be reordered to \\(Q(K^\\top V)\\). The key insight is that \\(K^\\top V\\) is a small \\(d \\times d\\) matrix — you compute it once, then multiply each query into it, so cost no longer grows with sequence length. A simple nonlinearity \\(\\phi(x) = \\operatorname{elu}(x)+1\\) is applied to queries and keys to keep all values positive.</p><p>For left-to-right (causal) generation, this is equivalent to running an RNN: at each step you add the new key–value pair to a memory matrix \\(S_t\\), then read the answer out with the query: $$S_t = S_{t-1} + v_t k_t^\\top, \\quad o_t = S_t q_t$$ To stop outputs from growing too large, a running sum \\(z_t\\) of past keys is kept and used to divide the output: $$S_t = S_{t-1} + v_t k_t^\\top, \\quad z_t = z_{t-1} + k_t, \\quad o_t = \\frac{S_t q_t}{z_t^\\top q_t}$$</p><p>The feature map \\(\\phi\\) was adopted by many models that followed. From here on we absorb it into the definitions of \\(q\\) and \\(k\\) to keep the notation clean.</p>`,
    mathR:'S_t = S_{t-1} + v_t k_t^\\top',
    mathRExtra:'z_t = z_{t-1} + k_t',
    mathO:'o_t = S_t q_t',
    mathOExtra:'o_t = S_t q_t \\,/\\, (z_t^\\top q_t)',
  },
  {
    id:'retnet', name:'RetNet', full:'Retentive Network',
    paperTitle:'Retentive Network: A Successor to Transformer for Large Language Models',
    paperAuthors:'Yutao Sun, Li Dong, Shaohan Huang, Shuming Ma, Yuqing Xia, Jilong Xue, Jianyong Wang, Furu Wei',
    paper:'https://arxiv.org/pdf/2307.08621',
    attn:'linear', decays:['scalar'], accent:'#0369a1',
    imgRef:'retnet-custom',
    desc:`<p><strong>RetNet</strong> was introduced shortly after LinAtt to fix one of its core problems: because LinAtt never forgets, the hidden state gets overwhelmed with accumulated context over time. RetNet's fix is simple — multiply the previous state by a fixed scalar \\(\\gamma \\in (0,1)\\) at every step, so older information fades out: $$S_t = \\gamma S_{t-1} + v_t k_t^\\top, \\quad o_t = S_t q_t$$ Keeping \\(\\gamma < 1\\) also prevents the state from exploding during training.</p><p>For parallel training, the decay is folded into a mask matrix \\(M \\in \\mathbb{R}^{T \\times T}\\) applied element-wise to the attention scores: $$O = (QK^\\top \\odot M)V, \\qquad M_{ij} = \\begin{cases} \\gamma^{i-j} & i \\geq j \\\\ 0 & i < j \\end{cases}$$ This recovers the efficiency of standard attention during training while keeping exact recurrent inference at test time.</p>`,
    mathR:'S_t = \\gamma S_{t-1} + v_t k_t^\\top',
    mathO:'o_t = S_t q_t',
  },
  {
    id:'gla', name:'GLA', full:'Gated Linear Attention',
    paperTitle:'Gated Linear Attention Transformers with Hardware-Efficient Training',
    paperAuthors:'Songlin Yang, Bailin Wang, Yikang Shen, Rameswar Panda, Yoon Kim',
    paper:'https://arxiv.org/pdf/2312.06635',
    attn:'linear', decays:['diagonal'], accent:'#7c3aed',
    imgRef:'gla-custom',
    desc:`<p><strong>GLA</strong> addresses a key bottleneck of linear models that rely on <strong>parallel scan</strong> for training: scan-based methods are slow compared to the matrix-multiply-based parallel form of softmax attention. GLA's solution is <strong>chunk-wise training</strong> — unrolling the recurrence over fixed-length chunks and applying a fast parallel form within each chunk, in the same spirit as <a href="https://arxiv.org/pdf/2205.14135" target="_blank">FlashAttention</a>. This is implemented in <a href="https://github.com/sustcsonglin/flash-linear-attention" target="_blank">flash-linear-attention</a>, a hardware-efficient training library for linear transformers.</p><p>The decay is <strong>diagonal and input-dependent</strong>, defined as \\(\\alpha_t = \\operatorname{sigmoid}(w x_t^\\top)^\\tau\\), where \\(\\tau\\) is a temperature parameter controlling how smooth the decay is — a smoother decay helps the model cover a wider context range. The recurrence is: $$S_t = S_{t-1}\\operatorname{Diag}(\\alpha_t) + v_t k_t^\\top, \\quad o_t = S_t q_t$$ Since \\(\\alpha_t < 1\\) for numerical stability, the parallel form is always applied chunk-by-chunk. GLA significantly closes the training-speed gap with Mamba, a goal also addressed concurrently by Mamba-2.</p>`,
    mathR:'S_t = S_{t-1}\\operatorname{Diag}(\\boldsymbol{\\alpha}_t) + v_t k_t^\\top',
    mathO:'o_t = S_t q_t',
  },
  {
    id:'mamba', name:'Mamba', full:'Mamba',
    paperTitle:'Mamba: Linear-Time Sequence Modeling with Selective State Spaces',
    paperAuthors:'Albert Gu*, Tri Dao* &nbsp;(*equal contribution)',
    paper:'https://arxiv.org/pdf/2312.00752',
    attn:'linear', decays:['diagonal'], accent:'#0f766e',
    imgRef:'mamba-custom',
    desc:`<p><strong>Mamba</strong> belongs to the family of <strong>state space models (SSMs)</strong>, following prior work such as <a href="https://arxiv.org/abs/2111.00396" target="_blank">S4</a> that model sequences as discretized linear dynamical systems — hence the name. It was the first such model to succeed at language modelling tasks, and its release brought wide attention back to recurrent architectures. The key step over RetNet is replacing the fixed scalar decay with an <strong>input-dependent diagonal decay</strong>: at each step the model decides how much of the past to keep based on the current input. The full recurrence is: $$\\begin{aligned} S_t &= S_{t-1} \\odot \\exp\\bigl(-(\\alpha_t \\mathbf{1}^\\top) \\odot \\exp(A)\\bigr) + (\\alpha_t \\odot v_t) k_t^\\top \\\\ o_t &= S_t q_t + d \\odot v_t \\end{aligned}$$ Here \\(A\\) and \\(d\\) are learnable parameters; \\(A\\) is diagonal so the decay acts independently on each feature dimension. The specific form of the decay — \\(\\exp(-\\alpha_t \\odot \\exp(A))\\) — comes from <a href="https://en.wikipedia.org/wiki/Zero-order_hold" target="_blank">zero-order-hold discretization</a> of a continuous-time SSM.</p><p>Mamba is trained using <a href="https://developer.nvidia.com/gpugems/gpugems3/part-vi-gpu-computing/chapter-39-parallel-prefix-sum-scan-cuda" target="_blank">parallel scan</a> rather than the matrix-multiply-based parallel form of softmax attention, which is slower on current hardware. Despite this, it achieves comparable accuracy to transformers while being much faster at inference time.</p><div class="az-aside"><div class="az-aside-lbl">Historical notation</div>SSMs like S4 and Mamba write the recurrence using the classical control-theory variables \\(A_t, B_t, C_t\\): $$h_t = A_t h_{t-1} + B_t x_t, \\quad y_t = C_t h_t + D x_t$$ where \\(h_t\\) is the hidden state, \\(x_t\\) is the input token, and \\(D\\) is a skip connection. This is exactly the same update written in different letters — the correspondence to the attention view used throughout this page is: \\(h_t \\leftrightarrow S_t\\) (hidden state), \\(B_t x_t \\leftrightarrow (\\alpha_t \\odot v_t)k_t^\\top\\) (write / value×key), \\(C_t \\leftrightarrow q_t\\) (read / query), \\(D \\leftrightarrow d\\) (skip). The \\(q, k, v\\) view makes the connection to attention explicit and is used by most recent work.</div>`,
    mathDisplay: true,
    mathR:'\\begin{aligned} S_t &= S_{t-1} \\odot \\exp\\bigl(-(\\alpha_t \\mathbf{1}^\\top) \\odot \\exp(A)\\bigr) \\\\ &\\quad + (\\alpha_t \\odot v_t) k_t^\\top \\end{aligned}',
    mathO:'o_t = S_t q_t + d \\odot v_t',
  },
  {
    id:'deltanet', name:'DeltaNet', full:'DeltaNetworks',
    paperTitle:'Parallelizing Linear Transformers with the Delta Rule over Sequence Length',
    paperAuthors:'Songlin Yang, Bailin Wang, Yu Zhang, Yikang Shen, Yoon Kim',
    paper:'https://arxiv.org/pdf/2406.06484',
    attn:'linear', decays:['delta'], accent:'#059669',
    imgRef:'deltanet-custom',
    desc:`<p><strong>DeltaNet</strong> replaces the simple outer-product write of linear attention with the <strong>delta rule</strong>, <a href="https://arxiv.org/abs/2102.11174" target="_blank">first introduced</a> as a biologically-inspired weight update for online learning. At each step, the model reads its current estimate for key \\(k_t\\) from memory — \\(\\hat{v}_t = S_{t-1} k_t\\) — then writes back only the <strong>prediction error</strong> \\(\\beta_t(v_t - \\hat{v}_t)\\), scaled by a per-token learning rate \\(\\beta_t\\): $$\\begin{aligned} S_t &= S_{t-1} + \\beta_t(v_t - S_{t-1}k_t)k_t^\\top \\\\ &= S_{t-1}(\\mathbf{I} - \\beta_t k_t k_t^\\top) + \\beta_t v_t k_t^\\top \\end{aligned}$$ This self-correcting mechanism lets the model <strong>precisely overwrite stale associations</strong> rather than merely accumulating new ones on top. DeltaNet makes this update <strong>efficient and fast to train</strong> by deriving a hardware-friendly chunkwise-parallel form.</p><p>A crucial property of the delta rule update is that the transition matrix \\(\\mathbf{I} - \\beta_t k_t k_t^\\top\\) is <strong>non-diagonal</strong> — it couples feature dimensions together. This is fundamentally different from diagonal SSMs like Mamba or GLA, and it matters: <strong>diagonal state transitions cannot solve state-tracking problems</strong> (such as permutation composition or string transduction), whereas DeltaNet's non-diagonal decay can. Keys are L2-normalised to keep the memory state bounded.</p>`,
    mathR:'S_t = S_{t-1}(\\mathbf{I} - \\beta_t k_t k_t^\\top) + \\beta_t v_t k_t^\\top',
    mathO:'o_t = S_t q_t',
  },
  {
    id:'gated-deltanet', name:'GDN', full:'Gated DeltaNetworks',
    paperTitle:'Gated Delta Networks: Improving Mamba2 with Delta Rule',
    paperAuthors:'Songlin Yang, Jan Kautz, Ali Hatamizadeh',
    paper:'https://arxiv.org/pdf/2412.06464',
    attn:'linear', decays:['delta','scalar'], accent:'#d97706',
    imgRef:'gated-deltanet-custom',
    desc:`<p><strong>Gated DeltaNet (GDN)</strong> extends DeltaNet by adding a <strong>scalar input-dependent forget gate \\(\\alpha_t \\in (0,1)\\)</strong>. Before the corrective delta-rule write, the entire memory is scaled down by \\(\\alpha_t\\), cleanly separating two concerns: how much of the past to retain and how precisely to overwrite specific associations. The recurrence is: $$\\begin{aligned} S_t &= \\alpha_t S_{t-1}(\\mathbf{I} - \\beta_t k_t k_t^\\top) + \\beta_t v_t k_t^\\top \\end{aligned}$$ This gives the model two complementary tools — <strong>rapid bulk forgetting</strong> when context shifts, and <strong>surgical delta-rule rewrites</strong> for fine-grained edits.</p><p>GDN specifically outperforms both <strong>Mamba-2 and DeltaNet on recall-intensive tasks</strong> such as associative recall and multi-query associative recall, where the ability to both forget stale context and precisely overwrite specific memory slots matters most. It preserves the chunkwise-parallel training efficiency of the base DeltaNet.</p>`,
    mathR:'S_t = \\alpha_t S_{t-1}(\\mathbf{I} - \\beta_t k_t k_t^\\top) + \\beta_t v_t k_t^\\top',
    mathO:'o_t = S_t q_t',
  },
  {
    id:'kimi-linear', name:'Kimi Linear', full:'Kimi Delta Attention (KDA)',
    paperTitle:'Kimi Linear: An Expressive, Efficient Attention Architecture',
    paperAuthors:'Kimi Team',
    paper:'https://arxiv.org/pdf/2510.26692',
    attn:'linear', decays:['delta','diagonal'], accent:'#0891b2',
    imgRef:'kimi-linear-custom',
    desc:`<p><strong>Kimi Linear</strong> (KDA) combines the corrective delta-rule update of DeltaNet with a <strong>per-feature diagonal forget gate Λ<sub>t</sub></strong>, giving each memory dimension its own independent retention rate. This is the richest decay structure in the delta-rule family, allowing the model to forget slowly in some feature subspaces while aggressively refreshing others.</p><p>In the deployed Moonshot AI architecture, N KDA layers (each with linear attention and a MoE FFN) are capped by a single MLA softmax layer — achieving near-O(1) inference for 99% of the network while retaining full attention expressivity at the top. The diagonal gate manages long-horizon forgetting; the delta term handles precise short-term overwriting.</p>`,
    mathR:'S_t = S_{t-1}\\operatorname{Diag}(\\boldsymbol{\\alpha}_t)(\\mathbf{I} - \\beta_t k_t k_t^\\top) + \\beta_t v_t k_t^\\top',
    mathO:'o_t = S_t q_t',
  },
  {
    id:'mamba2', name:'Mamba2', full:'Mamba2',
    paperTitle:'Transformers are SSMs: Generalized Models and Efficient Algorithms Through Structured State Space Duality',
    paperAuthors:'Tri Dao*, Albert Gu* &nbsp;(*equal contribution)',
    paper:'https://arxiv.org/pdf/2405.21060',
    attn:'linear', decays:['scalar'], accent:'#0891b2',
    imgRef:'mamba2-custom',
    desc:`<p><strong>Mamba2</strong> establishes the <em>State Space Duality (SSD)</em> framework, proving that structured SSMs with scalar-times-identity decay are mathematically equivalent to a form of linear attention. The scalar gate a<sub>t</sub> multiplies the entire hidden state uniformly at each step, yielding a 1-semiseparable recurrence matrix that enables a highly efficient chunkwise parallel scan.</p><p>Within each chunk, the computation reduces to a dense masked matrix multiplication (tensor-core friendly); cross-chunk state is propagated sequentially. The Parallel Mamba Block pairs a Conv1d for local context mixing with a data-dependent SSM core (A, B, C) and an output gate, achieving 2–8× faster training than Mamba1 at the cost of per-feature decay expressiveness.</p>`,
    mathR:'S_t = \\alpha_t S_{t-1} + v_t k_t^\\top',
    mathO:'o_t = S_t q_t',
  },
  {
    id:'fox', name:'FoX', full:'Forgetting Transformer',
    paperTitle:'Forgetting Transformer: Softmax Attention with a Forget Gate',
    paperAuthors:'Zhixuan Lin, Evgenii Nikishin, Xu Owen He, Aaron Courville',
    paper:'https://arxiv.org/pdf/2503.02130',
    attn:'softmax', decays:['scalar'], accent:'#d97706',
    imgRef:'fox-custom',
    desc:`<p><strong>FoX</strong> augments causal softmax attention with a <strong>scalar input-dependent forget gate f<sub>t</sub></strong> injected as a log-additive bias directly inside the softmax. Each position produces f<sub>t</sub> ∈ (0,1); its cumulative log-product over preceding positions forms a smooth causal decay mask that down-weights distant keys without discarding them entirely.</p><p>Because the gate lives inside the softmax normalisation, FoX preserves full softmax expressiveness — arbitrary key selection and proper probability renormalisation — while dynamically compressing effective context on long sequences. ShiftLinear projections for keys and values prevent look-ahead leakage, and RMSNorm on queries and keys stabilises training across scales.</p>`,
    mathR:'A_{ij} = \\tfrac{q_i^\\top k_j}{\\sqrt{d}} + \\sum_{l=j}^{i-1} \\log f_l',
    mathO:'O = \\operatorname{softmax}(A + M)\\,V',
  },
  {
    id:'nope', name:'NoPE', full:'NoPE Attention',
    paperTitle:'The Impact of Positional Encoding on Length Generalization in Transformers',
    paperAuthors:'Amirhossein Kazemnejad, Inkit Padhi, Karthikeyan Natesan Ramamurthy, Payel Das, Siva Reddy',
    paper:'https://arxiv.org/pdf/2305.19466',
    attn:'softmax', decays:['none'], accent:'#475569',
    imgRef:'nope-custom',
    desc:`<p><strong>NoPE</strong> (No Positional Encoding) studies what happens when a causal Transformer is trained with <em>no</em> positional encoding — no RoPE, ALiBi, or sinusoidal biases. Kazemnejad et al. find that such models generalise surprisingly well to lengths beyond training, challenging the assumption that explicit position signals are necessary.</p><p>As the baseline softmax model in this zoo, NoPE represents pure causal attention: full softmax expressiveness, no memory decay, and no positional prior. Its competitive length-generalisation behaviour motivates asking which structural inductive biases attention truly needs.</p>`,
    mathR:'A_{ij} = \\tfrac{q_i^\\top k_j}{\\sqrt{d}}',
    mathO:'O = \\operatorname{softmax}(A + M)\\,V',
  },
];

let activeAttn = 'all';
let activeDecays = new Set(['all']);

function exactMatch(m) {
  if (activeAttn !== 'all' && m.attn !== activeAttn) return false;
  if (activeDecays.has('all')) return true;
  if (m.decays.length !== activeDecays.size) return false;
  for (const d of m.decays) if (!activeDecays.has(d)) return false;
  return true;
}

function renderCard(m) {
  const attnBadge   = `<span class="az-badge b-${m.attn}">${m.attn}</span>`;
  const decayBadges = m.decays.map(d=>`<span class="az-badge b-${d}">${d}</span>`).join('');
  const eqLbl = m.attn === 'linear' ? 'Recurrence' : 'Attention logit';

  return `<div class="az-card" style="--card-accent:${m.accent}">
  <div class="az-card-header">
    <div>
      <div class="az-card-name">${m.name}</div>
      <div class="az-card-full">${m.full}</div>
      <div class="az-paper-meta">
        <span class="az-paper-title">${m.paperTitle}</span>
        <span class="az-paper-authors">${m.paperAuthors}</span>
      </div>
      <a class="az-paper-btn" href="${m.paper}" target="_blank">
        <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
          <path d="M14 2H6a2 2 0 00-2 2v16a2 2 0 002 2h12a2 2 0 002-2V8z"/>
          <polyline points="14 2 14 8 20 8"/>
        </svg>Paper
      </a>
    </div>
    <div class="az-card-badges">${attnBadge}${decayBadges}</div>
  </div>
  <div class="az-card-body">
    <div class="az-desc">${m.desc}</div>
    <div class="az-arch">
      <img class="az-arch-img"
        src="/assets/img/attention-zoo/${m.imgRef}.png"
        alt="${m.name} architecture" loading="lazy">
    </div>
  </div>
  <div class="az-eq-row">
    <div class="az-eq-cell">
      <div class="az-eq-lbl">${eqLbl}</div>
      <div class="az-eq-val">${m.mathDisplay ? `$$${m.mathR}$$` : `\\(${m.mathR}\\)`}</div>
      ${m.mathRExtra ? `<div class="az-eq-extra"><span class="az-eq-val">\\(${m.mathRExtra}\\)</span><span class="az-opt-tag">optional</span></div>` : ''}
    </div>
    <div class="az-eq-cell">
      <div class="az-eq-lbl">Readout</div>
      <div class="az-eq-val">${m.mathDisplay ? `$$${m.mathO}$$` : `\\(${m.mathO}\\)`}</div>
      ${m.mathOExtra ? `<div class="az-eq-extra"><span class="az-eq-val">\\(${m.mathOExtra}\\)</span><span class="az-opt-tag">optional</span></div>` : ''}
    </div>
  </div>
</div>`;
}

function updateGrid() {
  const grid  = document.getElementById('az-grid');
  const empty = document.getElementById('az-empty');
  const visible = MODELS.filter(exactMatch);
  grid.innerHTML = visible.map(renderCard).join('');
  grid.style.display = visible.length ? 'flex' : 'none';
  empty.style.display = visible.length ? 'none' : 'block';
  if (window.MathJax) MathJax.typesetPromise([grid]).catch(()=>{});
}

document.getElementById('az-attn').addEventListener('click', e => {
  const p = e.target.closest('.az-pill'); if (!p) return;
  activeAttn = p.dataset.type;
  document.querySelectorAll('#az-attn .az-pill').forEach(x => x.classList.toggle('active', x === p));
  updateGrid();
});

document.getElementById('az-decay').addEventListener('click', e => {
  const p = e.target.closest('.az-pill'); if (!p) return;
  const d = p.dataset.decay;
  if (d === 'all')  { activeDecays = new Set(['all']); }
  else if (d === 'none') { activeDecays = new Set(['none']); }
  else {
    activeDecays.delete('all'); activeDecays.delete('none');
    if (activeDecays.has(d)) { activeDecays.delete(d); if (!activeDecays.size) activeDecays.add('all'); }
    else {
      if (d === 'diagonal') activeDecays.delete('scalar');
      if (d === 'scalar') activeDecays.delete('diagonal');
      activeDecays.add(d);
    }
  }
  document.querySelectorAll('#az-decay .az-pill').forEach(x => {
    const dd = x.dataset.decay;
    x.classList.toggle('active', dd === 'all' ? activeDecays.has('all') : activeDecays.has(dd));
  });
  updateGrid();
});

updateGrid();
</script>]]></content><author><name>Arshia Afzal</name></author><category term="attention"/><category term="linear-attention"/><category term="SSM"/><category term="transformer"/><summary type="html"><![CDATA[An interactive guide to modern sequence models — explore architectures and recurrences across the linear-softmax landscape.]]></summary></entry><entry><title type="html">On the Legacy of Linear Transformers in Positional Embeddings 📍</title><link href="https://arshiaafzal.github.io/blog/2026/pe/" rel="alternate" type="text/html" title="On the Legacy of Linear Transformers in Positional Embeddings 📍"/><published>2026-01-18T00:00:00+00:00</published><updated>2026-01-18T00:00:00+00:00</updated><id>https://arshiaafzal.github.io/blog/2026/pe</id><content type="html" xml:base="https://arshiaafzal.github.io/blog/2026/pe/"><![CDATA[<hr/> <h1 id="motivation">Motivation</h1> <p>The literature on linear transformers and State Space Models (SSMs) is progressing rapidly <d-cite key="gu2023mamba"></d-cite> <d-cite key="yang2024parallelizing"></d-cite> <d-cite key="yang2024gated"></d-cite>, with many impressive linear models such as Mamba2 <d-cite key="dao2024transformers"></d-cite> and Gated DeltaNet <d-cite key="gdn"></d-cite>, as well as hybrid architectures that combine linear mechanisms with softmax attention <d-cite key="lieber2024jamba"></d-cite>. At the same time, many ideas from linear transformers and SSMs have been transferred to other areas, most notably to positional embeddings used in softmax transformers <d-cite key="yang2025path"></d-cite>.</p> <p>One of the most well-known examples is <strong>Rotary Position Embedding</strong> (<strong><em>RoPE</em></strong>) <d-cite key="su2021roformer"></d-cite>. One can say, nearly all positional embeddings used in softmax-based models can be traced back to forget gates of linear transformers 😊. The main goal of this blog post is to highlight the <strong>duality between the forget gates of SSMs and positional embeddings (PE)</strong> of softmax transformers.</p> <hr/> <h1 id="softmax-attention-and--position-of-tokens">Softmax Attention and Position of Tokens</h1> <h3 id="softmax-attention">Softmax Attention</h3> <p>Causal softmax attention takes the form:</p> <p>\begin{align} \mathbf{O} = \text{Softmax}\left(\frac{\mathbf{Q} \mathbf{K}^\top}{\sqrt{d}} + \mathbf{M}\right) \mathbf{V} \tag{Matrix Form}<br/> \end{align}</p> \[\mathbf{o_t} = \sum_{i=1}^t \frac{\exp(\mathbf{q^\top_t}\mathbf{k}_i)}{\sum_{j=1}^t \exp(\mathbf{q^\top_t}\mathbf{k}_j)} \mathbf{v}_i \tag{Recurrent Form}\] <p>where $\mathbf{Q}, \mathbf{K}, \mathbf{V}, \mathbf{O} \in \mathbb{R}^{T \times d}$ denote the queries, keys, values, and the output of the softmax attention, respectively, and $\mathbf{M} \in (-\infty, 1)$ is the lower-triangular causal mask. For simplicity, we assume that the values and keys share the same shape and omit the $\sqrt{d}$ scaling factor from here on <d-cite key="vaswani2017attention"></d-cite>.</p> <p>As shown above, <strong>permuting</strong> the order of the input tokens ($\mathbf{X}$) induces the <strong>same permutation</strong> in the attention output ($\mathbf{O}$). Consequently, the attention mechanism itself has no inherent notion of token order and does not encode positional information. Consequently, there is no inherent notion of positional information or explicit <strong><em>“positional embedding”</em></strong>. Softmax attention without any positional embedding, as presented in the equation above, is referred to as <em>No-Position Embedding</em> (<strong>NoPE</strong>) <d-cite key="kazemnejad2023impact"></d-cite>.</p> <h3 id="positional-embedding">Positional Embedding</h3> <p>To inject positional information into tokens, several approaches have been proposed. As an example, vanilla Transformer adds sinusoidal positional embeddings to the input tokens $\mathbf{x}_t$ only after embedding in the first layer. We are mostly interested in positional embeddings that have been shown to be performant, scalable, and sufficiently general to support many variants <d-cite key="yang2025path"></d-cite>. In particular, we focus on <strong>additive</strong> and <strong>multiplicative</strong> forms, which we introduce mathematically below.</p> \[\begin{aligned} \mathbf{o_t} &amp;= \sum_{i=1}^t \frac{ \exp(\mathbf{q_t^\top}\, \textcolor{blue}{\mathbf{D_{tj}}}\, \mathbf{k}_i) }{ \sum_{j=1}^t \exp(\mathbf{q_t^\top}\, \textcolor{blue}{\mathbf{D_{tj}}}\, \mathbf{k}_j) } \mathbf{v}_i \end{aligned} \tag{Multiplicative PE}\] \[\mathbf{o_t} = \sum_{i=1}^t \frac{\exp(\mathbf{q^\top_t}\mathbf{k}_i + \textcolor{blue}{d_{ti}})}{\sum_{j=1}^t \exp(\mathbf{q^\top_t}\mathbf{k}_j + \textcolor{blue}{d_{tj}})} \mathbf{v}_i \tag{Additive PE}\] <p>with $\textcolor{blue} {\mathbf{D_{tj}}} \in \mathbb{R}^{d \times d}$ and $\textcolor{blue}{d_{t j}} \in \mathbb{R}$ denote postional embeddings responsible for decoding the relative position between tokens $t$ and $j$. These matrices (or scalars) may depend on the positions of the tokens ($t$) or may even depend on the inputs themselves $\mathbf{x}_t$.</p> <p>The softmax Transformer, while highly performant, has quadratic <em>computational complexity</em>. To address this limitation, linear attention was introduced.</p> <hr/> <h1 id="linear-transformers-an-overview">Linear Transformers (An Overview)</h1> <h3 id="linear-attention">Linear Attention</h3> <p>Linear attention removes the softmax operation from the Transformer <d-cite key="katharopoulos2020transformers"></d-cite>, resulting in:</p> <p>\begin{align} \mathbf{O} = \left(\mathbf{Q} \mathbf{K}^\top \odot \mathbf{M}\right) \mathbf{V} \tag{Matrix Form}<br/> \end{align}</p> \[\mathbf{o_t} = \sum_{i=1}^t (\mathbf{q^\top_t}\mathbf{k}_i)\mathbf{v}_i \tag{Recurrent Form}\] <p>with $\mathbf{M}\in(0,1)$ denoting a binary causal mask. Examining the recurrent form of linear attention, it can be expressed as a linear recurrence:</p> \[\mathbf{S}_t = \mathbf{S}_{t-1} + \mathbf{v}_t \mathbf{k}^\top_t , \quad \mathbf{o}_t = \mathbf{S}_t\mathbf{q}_t\] <p>with $\mathbf{S}_t \in \mathbb{R}^{d \times d}$ being a two-dimensional hidden state.</p> <h3 id="forget-gate-as-positional-embedding">Forget Gate as Positional Embedding</h3> <p>Vanilla linear Transformers simply accumulate information in their hidden state without forgetting. Soon after the introduction of linear attention, and due to its close connection to RNNs, researchers incorporated forget gates into linear Transformers insipred by RNN litriture <d-cite key="van2018unreasonable"></d-cite>:</p> \[\mathbf{S}_t = \mathbf{S}_{t-1}\textcolor{red}{\mathbf{A}_t} + \mathbf{v}_t \mathbf{k}^\top_t , \quad \mathbf{o}_t = \mathbf{S}_t\mathbf{q}_t\] <p>with $\textcolor{red}{\mathbf{A}_t}$ being a structured matrix (dense, low-rank, diagonal, etc.) extracted from the input $\mathbf{X}$ whose eigenvalues have magnitude less than 1 to ensure stability.</p> <details> <summary>Why Eigenvalues of the Forget Gate Must Be $\le 1$</summary> Assume $\mathbf{A}_t$ has an eigenvalue $\lambda$ with $|\lambda|&gt;1$. Then repeated application yields exponential growth: $$(\mathbf{A}_t)^k v = \lambda^k v \;\;\xrightarrow{k\to\infty}\;\; \infty.$$ If instead $|\lambda(\mathbf{A}_t)|\le 1$, then each gate is non-expansive and $$\left\|\prod_{s=1}^t \mathbf{A}_s\right\|\le \prod_{s=1}^t \|\mathbf{A}_s\|\le 1,$$ ensuring stable and bounded state propagation. </details> <p>$\textcolor{red}{\mathbf{A}_t}$ is commonly referred to as the <em>forget gate</em>, not only decays historical information but also implicitly induces a relative positional embedding in linear Transformers. This effect becomes more apparent by unrolling the recurrence above:</p> \[\mathbf{o}_t = \sum_{j=1}^t \left( \mathbf{k^\top_j} \left( \textcolor{blue} {\prod_{s=j+1}^t \mathbf{A}_s} \right) \mathbf{q}_t \right) \mathbf{v}_j \tag{Unrolled LinAtt}\] <p>Looking at the product $\textcolor{blue}{\prod_{s=j+1}^t \mathbf{A_s}}$, we see that the forget gate implicitly encodes <strong>relative positional information</strong> between positions $j$ and $t$.</p> <p>Considering each $\mathbf{A_s}$ has eigenvalues $\le 1$, the product is contractive and can be written in log-space as $\textcolor{blue}{\prod_{s=j+1}^t \mathbf{A_s} = \exp \left(- \sum_{s=j+1}^t \mathbf{D_s}\right)}$, where each $\mathbf{D_s} \succeq 0$. We show that $\sum_{s=j+1}^t \mathbf{D_s}$ defines a <strong>distance $\mathbf{D}$</strong> that satisfies all metric properties <strong>except symmetry</strong>, which is appropriate for causal (autoregressive) modeling.</p> <details> <summary>Forget Gate in Log-Space is a Distance Metric</summary> Define the distance $$\mathbf{D}(j,t) = \sum_{s=j+1}^t \mathbf{D}_s$$ Non-negativity: Since $\mathbf{D}_s \succeq 0$ for all $s$, $$\mathbf{D}(j,t) \succeq 0$$ Identity of Indiscernibles: If $j=t$, the sum is empty, so $$\mathbf{D}(t,t)=0$$ implying $j=t$. <br/> Triangle Inequality: For $j&lt;k&lt;t$, $$\mathbf{D}(j,t) = \sum_{s=j+1}^k \mathbf{D}_s + \sum_{s=k+1}^t \mathbf{D}_s = \mathbf{D}(j,k)+\mathbf{D}(k,t)$$ TakeAway: $\mathbf{D}(j,t)=\sum_{s=j+1}^t \mathbf{D}_s$ is an asymmetric distance-metric. </details> <p>One of the key differences between different linear recurrent models lies in the structure of the forget gate $\mathbf{A}_t$. Using this insight, we establish a <strong><em>duality</em></strong> between well-known linear Transformers and different PEs, showing how the structure of popular linear models gives rise to well-known PEs.</p> <hr/> <h1 id="1-linear-attention--nope">1) Linear Attention ↔ NoPE</h1> <p>We begin with the simplest case $\mathbf{A_t}=1$, which results in $\textcolor{black} {\prod_{s=j+1}^t \mathbf{A}_s}=1$. Therefore, no relative positional embedding is applied, and the recurrence takes the form:</p> \[\mathbf{S}_t = \mathbf{S}_{t-1} + \mathbf{v}_t \mathbf{k}^\top_t\] <p>The dual position embedding of vanilla linear attention is simply applying the causal mask to softmax attention:</p> \[\mathbf{O} = \text{Softmax}\left(\mathbf{Q} \mathbf{K}^\top + \mathbf{M}\right) \mathbf{V}, \quad \mathbf{M}_{ij} = \begin{cases} \textcolor{blue}{0} &amp; i \ge j \\ -\infty &amp; i &lt; j \end{cases}\] <p>Therefore, $\textcolor{blue}{\mathbf{M}}$ can be interpreted as an additive positional embedding in which all tokens are at the same relative distance from each other.</p> <blockquote> \[\begin{aligned} &amp; \textcolor{black}{\mathbf{S}_t} \textcolor{black}{= \mathbf{S}_{t-1}\textcolor{blue}{1} + \mathbf{v}_t \mathbf{k}^\top_t} \\ &amp; \textcolor{black}{\text{Softmax}\left({\mathbf{Q} \mathbf{K}^\top} + \textcolor{blue}{\mathbf{D}}\right) }, \quad \textcolor{blue} {\mathbf{D_{ij}}} \textcolor{black}{=\begin{cases} \textcolor{blue}{0} &amp; i \ge j \\ - \infty &amp; i&lt;j \end{cases} }\end{aligned}\] </blockquote> <p>The above represents our first connection between linear attentions forgte gate and softmax transformer PE, where the forget gate is equal to $1$, i.e., no forgetting is applied. <strong><em>NoPE</em></strong> <d-cite key="kazemnejad2023impact"></d-cite> has the duality with <strong>Linear Attention</strong>.</p> <hr/> <h1 id="2-retnet--alibi">2) RetNet ↔ AliBi</h1> <p>A first improvement over linear attention is the <strong>Retentive Network (RetNet)</strong> <d-cite key="sun2023retentive"></d-cite>, which introduces a scalar decay $\textcolor{blue}{\mathbf{A}_t = \gamma}\in\mathbb{R}$ as the forget gate:</p> \[\mathbf{S}_t = \mathbf{S}_{t-1}\,\textcolor{blue}{\gamma} + \mathbf{v}_t \mathbf{k}_t^\top\] <p>From the above, we can derive the unrolled cumulative forget gate:</p> \[\textcolor{black}{\prod_{s=j+1}^t \mathbf{A}_s} = \gamma^{\,t-j}.\] <p>By setting $\gamma = \exp(-\lambda)$, the cumulative forget gate can be rewritten as</p> \[\prod_{s=j+1}^t \mathbf{A}_s = \exp\!\big(\textcolor{blue}{-\lambda (t-j)}\big)\] <p>The $\textcolor{blue}{\text{blue}}$ term above is encodes the relative position between tokens $t$ and $j$ and by applying above as <strong>additive</strong> positional embedding to softmax transformer we can reach a well known position embedding of <strong>AliBi</strong> <d-cite key="press2022train"></d-cite>. AliBi simply adds the releative distance of two tokens into their attention score:</p> <blockquote> \[\begin{aligned} &amp; \textcolor{black}{\mathbf{S}_t} \textcolor{black}{= \mathbf{S}_{t-1}\textcolor{blue}{\exp(-\lambda)} + \mathbf{v}_t \mathbf{k}^\top_t} \\ &amp; \textcolor{black}{\text{Softmax}\left({\mathbf{Q} \mathbf{K}^\top} + \textcolor{blue}{\mathbf{D}}\right) }, \quad \textcolor{blue} {\mathbf{D_{ij}}} \textcolor{black}{=\begin{cases} \textcolor{blue}{\lambda (i-j)} &amp; i \ge j \\ - \infty &amp; i&lt;j \end{cases} }\end{aligned}\] </blockquote> <p>Thus, <strong>ALiBi PE</strong>, admits a direct duality with the <strong>RetNet</strong> linear Transformer.</p> <details> <summary>Click to expand AliBi simple code</summary> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="kn">import</span> <span class="n">torch</span> 

<span class="k">def</span> <span class="nf">alibi</span><span class="p">(</span><span class="n">T</span><span class="p">,</span> <span class="n">lambd</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Simple AliBi implementation
    T: Sequence length
    lambd: lambda scaling of alibi
    </span><span class="sh">"""</span>
    <span class="n">idx</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="n">T</span><span class="p">)</span>
    <span class="n">I</span><span class="p">,</span> <span class="n">J</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">meshgrid</span><span class="p">(</span><span class="n">idx</span><span class="p">,</span> <span class="n">idx</span><span class="p">,</span> <span class="n">indexing</span><span class="o">=</span><span class="sh">'</span><span class="s">ij</span><span class="sh">'</span><span class="p">)</span>
    <span class="n">D_Add</span> <span class="o">=</span> <span class="o">-</span> <span class="p">(</span> <span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">abs</span><span class="p">((</span><span class="n">I</span><span class="o">-</span><span class="n">J</span><span class="p">)).</span><span class="nf">float</span><span class="p">())</span> <span class="o">*</span> <span class="n">lambd</span>   <span class="p">).</span><span class="nf">tril</span><span class="p">()</span>      
    <span class="k">return</span> <span class="n">D_Add</span>

<span class="k">def</span> <span class="nf">att_alibi</span><span class="p">(</span><span class="n">Q</span><span class="p">,</span><span class="n">K</span><span class="p">,</span><span class="n">V</span><span class="p">,</span><span class="n">lambd</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Simple FoX Attention
    Q,K,V: input (d , T)
    </span><span class="sh">"""</span>
    <span class="n">D_Add</span> <span class="o">=</span> <span class="nf">alibi</span><span class="p">(</span><span class="n">Q</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span> <span class="p">,</span> <span class="n">lambd</span><span class="p">)</span>
    <span class="n">out</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">Q</span><span class="p">.</span><span class="n">T</span><span class="nd">@K</span> <span class="o">+</span> <span class="n">D_add</span> <span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span><span class="nd">@V.T</span>

    <span class="k">return</span> <span class="n">out</span></code></pre></figure> </details> <hr/> <h1 id="3-mamba2gla--fox">3) Mamba2/GLA ↔ FoX</h1> <p>While RetNet incorporates a forget gate, it lacks <strong>input-dependent selectivity</strong> in the forgetting mechanism. Mamba2 <d-cite key="dao2024transformers"></d-cite> and Gated Linear Attention (GLA) <d-cite key="yang2024gated"></d-cite> explicitly address this limitation by introducing input-dependent forget gates. In particular, GLA uses diagonal forget gate $ \mathbf{A}_t = \sigma(\mathbf{W} x_t)^{1/\tau} \in \mathbb{R}^d, $ while Mamba2 simplifies it into a scalar $ \mathbf{A}_t = \exp\big(-\mathrm{SoftPlus}(\mathbf{w^\top x_t})\big) \in \mathbb{R}. $</p> <p>As an example, the recurrence of GLA is:</p> \[\textcolor{black}{\mathbf{S}_t} \textcolor{black}{= \mathbf{S}_{t-1}\textcolor{blue}{\sigma(\mathbf{Wx_t})^{1/\tau}} + \mathbf{v}_t \mathbf{k}^\top_t}\] <p>Above leads to the following cumulative forget-gate product:</p> \[\prod_{s=j+1}^t \mathbf{A}_s = \exp\!\big(\textcolor{blue}{\sum_{s=j+1}^t \log (\sigma(\mathbf{Wx_t})^{1/\tau}) }\big)\] <p>for the case of GLA. Simplifying GLA’s forget gate from diagonal $\mathbf{A}_t$ into scalar we reach:</p> \[\prod_{s=j+1}^t \mathbf{A}_s = \exp\!\big(\textcolor{blue}{\sum_{s=j+1}^t \log (\sigma(\mathbf{wx_t})^{1/\tau}) }\big)\] <p>The above forget gates can be interpreted as <strong>additive</strong> positional embeddings for Transformers with forget gates, also known as Forgetting Transformers (FoX) <d-cite key="fox"></d-cite>, leading to:</p> <blockquote> \[\begin{aligned} &amp; \textcolor{black}{\mathbf{S}_t} \textcolor{black}{= \mathbf{S}_{t-1}\textcolor{blue}{\sigma(\mathbf{Wx_t})^{1/\tau}} + \mathbf{v}_t \mathbf{k}^\top_t} \\ &amp; \textcolor{black}{\text{Softmax}\left({\mathbf{Q} \mathbf{K}^\top} + \textcolor{blue}{\mathbf{D}}\right) }, \quad \textcolor{blue} {\mathbf{D_{ij}}} \textcolor{black}{=\begin{cases} \textcolor{blue}{\sum_{s=j+1}^t \log (\sigma(\mathbf{wx_t+b})) }&amp; i \ge j \\ - \infty &amp; i&lt;j \end{cases} }\end{aligned}\] </blockquote> <p>Therefore, <strong>FoX</strong>, admits a direct duality with the <strong>Mamba2/GLA</strong> linear Transformers.</p> <details> <summary>Click to expand FoX simple code</summary> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="kn">import</span> <span class="n">torch</span> 

<span class="k">def</span> <span class="nf">fox_pe</span><span class="p">(</span><span class="n">w</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Simple FoX implementation
    x: input (d , T)
    w: learnable weight (d , d)
    </span><span class="sh">"""</span>
    <span class="n">a</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">logsigmoid </span><span class="p">(</span><span class="n">w</span><span class="p">.</span><span class="nf">transpose</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span><span class="o">-</span><span class="mi">2</span><span class="p">)</span><span class="nd">@x</span><span class="p">)</span>  <span class="c1"># (T)
</span>    <span class="n">a</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cumsum</span><span class="p">(</span><span class="n">a</span> <span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>  <span class="c1"># (T)
</span>    <span class="n">D_add</span> <span class="o">=</span> <span class="p">(</span><span class="n">a</span><span class="p">.</span><span class="nf">transpose</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span><span class="o">-</span><span class="mi">2</span><span class="p">)</span> <span class="o">-</span> <span class="n">a</span><span class="p">).</span><span class="nf">tril</span><span class="p">()</span>  <span class="c1"># (T , T)
</span>    <span class="k">return</span> <span class="n">D_Add</span>

<span class="k">def</span> <span class="nf">fox</span><span class="p">(</span><span class="n">Q</span><span class="p">,</span><span class="n">K</span><span class="p">,</span><span class="n">V</span><span class="p">,</span><span class="n">x</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Simple FoX Attention
    Q,K,V: input (d , T)
    w: learnable weight (d , d)
    </span><span class="sh">"""</span>
    <span class="n">D_Add</span> <span class="o">=</span> <span class="nf">fox_pe</span><span class="p">(</span><span class="n">w</span><span class="p">,</span> <span class="n">x</span><span class="p">)</span>
    <span class="n">out</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">Q</span><span class="p">.</span><span class="n">T</span><span class="nd">@K</span> <span class="o">+</span> <span class="n">D_add</span> <span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span><span class="nd">@V.T</span>

    <span class="k">return</span> <span class="n">out</span></code></pre></figure> </details> <hr/> <h1 id="4-complex-forget-gate---rope">4) Complex Forget Gate ↔ RoPE</h1> <p>Let’s now consider queries and keys are complex-valued, with $\mathbf{\tilde{q},\tilde{k}}\in\mathbb{C}^{d/2}$. We then choose a <em>diagonal</em> forget gate with <em>imaginary roots</em>, given by $\mathbf{A}_t = \exp(i\,\boldsymbol{\Omega}) \in \mathbb{C}^{d/2}$ where $\mathbf{\Omega}$ is a diagonal matrix of frequencies. Formally, the recurrence of this linear transformer is:</p> \[\mathbf{S}_t = \mathbf{S}_{t-1}\,\textcolor{blue}{e^{i\mathbf{\Omega}}} + \mathbf{v}_t \tilde{\mathbf{k}}_t^{\mathrm{H}}, \quad \mathbf{o}_t = \mathcal{R} \{\mathbf{S}_t \tilde{\mathbf{q}}_t\}, \quad \mathbf{o}_t = \sum_{j=1}^t \mathbf{v}_j \, \textcolor{blue}{\mathcal{R}\!\left\{\tilde{\mathbf{k}}_j^{\mathrm{H}} e^{i\mathbf{\Omega}(t-j)} \tilde{\mathbf{q}}_t \right\}}.\] <p>with $\mathrm{H}$ being the complex conjugate operation.</p> <p>We now breakdown some details:</p> <ul> <li> <p>Since the queries and keys are complex-valued, $\mathbf{\tilde{q},\tilde{k}}\in\mathbb{C}^{d/2}$, each channel has a real and an imaginary part, giving a total of $d$ floating-point values, as in previous setups.</p> </li> <li> <p>$\mathbf{\Omega}_n = 10000^{-2n/d}$ being a diagonal matrix of angles followed by RoPE <d-cite key="su2021roformer"></d-cite>.</p> </li> <li> <p>We can rewrite the complex-valued queries and keys using amplitude–phase notation as: $\mathbf{\tilde{q}_n} = |\mathbf{\tilde{q}_n}|e^{i\phi (\mathbf{\tilde{q}_n})}, \mathbf{\tilde{k}_n} = |\mathbf{\tilde{k}_n}|e^{i\phi (\mathbf{\tilde{k}_n})}$</p> </li> <li> <p>As $\mathbf{A}_t = \exp(i\boldsymbol{\Omega})$, all its eigenvalues have magnitude of 1; therefore, it leads to <em>stable</em> recurrence. This forget gate does not necessarily <strong>“forgets,”</strong> as it does not have any eigenvalues below one.</p> </li> </ul> <p>Using the phase–amplitude split of the queries and keys and $\mathbf{\Omega}$ being diagonal, the attention score ($\textcolor{blue}{\text{blue}}$ part above) can be expanded as:</p> \[\textcolor{black}{\mathcal{R}\!\left\{\tilde{\mathbf{k}}_j^{\mathrm{H}} e^{i\mathbf{\Omega}(t-j)} \tilde{\mathbf{q}}_t \right\}} = \mathcal{R}\!\left\{ \sum_{n=1}^{d/2} |\mathbf{\tilde{q}_{t,n}}|\,|\mathbf{\tilde{k}_{j,n}}|\, e^{i\left(\mathbf{\Omega_n}(t-j)-\phi (\mathbf{\tilde{q}_{t,n}}) -\phi (\mathbf{\tilde{k}_{j,n}}) \right)} \right\} =\] \[\sum_{n=1}^{d/2} \lvert \mathbf{\tilde{q}_{t,n}} \rvert \, \lvert \mathbf{\tilde{k}_{j,n}} \rvert \, \cos\!\left( \mathbf{\Omega_n} (t - j) - \phi (\mathbf{\tilde{q}_{t,n}}) - \phi (\mathbf{\tilde{k}_{j,n}}) \right).\] <p>Using the cosine identity, we can rewrite the cosine term as applying a 2D rotation matrix $R_{\mathbf{\Omega_n}}^{(t-j)}$ with the angle of rotation $\mathbf{\Omega_n}(t-j)$:</p> \[\cos\left(\mathbf{\Omega_n}(t-\tau)-\phi (\mathbf{\tilde{q}_{t,n}})-\phi (\mathbf{\tilde{k}_{j,n}})\right) =\] \[\begin{bmatrix} \cos(\phi (\mathbf{\tilde{q}_{t,n}})) \\ \sin(\phi (\mathbf{\tilde{q}_{t,n}})) \end{bmatrix}^{\!\top} \! \textcolor{blue} { \underbrace{ \begin{bmatrix} \cos(\mathbf{\Omega_n}(t-j)) &amp; -\sin(\mathbf{\Omega_n}(t-j)) \\ \sin(\mathbf{\Omega_n}(t-j)) &amp; \cos(\mathbf{\Omega_n}(t-j)) \end{bmatrix} }_{\textbf{Rotation Matrix = } R_{\mathbf{\Omega_n}}^{t-j} } } \! \begin{bmatrix} \cos(\phi (\mathbf{\tilde{k}_{j,n}})) \\ \sin(\phi (\mathbf{\tilde{k}_{j,n}})) \end{bmatrix}.\] <p>which we use the fact that $R_{(t-j)\mathbf{\Omega}}=R_{\mathbf{\Omega}}^{(t-j)}$ known as <em>Rotation Composition</em>.</p> <details> <summary>Proof for Rotation Composition</summary> Let the rotation matrix with angle of $\omega$ be: $$ R_\omega := \begin{pmatrix} \cos\omega &amp; -\sin\omega \\ \sin\omega &amp; \cos\omega \end{pmatrix}. $$ Then for any angles $\alpha,\beta$, $$ R_\alpha R_\beta = \begin{pmatrix} \cos\alpha &amp; -\sin\alpha \\ \sin\alpha &amp; \cos\alpha \end{pmatrix} \begin{pmatrix} \cos\beta &amp; -\sin\beta \\ \sin\beta &amp; \cos\beta \end{pmatrix} = \begin{pmatrix} \cos(\alpha+\beta) &amp; -\sin(\alpha+\beta) \\ \sin(\alpha+\beta) &amp; \cos(\alpha+\beta) \end{pmatrix} = R_{\alpha+\beta}. $$ where the equality follows from the trig identities $cos(\alpha+\beta)=\cos\alpha\cos\beta-\sin\alpha\sin\beta$ and $sin(\alpha+\beta)=\sin\alpha\cos\beta+\cos\alpha\sin\beta$ Therefore, we can easily see that: $$ R_\omega^{k+1} = R_\omega^k R_\omega = R_{k\omega}R_\omega = R_{k\omega+\omega} = R_{(k+1)\omega}. $$ Thus $R_\omega^k = R_{k\omega}$ for all $k\ge 0$. Substituting $k=t-j$ gives: $$ R_\omega^{t-j} = R_{(t-j)\omega} = R_{\omega(t-j)}. $$ </details> <p>Applying the rotation matrix to the output of the recurrence:</p> \[\sum_{n=1}^{d/2} \lvert \mathbf{\tilde{q}_{t,n}} \rvert \, \lvert \mathbf{\tilde{k}_{j,n}} \rvert \, \cos\!\left( \mathbf{\Omega_n} (t - j) - \phi (\mathbf{\tilde{q}_{t,n}}) - \phi (\mathbf{\tilde{k}_{j,n}}) \right) =\] \[\sum_{n=1}^{d/2} \lvert \mathbf{\tilde{q}_{t,n}} \rvert \, \lvert \mathbf{\tilde{k}_{j,n}} \rvert \, \begin{bmatrix} \cos(\phi (\mathbf{\tilde{q}_{t,n}})) \\ \sin(\phi (\mathbf{\tilde{q}_{t,n}})) \end{bmatrix}^{\!\top} \! \textcolor{blue}{R_{\mathbf{\Omega_n}}^{t-j}} \! \begin{bmatrix} \cos(\phi (\mathbf{\tilde{k}_{j,n}})) \\ \sin(\phi (\mathbf{\tilde{k}_{j,n}})) \end{bmatrix}=\] \[\sum_{n=1}^{d/2} \lvert \mathbf{\tilde{q}_{t,n}} \rvert \, \begin{bmatrix} \cos(\phi (\mathbf{\tilde{q}_{t,n}})) \\ \sin(\phi (\mathbf{\tilde{q}_{t,n}})) \end{bmatrix}^{\!\top} \! \textcolor{blue}{R_{\mathbf{\Omega_n}}^{t-j}} \lvert \mathbf{\tilde{k}_{j,n}} \rvert \, \begin{bmatrix} \cos(\phi (\mathbf{\tilde{k}_{j,n}})) \\ \sin(\phi (\mathbf{\tilde{k}_{j,n}})) \end{bmatrix}=\] \[\sum_{n=1}^{d/2} \begin{bmatrix} \mathcal{R} (\mathbf{\tilde{q}_{t,n}}) \\ \mathcal{I} (\mathbf{\tilde{q}_{t,n}}) \end{bmatrix}^{\!\top} \! \textcolor{blue}{R_{\mathbf{\Omega_n}}^{t-j}} \! \begin{bmatrix} \mathcal{R} (\mathbf{\tilde{k}_{t,n}}) \\ \mathcal{I} (\mathbf{\tilde{k}_{t,n}}) \end{bmatrix}.\] <p>By stacking the real and imaginary parts of queries and keys into a real-vector of shape $d$ we have:</p> \[\mathbf{q_t} = \bigoplus_{n=1}^{d/2} \begin{bmatrix} \mathcal{R} (\mathbf{\tilde{q}_{t,n}}) \\ \mathcal{I} (\mathbf{\tilde{q}_{t,n}}) \end{bmatrix}, \quad \mathbf{k_t} = \bigoplus_{n=1}^{d/2} \begin{bmatrix} \mathcal{R} (\mathbf{\tilde{k}_{t,n}}) \\ \mathcal{I} (\mathbf{\tilde{k}_{t,n}}) \end{bmatrix}.\] <p>This way, <strong>we do not actually need any computation in the complex domain</strong>; we can do everything in <strong>real numbers</strong> using $\mathbf{q_t, k_t}$. Using this we can write the attention-score as:</p> \[\mathbf{q^\top_{t}} \underbrace{\left(\bigoplus_{n=1}^{d/2} \textcolor{black}{R_{\mathbf{\Omega_n}}^{t-j}}\right)}_{\mathbf{R}^{(t-j)}_{\Omega}} \mathbf{k_{j}} = \mathbf{q^\top_{t}} \mathbf{R}^{(t-j)}_{\Omega} \mathbf{k_{j}} = \left(\mathbf{q^\top_{t}} \mathbf{R}^{t}_{\Omega} \right) \left( \mathbf{R}^{-j}_{\Omega} \mathbf{k_{j}} \right)\] <details> <summary>Rotation Matrix Inverse</summary> Let $$ R_{\omega}= \begin{pmatrix} \cos\omega &amp; -\sin\omega\\ \sin\omega &amp; \cos\omega \end{pmatrix}. $$ Using $\cos(-\omega)=\cos\omega$ and $\sin(-\omega)=-\sin\omega$, $$ R_{-\omega}= \begin{pmatrix} \cos\omega &amp; \sin\omega\\ -\sin\omega &amp; \cos\omega \end{pmatrix}. $$ Then $$ R_{\omega}R_{-\omega}=I, $$ so $$ (R_{\omega})^{-1}=R_{-\omega}=R_{\omega}^\top. $$ </details> <p>By using the inverse of the rotation matrix, we can directly apply the rotation of the forget gate $\mathbf{A}_t = \exp(i\mathbf{\Omega})$ to the queries and keys.</p> \[\left(\mathbf{q^\top_{t}} \mathbf{R}^{t}_{\Omega} \right) \left( \mathbf{R}^{-j}_{\Omega} \mathbf{k_{j}} \right) = \textcolor{blue}{ \left( \mathbf{R}_{-t\Omega} \mathbf{q_{t}} \right)^\top} \textcolor{red}{ \left( \mathbf{R}_{-j\Omega} \mathbf{k_{j}} \right)} = \textcolor{blue}{ \mathbf{\bar q^\top_{t}}} \textcolor{red}{ \mathbf{\bar k_{j}} }\] \[\mathbf{R}^{(t)}_{\Omega} = \bigoplus_{n=1}^{d/2} \textcolor{black}{R_{\mathbf{\Omega_n}}^{t}}\] <p>To wrap-up we get back to our complex-valued recurrence:</p> \[\mathbf{S}_t = \mathbf{S}_{t-1}\,\textcolor{black}{e^{i\mathbf{\Omega}}} + \mathbf{v}_t \tilde{\mathbf{k}}_t^{\mathrm{H}}, \quad \mathbf{o}_t = \mathcal{R} \{\mathbf{S}_t \tilde{\mathbf{q}}_t\}\] <p>We can apply this forget gate in the real domain by stacking the imaginary and real parts of the queries and keys, and forming the output directly:</p> \[\mathbf{o}_t = \left(\mathbf{q_{t}} \mathbf{R}_{-t\Omega} \right) \sum_{j=1}^t \mathbf{v}_j \left( \mathbf{R}_{-j\Omega}\mathbf{k_{j}} \right)^\top\] <p>The construction of rotation matrix based on the angles of $\mathbf{\Omega}$ is the well-known <strong>multiplicative</strong> position embedding of Rotary Position embedding (RoPE) <d-cite key="su2021roformer"></d-cite> used as default for many transformers like Llama <d-cite key="touvron2023llama"></d-cite>.</p> <blockquote> \[\begin{aligned} &amp; \textcolor{black}{\mathbf{S}_t} \textcolor{black}{= \mathbf{S}_{t-1}\, \textcolor{blue}{e^{i\mathbf{\Omega}}} + \mathbf{v}_t \tilde{\mathbf{k}}_t^{\mathrm{H}}}, \quad \textcolor{black}{\mathbf{o}_t = \mathcal{R}\{\mathbf{S}_t \tilde{\mathbf{q}}_t\}}, \\ &amp; \textcolor{black}{\text{Softmax}\!\left( \textcolor{blue}{\mathbf{\bar Q}}\,\textcolor{blue}{\mathbf{\bar K^\top}} \right)} , \quad \textcolor{blue}{\mathbf{\bar q_{t}}} \textcolor{black}{= \mathbf{R}_{-t\Omega} \mathbf{q_{t}}} , \quad \textcolor{blue}{\mathbf{\bar k_{j}}} \textcolor{black}{= \mathbf{R}_{-j\Omega} \mathbf{k_{t}}} \end{aligned}\] </blockquote> <p>Therefore, RoPE, admits a direct duality with the complex linear Transformers as also stated in original paper <d-cite key="su2021roformer"></d-cite>.</p> <details> <summary>Click to expand RoPE Code</summary> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="kn">import</span> <span class="n">torch</span>

<span class="k">def</span> <span class="nf">rope</span><span class="p">(</span><span class="n">Q</span><span class="p">,</span> <span class="n">K</span><span class="p">,</span> <span class="n">V</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Simple RoPE Attention (original paper)
    Q, K, V: (d, T), d must be even
    </span><span class="sh">"""</span>
    <span class="n">d</span><span class="p">,</span> <span class="n">T</span> <span class="o">=</span> <span class="n">Q</span><span class="p">.</span><span class="n">shape</span>
    <span class="n">device</span><span class="p">,</span> <span class="n">dtype</span> <span class="o">=</span> <span class="n">Q</span><span class="p">.</span><span class="n">device</span><span class="p">,</span> <span class="n">Q</span><span class="p">.</span><span class="n">dtype</span>

    <span class="c1"># Original RoPE frequencies
</span>    <span class="n">m</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">d</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="n">device</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="n">dtype</span><span class="p">)</span> <span class="o">/</span> <span class="n">d</span>
    <span class="n">omegas</span> <span class="o">=</span> <span class="mf">1.0</span> <span class="o">/</span> <span class="p">(</span><span class="mi">10000</span> <span class="o">**</span> <span class="n">m</span><span class="p">)</span>                          <span class="c1"># (d/2,)
</span>
    <span class="n">t</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="n">T</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="n">device</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="n">dtype</span><span class="p">)</span>     <span class="c1"># (T,)
</span>    <span class="n">theta</span> <span class="o">=</span> <span class="n">omegas</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">*</span> <span class="n">t</span><span class="p">[</span><span class="bp">None</span><span class="p">,</span> <span class="p">:]</span>                 <span class="c1"># (d/2, T)
</span>    <span class="n">cos</span><span class="p">,</span> <span class="n">sin</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cos</span><span class="p">(</span><span class="n">theta</span><span class="p">),</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="n">theta</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">rotate</span><span class="p">(</span><span class="n">x</span><span class="p">):</span>
        <span class="n">x1</span><span class="p">,</span> <span class="n">x2</span> <span class="o">=</span> <span class="n">x</span><span class="p">[</span><span class="mi">0</span><span class="p">::</span><span class="mi">2</span><span class="p">],</span> <span class="n">x</span><span class="p">[</span><span class="mi">1</span><span class="p">::</span><span class="mi">2</span><span class="p">]</span>
        <span class="n">y</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">empty_like</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="n">y</span><span class="p">[</span><span class="mi">0</span><span class="p">::</span><span class="mi">2</span><span class="p">]</span> <span class="o">=</span> <span class="n">x1</span> <span class="o">*</span> <span class="n">cos</span> <span class="o">-</span> <span class="n">x2</span> <span class="o">*</span> <span class="n">sin</span>
        <span class="n">y</span><span class="p">[</span><span class="mi">1</span><span class="p">::</span><span class="mi">2</span><span class="p">]</span> <span class="o">=</span> <span class="n">x1</span> <span class="o">*</span> <span class="n">sin</span> <span class="o">+</span> <span class="n">x2</span> <span class="o">*</span> <span class="n">cos</span>
        <span class="k">return</span> <span class="n">y</span>

    <span class="n">Qr</span> <span class="o">=</span> <span class="nf">rotate</span><span class="p">(</span><span class="n">Q</span><span class="p">)</span>
    <span class="n">Kr</span> <span class="o">=</span> <span class="nf">rotate</span><span class="p">(</span><span class="n">K</span><span class="p">)</span>

    <span class="n">out</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">Qr</span><span class="p">.</span><span class="n">T</span> <span class="o">@</span> <span class="n">Kr</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span> <span class="o">@</span> <span class="n">V</span><span class="p">.</span><span class="n">T</span>
    <span class="k">return</span> <span class="n">out</span></code></pre></figure> </details> <hr/> <h1 id="5-deltanet--path">5) Deltanet ↔ PaTH</h1> <p>Up to this point, all linear models considered employ scalar or diagonal forget gates. DeltaNet <d-cite key="yang2024parallelizing"></d-cite> is the first model to introduce a dense forget gate, derived from the Delta update rule <d-cite key="schlag2021linear"></d-cite>. Specifically, the forget matrix is defined as</p> \[\mathbf{A}_t = \mathbf{(I- \beta_t k_t k^\top_t)}\] <p>with $\beta_t=\sigma(\mathbf{w}^\top \mathbf{x_t})$ being an input-dependent scalar. Above recurrence follows a Householder-like construction. Under this design, the recurrence relation becomes:</p> <details> <summary>Householder vs. Householder-like Matrices</summary> <ul> <li> <strong>Householder matrix:</strong> $H = I - 2\frac{v v^\top}{v^\top v}$. It is exactly orthogonal and represents a reflection. </li> <li> <strong>Householder-like matrix:</strong> $H = I - \alpha v v^\top$. It generalizes the Householder form; it is symmetric but orthogonal only when $\alpha = 2/(v^\top v)$. </li> </ul> </details> \[\mathbf{S}_t = \mathbf{S}_{t-1}\,\textcolor{blue}{\mathbf{(I- \beta_t k_t k^\top_t)}} + \mathbf{v}_t \mathbf{k}_t^\top\] <p>Resulting in cumulative product of forget gate as:</p> \[\prod_{s=j+1}^t \mathbf{A}_s = \prod_{s=j+1}^t \textcolor{blue}{\mathbf{(I- \beta_s k_s k^\top_s)}}\] <p>Similar to other positional embeddings, DeltaNet’s cumulative forget gate implicitly defines a relative positional embedding. This structure can be directly incorporated into softmax-based Transformers, resulting in an attention mechanism that is refered to as <em>Position encoding based on Accumulated Products of Householder(-like) Transformations</em> <strong>(PaTH)</strong> attention <d-cite key="yang2025path"></d-cite>.</p> <blockquote> \[\begin{aligned} &amp; \textcolor{black}{\mathbf{S}_t} \textcolor{black}{= \mathbf{S}_{t-1}\, \textcolor{blue}{\mathbf{(I- \beta_t k_t k^\top_t)}} + \mathbf{v}_t {\mathbf{k}}_t^{\top}}, \quad \textcolor{black}{\mathbf{o}_t = \mathbf{S}_t {\mathbf{q}}_t}, \\ &amp; \textcolor{black}{\mathbf{Att}_{ij} = \exp( \mathbf{k^\top_j}}\textcolor{blue}{\prod_{s=j+1}^i \mathbf{(I- \beta_s k_s k^\top_s)}}\textcolor{black}{\mathbf{q_i} )},\quad \textcolor{black}{\text{SoftMax} (\mathbf{\textcolor{blue}{Att}})} \end{aligned}\] </blockquote> <p>Therefore, <strong>PaTH</strong>, admits a direct duality with the <strong>DeltaNet</strong>.</p> <details> <summary>Click to expand PaTH simple code</summary> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="kn">import</span> <span class="n">torch</span> 

<span class="k">def</span> <span class="nf">PaTH_attn</span><span class="p">(</span><span class="n">Q</span><span class="p">,</span><span class="n">K</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Simple PaTH implementation
    Q,K: input (d , T)
    </span><span class="sh">"""</span>

    <span class="n">H</span> <span class="o">=</span> <span class="n">beta</span><span class="p">.</span><span class="nf">reshape</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span><span class="mi">1</span><span class="p">,</span><span class="mi">1</span><span class="p">)</span> <span class="o">*</span> <span class="p">(</span> <span class="n">torch</span><span class="p">.</span><span class="nf">eye</span><span class="p">(</span><span class="n">d</span><span class="p">).</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span> <span class="o">-</span> <span class="n">K</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="o">@</span> <span class="n">K</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">2</span><span class="p">)</span> <span class="p">)</span>
    <span class="n">A</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">T</span><span class="p">,</span> <span class="n">T</span><span class="p">)</span>

    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">T</span><span class="p">):</span>
        <span class="k">for</span> <span class="n">j</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">i</span> <span class="o">+</span> <span class="mi">1</span><span class="p">):</span>  <span class="c1"># usually j &lt;= i
</span>            <span class="c1"># Compute product of H_s from s=j+1 to i
</span>            <span class="k">if</span> <span class="n">j</span> <span class="o">+</span> <span class="mi">1</span> <span class="o">&lt;=</span> <span class="n">i</span><span class="p">:</span>
                <span class="n">P</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">eye</span><span class="p">(</span><span class="n">d</span><span class="p">)</span>
                <span class="k">for</span> <span class="n">s</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">j</span> <span class="o">+</span> <span class="mi">1</span><span class="p">,</span> <span class="n">i</span> <span class="o">+</span> <span class="mi">1</span><span class="p">):</span>
                    <span class="n">P</span> <span class="o">=</span> <span class="n">H</span><span class="p">[</span><span class="n">s</span><span class="p">]</span> <span class="o">@</span> <span class="n">P</span>
            <span class="k">else</span><span class="p">:</span>
                <span class="n">P</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">eye</span><span class="p">(</span><span class="n">d</span><span class="p">)</span>

            <span class="c1"># k_j^T (P q_i)
</span>            <span class="n">score</span> <span class="o">=</span> <span class="n">K</span><span class="p">[</span><span class="n">j</span><span class="p">]</span> <span class="o">@</span> <span class="p">(</span><span class="n">P</span> <span class="o">@</span> <span class="n">Q</span><span class="p">[</span><span class="n">i</span><span class="p">])</span>

            <span class="n">A</span><span class="p">[</span><span class="n">i</span><span class="p">,</span> <span class="n">j</span><span class="p">]</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">score</span><span class="p">)</span>

<span class="k">return</span> <span class="n">A</span> <span class="p">,</span> <span class="n">raw_scores</span>
   </code></pre></figure> </details> <p>⚠️ <strong>PaTH</strong> supports parallelization across the sequence length ($T$) and leverages an efficient implementation based on the ($UT$) transform. Further implementation details are available in the <a href="https://github.com/fla-org/flash-linear-attention">fla-repository</a>.</p> <hr/> <h1 id="6-gateddeltanet--path-fox">6) GatedDeltanet ↔ PaTH-FoX</h1> <p>As DeltaNet uses Householder products as a forget gate, it lacks sharp decay as in Mamba2 and GLA therefor Gated DeltaNet <d-cite key="gdn"></d-cite> tackles this by multiplying a scalar selective forget gate $a_t\in \mathbb{R}$ into the recurrence as:</p> \[\mathbf{S}_t = \mathbf{S}_{t-1}\,\textcolor{blue}{\mathbf{a_t(I- \beta_t k_t k^\top_t)}} + \mathbf{v}_t \mathbf{k}_t^\top , \quad \mathbf{a}_t = \exp\big(-\mathrm{SoftPlus}(\mathbf{w^\top x_t})\big)\] <p>with $\mathbf{w}$ is a learnable weight vector, and the decay term follows the scalar decay mechanism of Mamba-2.</p> <p>In the context of positional embeddings for softmax Transformers, the use of the cumulative forget gate as a positional embedding is not entirely new. It can be interpreted as a combination of <strong>PaTH and FoX</strong>, both described in the previous sections. As a result, PaTH-FoX emerges as a PE that has both <strong>additive</strong> and <strong>multiplicative</strong> embeddings together as:</p> <blockquote> \[\begin{aligned} &amp; \textcolor{black}{\mathbf{S}_t} \textcolor{black}{= \mathbf{S}_{t-1}\, \textcolor{red}{a_t}\textcolor{blue}{\mathbf{(I- \beta_t k_t k^\top_t)}} + \mathbf{v}_t {\mathbf{k}}_t^{\top}}, \quad \textcolor{black}{\mathbf{o}_t = \mathbf{S}_t {\mathbf{q}}_t}, \\ &amp; \textcolor{black}{\mathbf{Att}_{ij} = \mathbf{k^\top_j}}\textcolor{blue}{\prod_{s=j+1}^i \mathbf{(I- \beta_s k_s k^\top_s)}}\textcolor{black}{\mathbf{q_i} },\quad \textcolor{black}{\text{SoftMax} (\mathbf{\textcolor{blue}{Att}+\textcolor{red}{D}})} , \\ &amp; \textcolor{red} {\mathbf{D_{ij}}} \textcolor{black}{=\begin{cases} \textcolor{red}{\sum_{s=j+1}^t \log (\sigma(\mathbf{wx_t})^{1/\tau}) }&amp; i \ge j \\ - \infty &amp; i&lt;j \end{cases} } \end{aligned}\] </blockquote> <p>And <strong>PaTH-FoX</strong> <d-cite key="yang2025path"></d-cite>, admits a direct duality with the <strong>GatedDeltaNet</strong>.</p> <details> <summary>Click to expand PaTH-FoX simple code</summary> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="kn">import</span> <span class="n">torch</span> 

<span class="k">def</span> <span class="nf">Path</span><span class="o">-</span><span class="nc">Fox</span><span class="p">(</span><span class="n">Q</span><span class="p">,</span><span class="n">K</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Simple PaTH-FoX implementation
    Q,K: input (d , T)
    w : learnable weight of fox
    x : input (d , T)
    </span><span class="sh">"""</span>
    <span class="n">D_add</span> <span class="o">=</span> <span class="nf">fox_pe</span><span class="p">(</span><span class="n">w</span><span class="p">,</span> <span class="n">x</span><span class="p">)</span>
    <span class="n">_</span> <span class="p">,</span> <span class="n">scores</span> <span class="o">=</span> <span class="nc">PaTH_attn</span><span class="p">(</span><span class="n">Q</span><span class="p">,</span><span class="n">K</span><span class="p">)</span> 
    <span class="n">Attn</span> <span class="o">=</span>  <span class="n">scores</span> <span class="o">+</span> <span class="n">D_add</span>
    <span class="k">return</span> <span class="n">Attn</span></code></pre></figure> </details> <hr/> <h1 id="7-gla--wall-attention">7) GLA ↔ Wall Attention</h1> <p>Section 3 established a duality between GLA and FoX by <strong>collapsing</strong> GLA’s diagonal forget gate to a scalar before applying it as an additive bias. But what happens if we preserve the <strong>full per-channel diagonal structure</strong> and apply it <strong>multiplicatively</strong>? This is exactly the idea behind <strong>Wall Attention</strong> <d-cite key="wallattn"></d-cite>, providing another connection between GLA’s diagonal gates and softmax Transformers.</p> <p>Recall that GLA’s recurrence uses a diagonal forget gate $\mathbf{A}_t = \textcolor{blue}{\text{diag}(\mathbf{g}_t)}$, with $\mathbf{g}_t \in (0,1)^d$ being an input-dependent per-channel decay vector:</p> \[\mathbf{S}_t = \mathbf{S}_{t-1}\,\textcolor{blue}{\text{diag}(\mathbf{g}_t)} + \mathbf{v}_t \mathbf{k}_t^\top\] <p>Because $\mathbf{A}_t$ is diagonal, its cumulative product is also diagonal, with per-channel entries:</p> \[\prod_{s=j+1}^t \mathbf{A}_s = \text{diag}(\mathbf{F}_{ij}), \quad \textcolor{blue}{F_{ij,n}} = \prod_{s=j+1}^i \mathbf{g}_{s,n} = \exp\!\Bigl(\sum_{s=j+1}^i \log \mathbf{g}_{s,n}\Bigr)\] <p>Each channel $n$ decays at its own learned rate — some channels forget quickly (capturing local context) while others persist over long ranges (capturing global dependencies). Unlike FoX, which projects this diagonal structure down to a scalar additive bias, Wall Attention directly lifts the full per-channel cumulative decay into softmax attention as a <strong>multiplicative</strong> positional embedding:</p> \[\text{score}_{ij} = \mathbf{q}_{i}^\top \, \textcolor{blue}{\text{diag}(\mathbf{F}_{ij})} \, \mathbf{k}_{j} = \sum_{n=1}^d \textcolor{blue}{F_{ij,n}} \, \mathbf{q}_{i,n} \, \mathbf{k}_{j,n}\] <p>This matches the <strong>Multiplicative PE</strong> form introduced earlier, with $\textcolor{blue}{\mathbf{D}<em>{ij}} = \text{diag}(\mathbf{F}</em>{ij}) \in \mathbb{R}^{d \times d}$.</p> <blockquote> \[\begin{aligned} &amp; \mathbf{S}_t = \mathbf{S}_{t-1}\,\textcolor{blue}{\text{diag}(\mathbf{g}_t)} + \mathbf{v}_t \mathbf{k}_t^\top \\ &amp; \text{Softmax}\!\left(\textcolor{blue}{\tilde{\mathbf{Q}}}\,\textcolor{blue}{\tilde{\mathbf{K}}^\top}\right)\mathbf{V} \\ &amp; \textcolor{blue}{\tilde{\mathbf{q}}_i = \exp(\mathbf{P}_i) \odot \mathbf{q}_i}, \quad \textcolor{blue}{\tilde{\mathbf{k}}_j = \exp(-\mathbf{P}_j) \odot \mathbf{k}_j}, \quad \mathbf{P}_{t,n} = \sum_{u \le t} \log \mathbf{g}_{u,n} \end{aligned}\] </blockquote> <p>Therefore, <strong>Wall Attention</strong> admits a direct duality with <strong>GLA</strong>, where GLA’s full diagonal per-channel decay is lifted into the softmax Transformer as a <strong>multiplicative</strong> positional embedding — in contrast to FoX, which uses the same gate family but applies it as a scalar additive bias.</p> <details> <summary>Click to expand Wall Attention simple code</summary> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="kn">import</span> <span class="n">torch</span>
<span class="kn">import</span> <span class="n">torch.nn.functional</span> <span class="k">as</span> <span class="n">F</span>

<span class="k">def</span> <span class="nf">wall_attention</span><span class="p">(</span><span class="n">Q</span><span class="p">,</span> <span class="n">K</span><span class="p">,</span> <span class="n">V</span><span class="p">,</span> <span class="n">W</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Simple Wall Attention implementation
    Q, K, V: input (d, T)
    W: learnable weight (d, d) for computing gates
    x: input tokens (d, T)
    </span><span class="sh">"""</span>
    <span class="c1"># Per-channel input-dependent log-gates: log g_{t,n} in (-inf, 0)
</span>    <span class="n">log_g</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">logsigmoid</span><span class="p">(</span><span class="n">W</span><span class="p">.</span><span class="n">T</span> <span class="o">@</span> <span class="n">x</span><span class="p">)</span>             <span class="c1"># (d, T)
</span>
    <span class="c1"># Prefix sums in log-space: P_{t,n} = sum_{u&lt;=t} log g_{u,n}
</span>    <span class="n">P</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cumsum</span><span class="p">(</span><span class="n">log_g</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>            <span class="c1"># (d, T)
</span>
    <span class="c1"># Rescale queries and keys per-channel with positional factors
</span>    <span class="n">Q_tilde</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">P</span><span class="p">)</span> <span class="o">*</span> <span class="n">Q</span>                <span class="c1"># (d, T)
</span>    <span class="n">K_tilde</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="o">-</span><span class="n">P</span><span class="p">)</span> <span class="o">*</span> <span class="n">K</span>               <span class="c1"># (d, T)
</span>
    <span class="c1"># Standard softmax attention on positionally-encoded Q, K
</span>    <span class="n">out</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">Q_tilde</span><span class="p">.</span><span class="n">T</span> <span class="o">@</span> <span class="n">K_tilde</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span> <span class="o">@</span> <span class="n">V</span><span class="p">.</span><span class="n">T</span>

    <span class="k">return</span> <span class="n">out</span></code></pre></figure> </details> <hr/> <h1 id="8-kda--path-wall">8) KDA ↔ PaTH-Wall</h1> <p>Just as GatedDeltaNet extends DeltaNet with a <strong>scalar</strong> decay (leading to PaTH-FoX), <strong>Kimi Delta Attention (KDA)</strong> <d-cite key="kda"></d-cite> extends GatedDeltaNet by upgrading the scalar $a_t$ to a <strong>diagonal</strong> per-channel decay $\textcolor{red}{\text{diag}(\boldsymbol{\alpha}_t)}$:</p> \[\mathbf{S}_t = \mathbf{S}_{t-1}\,\textcolor{red}{\text{diag}(\boldsymbol{\alpha}_t)}\textcolor{blue}{\mathbf{(I- \beta_t \mathbf{k}_t \mathbf{k}^\top_t)}} + \mathbf{v}_t \mathbf{k}_t^\top\] <p>Following the same duality, the dual PE of KDA — which we call <strong>PaTH-Wall</strong> — combines the multiplicative Householder embedding of PaTH ($\textcolor{blue}{\text{blue}}$) with the per-channel diagonal decay of Wall ($\textcolor{red}{\text{red}}$):</p> <blockquote> \[\begin{aligned} &amp; \mathbf{S}_t = \mathbf{S}_{t-1}\, \textcolor{red}{\text{diag}(\boldsymbol{\alpha}_t)}\textcolor{blue}{\mathbf{(I- \beta_t \mathbf{k}_t \mathbf{k}^\top_t)}} + \mathbf{v}_t \mathbf{k}_t^\top \\ &amp; \mathbf{Att}_{ij} = \mathbf{k}^\top_j \prod_{s=j+1}^i \textcolor{red}{\text{diag}(\boldsymbol{\alpha}_s)}\textcolor{blue}{\mathbf{(I- \beta_s \mathbf{k}_s \mathbf{k}^\top_s)}} \mathbf{q}_i, \quad \text{SoftMax}(\mathbf{Att}) \end{aligned}\] </blockquote> <p><strong>PaTH-Wall is not yet built — but this is precisely what we are looking for 🔍.</strong></p> <hr/> <h1 id="takeaway">Takeaway</h1> <p>Finally, the table below provides a compact overview, summarizing all the key dualities between linear transformers and positional embeddings discussed above, at a single glance 🙂</p> <style>.pet-wrap{overflow-x:auto;margin:2.5rem 0;border-radius:12px;box-shadow:0 2px 14px rgba(0,0,0,.08);width:min(98vw,1080px);position:relative;left:50%;transform:translateX(-50%)}.pet{width:100%;border-collapse:collapse;font-size:.76em;background:#fff;border-radius:12px;overflow:hidden;border:1px solid #d1d5db}.pet thead tr{background:#f1f5f9}.pet th{padding:.65em .9em;font-weight:700;text-align:left;border-bottom:2px solid #cbd5e1;color:#1e293b;font-size:.93em;letter-spacing:.01em;white-space:nowrap}.pet td{padding:.65em .9em;border-bottom:1px solid #e2e8f0;vertical-align:middle;line-height:1.6;color:#1e293b}.pet tr:last-child td{border-bottom:0}.pet .r-even td{background:#f8fafc}.pet .r-wall td{background:rgba(239,68,68,.05)}.pet .r-pathwall td{background:rgba(239,68,68,.09)}.pet .mc{font-weight:700;white-space:nowrap;min-width:108px}.pet .mc small{display:block;font-weight:400;color:#64748b;font-size:.88em;margin-top:2px}.pbadge{display:inline-block;font-size:.67em;padding:.12em .5em;border-radius:99px;font-weight:700;vertical-align:middle;margin-left:4px;line-height:1.5}.pbadge.pnew{background:#dbeafe;color:#1d4ed8}.pbadge.pwip{background:#fee2e2;color:#b91c1c}[data-theme="dark"] .pet{background:#1e293b;border-color:#374151}[data-theme="dark"] .pet thead tr{background:#0f172a}[data-theme="dark"] .pet th{color:#f1f5f9;border-color:#4b5563}[data-theme="dark"] .pet td{color:#e2e8f0;border-color:#334155}[data-theme="dark"] .pet .r-even td{background:#1a2742}[data-theme="dark"] .pet .r-wall td{background:rgba(239,68,68,.09)}[data-theme="dark"] .pet .r-pathwall td{background:rgba(239,68,68,.14)}[data-theme="dark"] .pet .mc small{color:#94a3b8}</style> <div class="pet-wrap"> <table class="pet"> <thead><tr> <th style="min-width:115px">Model / PE</th> <th style="min-width:230px">Recurrence</th> <th>Softmax Attention PE</th> </tr></thead> <tbody> <tr> <td class="mc">LinAtt<small>NoPE</small></td> <td>$\mathbf{S}_t = \mathbf{S}_{t-1} + \mathbf{v}_t\mathbf{k}_t^\top$</td> <td>$\mathbf{O}=\text{Softmax}(\mathbf{Q}\mathbf{K}^\top+\mathbf{M})\mathbf{V}$,&nbsp;&nbsp; $\mathbf{M}_{ij}=\begin{cases}0&amp;i\ge j\\-\infty&amp;i&lt;j\end{cases}$</td> </tr> <tr class="r-even"> <td class="mc">RetNet<small>AliBi</small></td> <td>$\mathbf{S}_t = \textcolor{blue}{\gamma}\,\mathbf{S}_{t-1} + \mathbf{v}_t\mathbf{k}_t^\top$</td> <td>$\mathbf{O}=\text{Softmax}(\mathbf{Q}\mathbf{K}^\top+\textcolor{blue}{\mathbf{D}_{\text{AliBi}}})\mathbf{V}$<br/>$(\mathbf{D}_{\text{AliBi}})_{ij}=\begin{cases}-\textcolor{blue}{\lambda(i-j)}&amp;i\ge j\\-\infty&amp;i&lt;j\end{cases}$</td> </tr> <tr> <td class="mc">Mamba2 / GLA<small>FoX</small></td> <td>$\mathbf{S}_t = \mathbf{S}_{t-1}\textcolor{blue}{a_t} + \mathbf{v}_t\mathbf{k}_t^\top$</td> <td>$\mathbf{O}=\text{Softmax}(\mathbf{Q}\mathbf{K}^\top+\textcolor{blue}{\mathbf{D}_{\text{FoX}}})\mathbf{V}$<br/>$(\mathbf{D}_{\text{FoX}})_{ij}=\begin{cases}\textcolor{blue}{\sum_{s=j+1}^{i}\log a_s}&amp;i\ge j\\-\infty&amp;i&lt;j\end{cases}$</td> </tr> <tr class="r-even"> <td class="mc">Complex<small>RoPE</small></td> <td>$\mathbf{S}_t = \mathbf{S}_{t-1}\textcolor{blue}{e^{i\mathbf{\Omega}}} + \mathbf{v}_t\tilde{\mathbf{k}}_t^{\mathrm{H}}$</td> <td>$\textcolor{blue}{\bar{\mathbf{q}}_t=\mathbf{R}_{-t\Omega}\mathbf{q}_t},\quad\textcolor{blue}{\bar{\mathbf{k}}_t=\mathbf{R}_{-t\Omega}\mathbf{k}_t}$<br/>$\mathbf{O}=\text{Softmax}(\textcolor{blue}{\bar{\mathbf{Q}}\bar{\mathbf{K}}^\top})\mathbf{V}$</td> </tr> <tr class="r-wall"> <td class="mc">GLA <span class="pbadge pnew">New</span><small>Wall Att.</small></td> <td>$\mathbf{S}_t = \mathbf{S}_{t-1}\,\textcolor{red}{\operatorname{Diag}(\mathbf{g}_t)} + \mathbf{v}_t\mathbf{k}_t^\top$</td> <td>$\textcolor{red}{\mathbf{P}_{t,n}=\sum_{u\le t}\!\log \mathbf{g}_{u,n}}$<br/>$\tilde{\mathbf{q}}_{i,n}=e^{\textcolor{red}{\mathbf{P}_{i,n}}}\mathbf{q}_{i,n},\quad\tilde{\mathbf{k}}_{j,n}=e^{-\textcolor{red}{\mathbf{P}_{j,n}}}\mathbf{k}_{j,n}$<br/>$\mathbf{O}=\text{Softmax}(\tilde{\mathbf{Q}}\tilde{\mathbf{K}}^\top)\mathbf{V}$</td> </tr> <tr> <td class="mc">DeltaNet<small>PaTH</small></td> <td>$\mathbf{S}_t = \mathbf{S}_{t-1}\textcolor{blue}{(\mathbf{I}-\beta_t\mathbf{k}_t\mathbf{k}_t^\top)} + \mathbf{v}_t\mathbf{k}_t^\top$</td> <td>$\mathbf{H}_s = \mathbf{I}-\beta_s\mathbf{k}_s\mathbf{k}_s^\top$<br/>$\mathbf{O}=\text{Softmax}(\mathbf{A})\mathbf{V},\quad\mathbf{A}_{ij}=\begin{cases}\exp\!\big(\mathbf{k}_j^\top\big(\textcolor{blue}{\prod_{s=j+1}^{i}\mathbf{H}_s}\big)\mathbf{q}_i\big)&amp;i\ge j\\0&amp;i&lt;j\end{cases}$</td> </tr> <tr class="r-even"> <td class="mc">Gated DeltaNet<small>PaTH-FoX</small></td> <td>$\mathbf{S}_t = \mathbf{S}_{t-1}\textcolor{red}{a_t}\textcolor{blue}{(\mathbf{I}-\beta_t\mathbf{k}_t\mathbf{k}_t^\top)} + \mathbf{v}_t\mathbf{k}_t^\top$</td> <td>$\mathbf{O}=\text{Softmax}(\textcolor{blue}{\mathbf{A}}+\textcolor{red}{\mathbf{D}_{\text{FoX}}})\mathbf{V}$<br/>$\mathbf{A}_{ij}=\begin{cases}\exp\!\big(\mathbf{k}_j^\top\big(\textcolor{blue}{\prod_{s=j+1}^{i}\mathbf{H}_s}\big)\mathbf{q}_i\big)&amp;i\ge j\\0&amp;i&lt;j\end{cases}$<br/>$(\mathbf{D}_{\text{FoX}})_{ij}=\begin{cases}\textcolor{red}{\sum_{s=j+1}^{i}\log a_s}&amp;i\ge j\\-\infty&amp;i&lt;j\end{cases}$</td> </tr> <tr class="r-pathwall"> <td class="mc">KDA <span class="pbadge pwip">🔍</span><small>PaTH-Wall</small></td> <td>$\mathbf{S}_t = \mathbf{S}_{t-1}\,\textcolor{red}{\operatorname{Diag}(\boldsymbol{\alpha}_t)}\textcolor{blue}{(\mathbf{I}-\beta_t\mathbf{k}_t\mathbf{k}_t^\top)} + \mathbf{v}_t\mathbf{k}_t^\top$</td> <td>$\mathbf{H}_s=\mathbf{I}-\beta_s\mathbf{k}_s\mathbf{k}_s^\top$<br/>$\mathbf{A}_{ij}=\mathbf{k}_j^\top\!\left(\prod_{s=j+1}^{i}\textcolor{red}{\operatorname{Diag}(\boldsymbol{\alpha}_s)}\textcolor{blue}{\mathbf{H}_s}\right)\mathbf{q}_i$<br/>$\mathbf{O}=\text{Softmax}(\mathbf{A})\mathbf{V}$</td> </tr> </tbody> </table> </div> <p>If you’re interested in learning more about different variants of linear transformers, I highly recommend checking out <strong>Table 4</strong> of <d-cite key="yang2024parallelizing"></d-cite> :).</p> <p>You may also enjoy the excellent blog posts by:</p> <ul> <li><a href="https://sustcsonglin.github.io/">Soglin Yang</a></li> <li><a href="https://goombalab.github.io/blog/">GoombaLab</a></li> </ul> <p>which provide intuitive explanations and deeper insights into the topic.</p> <hr/> <h1 id="to-cite">To Cite</h1> <p>If you use find this blog useful, please consider citing me:</p> <div class="language-bibtex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">@article</span><span class="p">{</span><span class="nl">afzal2025legacy</span><span class="p">,</span>
  <span class="na">title</span><span class="p">=</span><span class="s">{On the Legacy of Linear Transformers in Positional Embedding}</span><span class="p">,</span>
  <span class="na">author</span><span class="p">=</span><span class="s">{Afzal Arshia}</span><span class="p">,</span>
  <span class="na">year</span><span class="p">=</span><span class="s">{2026}</span><span class="p">,</span>
  <span class="na">url</span><span class="p">=</span><span class="s">{https://arshiaafzal.github.io/blog/2026/pe/}</span><span class="p">,</span>
<span class="p">}</span>
</code></pre></div></div>]]></content><author><name>Arshia Afzal</name></author><summary type="html"><![CDATA[Duality of Forget Gates and Position Embeddings in Sequence Modeling]]></summary></entry><entry><title type="html">LION 🦁 Part I - Full Linear Attention</title><link href="https://arshiaafzal.github.io/blog/2025/lion-part1-model/" rel="alternate" type="text/html" title="LION 🦁 Part I - Full Linear Attention"/><published>2025-02-24T00:00:00+00:00</published><updated>2025-02-24T00:00:00+00:00</updated><id>https://arshiaafzal.github.io/blog/2025/lion-part1-model</id><content type="html" xml:base="https://arshiaafzal.github.io/blog/2025/lion-part1-model/"><![CDATA[<figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lion-480.webp 480w,/assets/img/lion-800.webp 800w,/assets/img/lion-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lion.jpg" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>[<a href="https://www.arxiv.org/abs/2502.16249">Paper</a>] [<a href="https://github.com/LIONS-EPFL/LION">Code</a>]</p> <ol> <li>Part I - Full Linear Attention</li> <li><a href="/blog/2025/lion-part2-theory/">Part II - Bi-directional RNN</a></li> <li><a href="/blog/2025/lion-part3-chunk/">Part III - Chunkwise Parallel from of LION</a></li> <li><a href="/blog/2025/lion-part4-results/">Part IV - Results</a></li> </ol> <hr/> <h1 id="tldr">TL;DR</h1> <p>Transformers with Linear Attention enable fast and parallel training. Moreover, they can be formulated as Recurrent Neural Networks (RNNs), for efficient linear-time inference. While extensively evaluated in causal sequence modeling, they have yet to be extended to the bi-directional setting. We introduce the <strong>LION</strong> framework, establishing new theoretical foundations for Linear Transformers in bi-directional sequence modeling. <strong>LION</strong> constructs a bi-directional RNN equivalent to full <strong>Linear Attention</strong>. This extends the benefits of Linear Transformers: parallel training and efficient inference into the bi-directional setting.</p> <div style="display: flex; align-items: flex-start;"> <div style="flex: 1; padding-right: 10px;"> <p> Existing memory-efficient bi-directional models employ more than 2x the training time of a Transformer. Our Linear Attention framework benefits from memory-efficient inference while maintaining the Transformer training speed. </p> <table> <tr> <th>Task</th> <th><span style="background-color: rgb(230, 255, 230); padding: 3px; color:black">🦁-🔥 </span></th> <th><span style="background-color: rgb(229, 204, 230); padding: 3px; color:black">🦁-D </span></th> <th><span style="background-color: rgb(255, 233, 211); padding: 3px; color:black">🦁-S </span></th> <th>Hydra</th> <th>Vim</th> </tr> <tr> <td>Vision</td> <td>$\times 0.73$</td> <td>$\times 1.39$</td> <td>$\times 1.46$</td> <td>$\times 2.51$</td> <td>$\times 10.86$</td> </tr> <tr> <td>MLM </td> <td>$\times 0.95$</td> <td>$\times 1.10$</td> <td>$\times 1.32$</td> <td>$\times 3.13$</td> <td>✗</td> </tr> </table> <div class="caption" style="color: #666666; margin-top: 1px;"> Training time (↓) relative to Transformer of the same scale </div> </div> <div style="flex: 0 0 50%;"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/fig1_plot.svg" sizes="95vw"/> <img src="/assets/img/fig1_plot.svg" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Using <strong>LION</strong>, we cast three Linear Transformers to their bi-directional form:</p> <ul> <li><strong>LION-️‍🔥</strong>, the bi-directional variant corresponding to <a href="https://arxiv.org/abs/2006.16236">LinearTransformer</a>.</li> <li><strong>LION-D</strong>, extending <a href="https://arxiv.org/abs/2307.08621">RetNet</a>.</li> <li><strong>LION-S</strong>, a Linear Transformer with a stable selective mask inspired by selectivity of SSMs like <a href="https://arxiv.org/abs/2405.21060">Mamba🐍</a>.</li> </ul> <p>By replacing the attention block with <strong>LION (-️‍🔥, -D, -S)</strong>, we achieve performance on bi-directional tasks that is comparable to Transformers and State-Space Models (SSMs) while improving training speed.</p> <hr/> <p>Recently, Transformers with Linear Attention <d-cite key="katharopoulos2020transformers"></d-cite> and State Space Models <d-cite key="gu2023mamba"></d-cite> <d-cite key="gu2022efficiently"></d-cite> <d-cite key="dao2024transformers"></d-cite> (SSMs) have gained significant popularity for causal sequence modeling due to their ability to efficiently support both parallel training and RNN-like inference. These models have demonstrated impressive accuracy in causal tasks, particularly in causal language modeling. For bi-directional sequence modeling, SSMs, particularly Mamba <d-cite key="gu2023mamba"></d-cite>, have been evaluated in vision tasks along with architecture iterations like Vision Mamba <d-cite key="zhu2024vision"></d-cite> and Hydra <d-cite key="hwang2025hydra"></d-cite>. However, Transformers with Linear Attention have been less explored in the bi-directional setting.</p> <p>We are curious to explore whether Linear Attention Transformers, including the vanilla Linear Transformer <d-cite key="katharopoulos2020transformers"></d-cite> or RetNet <d-cite key="sun2023retentive"></d-cite> can perform effectively on bi-directional sequence modeling. More specifically, what modifications are needed to adapt them for tasks like image classification and masked language modeling? 😊</p> <p>Let’s break this down with three key questions:</p> <h3 id="question-1-applicability">Question 1 (Applicability)</h3> <p>Given that Linear Transformers can be formulated as RNNs, offering efficiency benefits during inference and enabling parallel training for causal sequence modeling, can they also provide similar advantages for bi-directional processing? If so, what would the parallel form be, and how would the equivalent bi-directional RNN be structured?</p> <h3 id="question-2-performance">Question 2 (Performance)</h3> <p>Can simple Linear Transformers, like Linear Transformer <d-cite key="katharopoulos2020transformers"></d-cite> or RetNet <d-cite key="sun2023retentive"></d-cite>, perform well on bi-directional tasks such as image classification or masked language modeling?</p> <h3 id="question-3-training-throughput">Question 3 (Training Throughput)</h3> <p>While bi-directional SSMs are performant, they tend to be difficult and slow to train compared to Transformers with Full Attention (e.g., ViT <d-cite key="dosovitskiy2020image"></d-cite> and BERT <d-cite key="devlin2018bert"></d-cite>). Can Linear Transformers match the accuracy and efficiency of bi-directional SSMs while maintaining the training throughput of Softmax Transformers?</p> <h2 id="from-causal-to-full-linear-attention">From Causal to Full Linear Attention</h2> <p>Let’s start with Linear Attention Recurrence:</p> \[\begin{aligned} &amp; S_i = S_{i-1} + k_i v^\top_i, \quad z_i = z_{i-1} + k_i, \\ &amp; Scaled: y_i = \frac{q^\top_i S_i}{q^\top_i z_i}, \quad Non-Scaled: y_i = q^\top_i S_i \\ \end{aligned}\] <p>Above is the RNN form of the Linear Attention which has the parallel form of:</p> \[\mathbf{Y} = Scale \left(\mathbf{Q} \mathbf{K}^\top \odot \mathbf{M}^C \right)\] <p>and the mask \(\mathbf{M}^C\) is a lower triangular \(C\)ausal mask. Causal Linear Transformers are a class of models introduced following the development of the original Linear Transformer as shown above <d-cite key="katharopoulos2020transformers"></d-cite>. These models typically define a recurrence of the form:</p> \[\begin{aligned} S_i = \boldsymbol{\Lambda_i} \star S_{i-1} + \gamma_i k_i v^\top_i, \quad z_i = \boldsymbol{\Lambda_i} \star z_{i-1} + \gamma_i k_i, \\ Scaled: y_i = \frac{q^\top_i S_i}{q^\top_i z_i}, \quad Non-Scaled: y_i = q^\top_i S_i \\ \end{aligned}\] <p>Here, \(\boldsymbol{\Lambda_i}\) and \(\gamma_i\) are decay factors introduced after the Linear Transformer to enhance the performance and \(\star\) denotes an associative operator which depends on the specific model. (Spoiler alert ⚠️: the family of Linear Transformers has strong connections to SSMs, as explored in works like Deltanet <d-cite key="yang2024parallelizing"></d-cite> and Mamba2 <d-cite key="dao2024transformers"></d-cite> through state space duality (SSD) 😉). Many models apply a non-linear activation to queries and keys, such that \(\mathbf{k}_i = \phi(\mathbf{k}_i)\) and \(\mathbf{q}_i = \phi(\mathbf{q}_i)\). To avoid notation clutter, we omit explicitly writing \(\phi(.)\) everywhere assuming by default that queries and keys are already non-linearized. For simplicity, we consider \(\boldsymbol{\Lambda_i} = \lambda_i\) as a scalar and \(\gamma_i = 1\). We now present the general Scaled Linear Attention in the following form:</p> \[\begin{aligned} S_i &amp;= \lambda_i S_{i-1} + k_i v^\top_i,\\ z_i &amp;= \lambda_i z_{i-1} + k_i, \\ y_i &amp;= \frac{q^\top_i S_i}{q^\top_i z_i} \\ \end{aligned}\] <p>The first goal is to extend the Causal Linear Attention parallel form</p> \[\mathbf{Y} = \text{Scale} \left(\mathbf{Q} \mathbf{K}^\top \odot \mathbf{M}^C \right)\] <p>to a Scaled and Masked Full Linear Attention mechanism.</p> <h2 id="creating-scaled-and-masked-full-attention">Creating Scaled and Masked Full Attention</h2> <p>The first step is quite simple: the Masked and Scaled Attention can naturally take the following form, as suggested by its name:</p> <blockquote class="block-tip"> <p><strong>Full Linear Attention</strong></p> \[\mathbf{Y} = \text{Scale} \left(\mathbf{Q} \mathbf{K}^\top \odot \mathbf{M} \right)\] </blockquote> <p>The important part is how to well define the matrix \(\mathbf{M}\). A natural choice is to extend the causal mask \(\mathbf{M^C}\), where the causal mask between tokens \(i,j\) is given by \(\mathbf{M}^C_{ij} = \lambda_{j+1} \lambda_{j+2} \dots \lambda_i\), representing the product of all selective scalers between \(i\) and \(j\). In the bi-directional case, the full mask should preserve this desirable property. One can interpret the mask entries as a relative positional encoding between two tokens taking the following form:</p> \[\begin{aligned} \mathbf{M}_{ij} = \begin{cases} \Pi_{k=j}^{i-1}{\lambda_k}, &amp; i &gt; j \\ 1 &amp; i=j\\ \Pi_{k=i+1}^{j}{\lambda_k}, &amp; i &lt; j. \end{cases} \end{aligned}\] <p>To recap, the full output of Full Linear Attention can be presented as:</p> <p><span style="font-size: 0.7em;"> \(\mathbf{Y} = Scale \left( \underbrace{\left( \renewcommand*{\arraystretch} \begin{array}{ccccc} \mathbf{q}_1^{\top}\mathbf{k}_1 &amp; \mathbf{q}_1^{\top}\mathbf{k}_2 &amp; \cdots &amp; \mathbf{q}_1^{\top}\mathbf{k}_L \\ \mathbf{q}_2^{\top}\mathbf{k}_1 &amp; \mathbf{q}_2^{\top}\mathbf{k}_2 &amp; \cdots &amp; \mathbf{q}_2^{\top}\mathbf{k}_L\\ \vdots &amp; \vdots &amp; \ddots &amp; \vdots \\ \mathbf{q}_L^{\top}\mathbf{k}_1 &amp; \mathbf{q}_L^{\top}\mathbf{k}_2 &amp; \cdots &amp; \mathbf{q}_L^{\top}\mathbf{k}_L\\ \end{array} \right)}_{\hspace{1mm} \mathbf{A} = \mathbf{Q} \mathbf{K}^{\top}} \odot \underbrace{ \left( \renewcommand*{\arraystretch} \begin{array}{ccccc} 1 &amp; \lambda_2 &amp; \lambda_2 \lambda_3 &amp; \cdots &amp; \lambda_2 \cdots \lambda_L \\ \lambda_1 &amp; 1 &amp; \lambda_3 &amp; \cdots &amp; \lambda_3 \cdots \lambda_L \\ \lambda_2 \lambda_1 &amp; \lambda_2 &amp; 1 &amp; \cdots &amp; \lambda_4 \cdots \lambda_L \\ \vdots &amp; \vdots &amp; \vdots &amp; \ddots &amp; \vdots \\ \lambda_{L-1} \cdots \lambda_1 &amp; \lambda_{L-1} \cdots \lambda_2 &amp; \lambda_{L-1} \cdots \lambda_3 &amp; \cdots &amp; 1 \\ \end{array} \right) }_{\hspace{1mm} \mathbf{M}} \right) \left( \renewcommand*{\arraystretch} \begin{array}{c} \mathbf{v}_1^\top \\ \mathbf{v}_2^\top \\ \mathbf{v}_3^\top \\ \vdots \\ \mathbf{v}_L^\top \\ \end{array} \right)\) </span></p> <p>The equation above represents the Full <strong>Linear</strong> Attention in parallel form. Now that we have established Full Linear Attention for bi-directional sequence modeling, it’s time to derive its equivalent bi-directional RNN.</p> <h3 id="an-important-question"><strong>An Important Question:</strong></h3> <blockquote> <p><strong>Question:</strong> Is it worth training with Full Attention on bi-directional tasks considering it has quadratic complexity with sequence length \(O(L^2)\)?</p> </blockquote> <p>The answer is <strong>yes</strong>! Unlike causal language modeling, for bi-directional tasks such as Vision ($L=196$) and Masked Language Modeling (MLM) ($L=128$), sequence lengths used in practice are relatively short. This means that we can usually fit Full Attention in memory enalbing higher throughput without a significant trade-off in complexity.</p> <p>We believe that architectures designed for causal tasks can really benefit from modifications to adapt them to the bi-directional domain.</p> <h2 id="next-up">Next Up</h2> <ul> <li> <p>We introduce our framework, <strong>LION</strong>, which derives an equivalent bi-directional RNN for Full Linear Attention.</p> </li> <li> <p>Within this framework, we demonstrate how different Linear Transformers can be extended to their bi-directional counterparts.</p> </li> <li> <p>We explore the construction of stable masks \(\mathbf{M}\), enabling models using LION to <strong>TRAIN IN PARALLEL</strong> using Full Attention and <strong>INFER EFFICIENTLY</strong> like an RNN.</p> </li> <li> <p>Finally, we introduce a <strong>chunkwise parallel</strong> variant of LION to balance recurrence and parallelism 🙂.</p> </li> </ul> <p><a href="/blog/2025/lion-part2-theory/">Continue reading to Part II - Bi-directional RNN</a></p> <p><em>Acknowledgement:</em> We appreciate <a href="https://goombalab.github.io/">Albert Gu</a> and <a href="https://tridao.me/blog/">Tri Dao</a> for their insightful blog posts, which have been helpful in shaping our own.</p>]]></content><author><name>Arshia Afzal</name></author><summary type="html"><![CDATA[Explaining the Full Linear Attention paradigm for bi-directional sequence modeling]]></summary></entry><entry><title type="html">LION 🦁 Part II - Bi-directional RNN</title><link href="https://arshiaafzal.github.io/blog/2025/lion-part2-theory/" rel="alternate" type="text/html" title="LION 🦁 Part II - Bi-directional RNN"/><published>2025-02-24T00:00:00+00:00</published><updated>2025-02-24T00:00:00+00:00</updated><id>https://arshiaafzal.github.io/blog/2025/lion-part2-theory</id><content type="html" xml:base="https://arshiaafzal.github.io/blog/2025/lion-part2-theory/"><![CDATA[<p>[<a href="https://www.arxiv.org/abs/2502.16249">Paper</a>] [<a href="https://github.com/LIONS-EPFL/LION">Code</a>]</p> <ol> <li><a href="/blog/2025/lion-part1-model/">Part I - Full Linear Attention</a></li> <li>Part II - Bi-directional RNN</li> <li><a href="/blog/2025/lion-part3-chunk/">Part III - Chunkwise Parallel from of LION</a></li> <li><a href="/blog/2025/lion-part4-results/">Part IV - Results</a></li> </ol> <p>In <a href="/blog/2025/lion-part1-model/">Part I</a> of this series, we defined Full Linear Attention with Masking and Scaling. Similar to all Linear Transformers designed for Causal Sequence Modeling, we aim to derive an RNN form for efficiency during inference. In this section, we theoretically demonstrate the equivalent bi-directional RNN for the Full Linear Transformer.</p> <h2 id="finding-bi-directional-rnn-equal-to-full-linear-attention">Finding Bi-directional RNN Equal to Full Linear Attention</h2> <p>We aim to construct an RNN that is equivalent to the Masked and Scaled Linear Attention. The idea of a bi-directional RNN is to process the sequence in both the forward order (from first to last) and the reverse order (from last to first), these naturally correspond to the upper and lower parts of the Attention matrix and mask.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/att_mask_color.svg" sizes="95vw"/> <img src="/assets/img/att_mask_color.svg" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p><strong>Note:</strong> We use a consistent color coding for this section of the blog post and throughout our <a href="https://www.arxiv.org/abs/2502.16249">paper</a> 😊.</p> <ul> <li><span style="background-color: rgb(255, 248, 203); padding: 3px; color:black">Yellow</span> color indicates the <strong>upper part of the matrix (non-causal)</strong>.</li> <li><span style="background-color: rgb(254, 200, 201); padding: 3px; color:black">Red</span> color represents the <strong>diagonal elements</strong>.</li> <li><span style="background-color: rgb(208, 243, 248); padding: 3px; color:black">Blue</span> color corresponds to the <strong>lower triangular (causal) part</strong>.</li> </ul> <p>Let’s seperate the Attention into upper and lower parts:</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/att_sep.svg" sizes="95vw"/> <img src="/assets/img/att_sep.svg" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>This formulation represents both the causal and non-causal forms of Attention. We would like to model each triangular part using an RNN. Similarly, we can also separate the mask in the same way:</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/mask_sep.svg" sizes="95vw"/> <img src="/assets/img/mask_sep.svg" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>Let’s also write the scaling part of the Masked Attention $\mathbf{Y} = \text{Scale}(\mathbf{Q} \mathbf{K}^\top \odot \mathbf{M} ) \mathbf{V}$ as:</p> \[\begin{aligned} \mathbf{Y} = \big(\text{Scale}(\mathbf{Q}\mathbf{K}^{\top} \odot \mathbf{M})\big) \mathbf{V} = (\mathbf{C}^{-1}(\mathbf{Q}\mathbf{K}^{\top} \odot \mathbf{M}))\mathbf{V}, \hspace{1mm} \mathbf{C}_i = \mathbf{q}^{\top}_i\sum\limits_{j=1}^{L} \mathbf{M}_{ij}\mathbf{k}_j. \end{aligned}\] <p>Also, we can decompose the scaling matrix \(\mathbf{C}_i\) as:</p> \[\begin{aligned} \mathbf{C}_{i}= \underbrace{\mathbf{q}^{\top}_i\sum\nolimits_{j=1}^{i} \mathbf{M}_{ij}\mathbf{k}_j - \frac{1}{2} \mathbf{q}^{\top}_i\mathbf{k}_i}_{\mathbf{C}^F_i} + \underbrace{\mathbf{q}^{\top}_i\sum\nolimits_{j=i}^{L} \mathbf{M}_{ij}\mathbf{k}_j - \frac{1}{2} \mathbf{q}^{\top}_i\mathbf{k}_i}_{\mathbf{C}^B_i} \end{aligned}\] <p>Now we replace the above scaling matrix $\mathbf{C}$ in the output of the Attention form of $\mathbf{Y} = \text{Scale}(\mathbf{Q} \mathbf{K}^\top \odot \mathbf{M} ) \mathbf{V}$ . Interestingly, many terms naturally cancel out with each other.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/proofC.svg" sizes="95vw"/> <img src="/assets/img/proofC.svg" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>This results in only the forward and backward directions of the RNN remaining. As observed, the forward path aligns with Causal Linear Attention with masking. Now, we need to demonstrate that the backward path follows the same RNN structure in the reverse direction. We can simply flip the upper triangular matrices using the <a href="https://en.wikipedia.org/wiki/Exchange_matrix">Exchange Matrix</a> \(\mathbf{J}_L\) and the function \(F(X) = \mathbf{J}_L X \mathbf{J}_L\):</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/flip.svg" sizes="95vw"/> <img src="/assets/img/flip.svg" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>Cool! Now, both the upper part (equivalent to the RNN in the forward direction) and the lower part (equivalent to the RNN in the backward direction) can be formulated as RNNs. This is exactly what we need to construct our bi-directional RNN equivalent to Full Linear Attention.</p> <blockquote class="block-tip"> <p><strong>LION: Reccurence form</strong></p> \[\begin{aligned} \mathbf{S}_i^{F/B} &amp;= \lambda_i \mathbf{S}^{F/B}_{i-1} + \mathbf{k}_i \mathbf{v}_i^{\top}, \\ \mathbf{z}^{F/B}_i &amp;= \lambda_i \mathbf{z}^{F/B}_{i-1} + \mathbf{k}_i, \\ c^{F/B}_i &amp; = \mathbf{q}_i^{\top} \mathbf{z}^{F/B}_{i} - \frac{\mathbf{q}_i^{\top} \mathbf{k}_i}{2}, \\ \mathbf{y}^{F/B}_i &amp;= \mathbf{q}_i^{\top} \mathbf{S}^{F/B}_i - \frac{\mathbf{q}_i^{\top} \mathbf{k}_i}{2} \mathbf{v}_i, \\ out&amp;put: \mathbf{y}_i = \frac{\mathbf{y}^{F}_i + \mathbf{y}^{B}_i}{c^F_i + c^B_i}. \\ \end{aligned}\] </blockquote> <p>The terms \(\frac{\mathbf{q}_i^{\top} \mathbf{k}_i}{2}\) and \(\frac{\mathbf{q}_i^{\top} \mathbf{k}_i}{2} \mathbf{v}_i\) are subtracted to avoid double counting. This bi-directional RNN is equivalent to Scaled and Masked Linear Attention described in previous section of this blogpost.</p> <h2 id="some-important-details-of-our-rnn">Some Important details of our RNN</h2> <blockquote> <p>Only the states \(c^{F/B}_i\) and \(\mathbf{y}^{F/B}_i\) are stored per token, resulting in \(\mathcal{O}(Ld)\) memory usage. In contrast, naively storing full matrix-valued hidden states would require \(\mathcal{O}(Ld^2)\), which becomes infeasible for large models.</p> </blockquote> <blockquote> <p>Forward and backward recurrences run independently, completing in \(L\) time steps with \(L\) memory units, compared to \(2L\) in the naive approach.</p> </blockquote> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/memory.svg" sizes="95vw"/> <img src="/assets/img/memory.svg" width="100%" height="auto" title="Memory Allocation of LION in RNN form" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Memory allocation in LION during Forward and Backward recurrences.</figcaption> </figure> <p>All in one, we can visualize our framework:</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/frlion.svg" sizes="95vw"/> <img src="/assets/img/frlion.svg" width="100%" height="auto" title="LION" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">LION 🦁: Our framework for training in parallel using Full Linear Attention which also supports the efficient bi-directional RNN format.</figcaption> </figure> <h2 id="different-masks-of-lion">Different Masks of LION</h2> <p>Now that we have created our framework let’s see what are the choices of the decay factor \(\lambda_i\) and how they resemble known Linear Transformer models. Let’s set:</p> <blockquote> <p>$\lambda_i=1$ resembles the bi-directional version of the vanilla Linear Transformer <d-cite key="katharopoulos2020transformers"></d-cite> which we refer to as <span style="background-color: rgb(230, 255, 230); padding: 3px; color:black">LION-🔥 </span> (-LIT in <a href="https://www.arxiv.org/abs/2502.16249">the paper</a>).</p> </blockquote> <blockquote> <p>$\lambda_i=\lambda$ resembles the bi-directional version of RetNet <d-cite key="sun2023retentive"></d-cite> which we refer to as <span style="background-color: rgb(229, 204, 230); padding: 3px; color:black">LION-D </span>.</p> </blockquote> <blockquote> <p>$\lambda_i=\sigma(\mathbf{W}\mathbf{x}_i)$ (input dependent) resembles a bi-directional selective Linear Transformer inspired by Mamba2 <d-cite key="dao2024transformers"></d-cite> which we refer to as <span style="background-color: rgb(255, 233, 211) ; padding: 3px; color:black">LION-S </span>.</p> </blockquote> <p>We evaluate all models on several bi-directional tasks. Also inspired by Linear Transformers applying a feature mapping $\phi(.)$ to queries and keys we apply normalized shifted SILU $\phi(x)=$ <code class="language-plaintext highlighter-rouge">(SILU(x)+0.5)/(norm(SILU(x)+0.5))</code> as a non-linear activation function. Let’s dive deep in each of these models in LION framework.</p> <h3 id="lion-">LION-🔥</h3> <p>LION-🔥 is an extension of the original Linear Transformer <d-cite key="katharopoulos2020transformers"></d-cite>. Without any masking, the bi-directional parallel form can be simply written as:</p> \[\mathbf{Y} = Scale(\mathbf{Q} \mathbf{K}^\top )\mathbf{V}\] <p>the RNN form is the one introduced the previous green box “LION: Reccurence form” with \(\lambda_i=1\).</p> <h3 id="lion-d">LION-D</h3> <p>By fixing \(\lambda_i = \lambda\), the mask \(\mathbf{M}\) has the form:</p> \[\begin{align} \mathbf{M}_{ij} = \lambda^{|i-j|}, \quad \mathbf{D}_{ij} = |i-j|\log(\lambda), \quad \mathbf{M} = \exp(\mathbf{D}). \notag \end{align}\] <p>\(\mathbf{M}\) above is a Toeplitz mask <d-cite key="qin2023toeplitz"></d-cite>, we can efficiently create a decay mask with such structure using simple PyTorch functions. To ensure numerical stability, we bound the parameter \(\lambda\) with a <strong>sigmoid</strong>, setting \(\lambda = \sigma(a)\). Without this constraint, the scalar \(\lambda^L\) could become excessively large, leading to instability. In practice, for <strong>RetNet-style mask</strong> with a fixed decay, multiplication remains stable. Such mask can be implemented as follows:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">decay_mask</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">length</span><span class="p">):</span>
    <span class="n">idx</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="n">length</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="n">a</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>
    <span class="n">i</span><span class="p">,</span> <span class="n">j</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">meshgrid</span><span class="p">(</span><span class="n">idx</span><span class="p">,</span> <span class="n">idx</span><span class="p">,</span> <span class="n">indexing</span><span class="o">=</span><span class="sh">"</span><span class="s">ij</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">e</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">abs</span><span class="p">((</span><span class="n">i</span> <span class="o">-</span> <span class="n">j</span><span class="p">)).</span><span class="nf">float</span><span class="p">().</span><span class="nf">view</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">length</span><span class="p">,</span> <span class="n">length</span><span class="p">)</span>
    <span class="n">m</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sigmoid</span><span class="p">(</span><span class="n">a</span><span class="p">).</span><span class="nf">view</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span> <span class="o">**</span> <span class="n">e</span>
    <span class="k">return</span> <span class="n">m</span>
</code></pre></div></div> <h3 id="lion-s">LION-S</h3> <p>Observing the structure of $\mathbf{M}$, its upper ($\mathbf{M}^B$) and lower ($\mathbf{M}^F$) triangular parts are rank-1 <a href="https://people.cs.kuleuven.be/~raf.vandebril/homepage/publications/papers_html/qrq_07/node16.html">semi-separable matrices</a> <d-cite key="dao2024transformers"></d-cite>, allowing for efficient computation via matrix multiplications.</p> <p>During training, the decay factors $\lambda_i$ are stacked into ${\lambda}^F \in \mathbb{R}^L$, and the cumulative product</p> \[\mathbf{L}^F = cumprod(\lambda^F) = \prod_{k=0}^{i} \lambda^F_k\] <p>is used to generate the lower triangular mask \(\mathbf{M}^F\). For the upper triangular mask \(\mathbf{M}^B\), the input sequence is flipped, and the decay factors are computed as</p> \[\boldsymbol{\lambda}^B = \text{Flip}(\boldsymbol{\lambda}^F), \quad \mathbf{L}^B = cumprod(\boldsymbol{\lambda}^B).\] <p>The masks are then constructed as, \(\mathbf{M}^F =\) <code class="language-plaintext highlighter-rouge">tril(LF@inv(LF)^T)</code> for the forward part and \(\mathbf{M}^B =\) <code class="language-plaintext highlighter-rouge">triu(LB@inv(LB)^T)</code> for the backward part. Where <code class="language-plaintext highlighter-rouge">tril(.)</code> and <code class="language-plaintext highlighter-rouge">triu(.)</code> extract the lower and upper triangular parts of the input matrix respectively and <code class="language-plaintext highlighter-rouge">inv(.)</code> is a element wise inverse. The full mask is then obtained as</p> \[\mathbf{M} = \mathbf{M}^F + \mathbf{M}^B - \mathbf{I}.\] <p>To improve numerical stability, the selective scalar $\lambda_i$ is designed in exponential form</p> \[\lambda_i = e^{a_i}.\] <p>This results in the cumulative sum:</p> \[\mathbf{D}^F_{ij} = \begin{cases} \sum_{k=i}^{j+1} a_k, &amp; \text{if } i &gt; j, \\ - \infty, &amp; \text{if } i &lt; j, \\ 0, &amp; \text{if } i = j, \end{cases}\] \[\mathbf{M^F} = \exp(\mathbf{D^F}),\] <p>where $\exp(\cdot)$ is applied element-wise. The same process applies to $\mathbf{M}^B$ by flipping the input sequence order.</p> <p>Here, $\mathbf{D}^{F/B} = cumsum(\mathbf{a}^{F/B})$, where $\mathbf{a} \in \mathbb{R}^L$ contains the selective exponents $a_i$.</p> <p>Ensuring stability is crucial, as $\mathbf{L}^{F/B}$ can overflow or underflow when forming the full mask without chunking. To mitigate this, we define</p> \[a_i = \log(\sigma(\mathbf{W}_{a}^\top\mathbf{x}_i + b)),\] <p>where $\sigma(.)$ is the sigmoid function. By bounding $a_i$ within the interval $[0,1]$ we get numerical stability.</p> <p><strong>Note:</strong> We find using bounded activation functions to be important in practise since unbounded activations could cause NaN values in the loss function. To maintain stability, <strong>Chunking</strong> is required during training in Mamba and Hydra models when using the full sequence. This issue has been specifically highlighted in the <strong>Mamba2</strong> <a href="https://goombalab.github.io/blog/2024/mamba2-part3-chunk/">blog post</a> and can, again, be attributed to the softplus activation being unbounded. Since LION models use sigmoid activation, chunking is not required for training. In the <strong>Results</strong> section of this blog post, we explore why using <strong>Full Attention</strong> is beneficial for achieving <strong>high throughput</strong> during training.</p> <p>The code for building the mask of LION-S is simple, a Pytorch implementation is provided below:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">create_causal_mask_lions</span><span class="p">(</span><span class="n">tensor</span><span class="p">):</span>
    <span class="n">cumsum</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cumsum</span><span class="p">(</span><span class="n">tensor</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
    <span class="n">cumprod</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">cumsum</span><span class="p">)</span>
    <span class="n">a</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">matmul</span><span class="p">(</span>
        <span class="n">cumprod</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">),</span> <span class="mi">1</span> <span class="o">/</span> <span class="p">(</span><span class="n">cumprod</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">).</span><span class="nf">transpose</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="o">-</span><span class="mi">2</span><span class="p">)</span> <span class="o">+</span> <span class="mf">1e-7</span><span class="p">)</span>
    <span class="p">)</span>
    <span class="k">return</span> <span class="n">torch</span><span class="p">.</span><span class="nf">tril</span><span class="p">(</span><span class="n">a</span><span class="p">)</span>
</code></pre></div></div> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">selective_mask</span><span class="p">(</span><span class="n">vec</span><span class="p">):</span>
    <span class="n">vec_shape</span> <span class="o">=</span> <span class="n">vec</span><span class="p">.</span><span class="n">shape</span>
    <span class="n">a_for</span> <span class="o">=</span> <span class="nf">create_matrix_from_tensor</span><span class="p">(</span><span class="n">vec</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">).</span><span class="nf">transpose</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="o">-</span><span class="mi">2</span><span class="p">)).</span><span class="nf">squeeze</span><span class="p">()</span>
    <span class="n">a_back</span> <span class="o">=</span> <span class="p">(</span>
        <span class="nf">create_matrix_from_tensor</span><span class="p">(</span>
            <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">(</span>
                <span class="p">(</span><span class="n">vec</span><span class="p">,</span> <span class="n">torch</span><span class="p">.</span><span class="nf">ones</span><span class="p">((</span><span class="n">vec_shape</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="n">vec_shape</span><span class="p">[</span><span class="mi">1</span><span class="p">],</span> <span class="mi">1</span><span class="p">),</span> <span class="n">device</span><span class="o">=</span><span class="n">vec</span><span class="p">.</span><span class="n">device</span><span class="p">)),</span>
                <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">,</span>
            <span class="p">)[:,</span> <span class="p">:,</span> <span class="mi">1</span><span class="p">:]</span>
            <span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>
            <span class="p">.</span><span class="nf">transpose</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="o">-</span><span class="mi">2</span><span class="p">)</span>
        <span class="p">)</span>
        <span class="p">.</span><span class="nf">transpose</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="o">-</span><span class="mi">2</span><span class="p">)</span>
        <span class="p">.</span><span class="nf">squeeze</span><span class="p">()</span>
    <span class="p">)</span>
    <span class="k">return</span> <span class="n">a_for</span> <span class="o">+</span> <span class="n">a_back</span> <span class="o">-</span> <span class="n">torch</span><span class="p">.</span><span class="nf">eye</span><span class="p">(</span><span class="n">a_for</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">]).</span><span class="nf">to</span><span class="p">(</span><span class="n">a_for</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>
</code></pre></div></div> <h2 id="lion-attention-block">LION Attention Block</h2> <p>We can formulate the parallel Attention form of LION supporting all three extensions of our main experiments:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">LIONAttention</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span>
        <span class="n">self</span><span class="p">,</span>
        <span class="n">dim</span><span class="p">,</span>
        <span class="n">num_heads</span><span class="o">=</span><span class="mi">8</span><span class="p">,</span>
        <span class="n">qkv_bias</span><span class="o">=</span><span class="bp">False</span><span class="p">,</span>
        <span class="n">qk_scale</span><span class="o">=</span><span class="bp">None</span><span class="p">,</span>
        <span class="n">attn_drop</span><span class="o">=</span><span class="mf">0.0</span><span class="p">,</span>
        <span class="n">proj_drop</span><span class="o">=</span><span class="mf">0.0</span><span class="p">,</span>
        <span class="n">silunorm</span><span class="p">:</span> <span class="nb">bool</span> <span class="o">=</span> <span class="bp">False</span><span class="p">,</span>
        <span class="n">mask_type</span><span class="o">=</span><span class="sh">"</span><span class="s">Lit</span><span class="sh">"</span><span class="p">,</span>
    <span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">num_heads</span> <span class="o">=</span> <span class="n">num_heads</span>
        <span class="n">head_dim</span> <span class="o">=</span> <span class="n">dim</span> <span class="o">//</span> <span class="n">num_heads</span>
        <span class="n">self</span><span class="p">.</span><span class="n">scale</span> <span class="o">=</span> <span class="n">qk_scale</span> <span class="ow">or</span> <span class="n">head_dim</span><span class="o">**-</span><span class="mf">0.5</span>

        <span class="n">self</span><span class="p">.</span><span class="n">qkv</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">dim</span><span class="p">,</span> <span class="n">dim</span> <span class="o">*</span> <span class="mi">3</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="n">qkv_bias</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">attn_drop</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Dropout</span><span class="p">(</span><span class="n">attn_drop</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">proj</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">dim</span><span class="p">,</span> <span class="n">dim</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">proj_drop</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Dropout</span><span class="p">(</span><span class="n">proj_drop</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">mask_type</span> <span class="o">=</span> <span class="n">mask_type</span>

        <span class="k">if</span> <span class="n">mask_type</span> <span class="o">==</span> <span class="sh">"</span><span class="s">Lit</span><span class="sh">"</span><span class="p">:</span>
            <span class="k">pass</span>
        <span class="k">elif</span> <span class="n">mask_type</span> <span class="o">==</span> <span class="sh">"</span><span class="s">Selective</span><span class="sh">"</span><span class="p">:</span>
            <span class="n">self</span><span class="p">.</span><span class="n">a_i</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">dim</span><span class="p">,</span> <span class="n">num_heads</span><span class="p">)</span>
        <span class="k">elif</span> <span class="n">mask_type</span> <span class="o">==</span> <span class="sh">"</span><span class="s">Decay</span><span class="sh">"</span><span class="p">:</span>
            <span class="n">self</span><span class="p">.</span><span class="n">a_i</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Parameter</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">num_heads</span><span class="p">))</span>

        <span class="n">self</span><span class="p">.</span><span class="n">non_lin</span> <span class="o">=</span> <span class="n">silu_shifted</span>
        <span class="n">self</span><span class="p">.</span><span class="n">silunorm</span> <span class="o">=</span> <span class="n">silunorm</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
        <span class="n">b</span><span class="p">,</span> <span class="n">n</span><span class="p">,</span> <span class="n">c</span> <span class="o">=</span> <span class="n">x</span><span class="p">.</span><span class="n">shape</span>
        <span class="n">qkv</span> <span class="o">=</span> <span class="p">(</span>
            <span class="n">self</span><span class="p">.</span><span class="nf">qkv</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
            <span class="p">.</span><span class="nf">reshape</span><span class="p">(</span><span class="n">b</span><span class="p">,</span> <span class="n">n</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">num_heads</span><span class="p">,</span> <span class="n">c</span> <span class="o">//</span> <span class="n">self</span><span class="p">.</span><span class="n">num_heads</span><span class="p">)</span>
            <span class="p">.</span><span class="nf">permute</span><span class="p">(</span><span class="mi">2</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">4</span><span class="p">)</span>
        <span class="p">)</span>
        <span class="n">q</span><span class="p">,</span> <span class="n">k</span><span class="p">,</span> <span class="n">v</span> <span class="o">=</span> <span class="p">(</span>
            <span class="n">self</span><span class="p">.</span><span class="nf">non_lin</span><span class="p">(</span><span class="n">qkv</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="n">silunorm</span><span class="o">=</span><span class="n">self</span><span class="p">.</span><span class="n">silunorm</span><span class="p">),</span>
            <span class="n">self</span><span class="p">.</span><span class="nf">non_lin</span><span class="p">(</span><span class="n">qkv</span><span class="p">[</span><span class="mi">1</span><span class="p">],</span> <span class="n">silunorm</span><span class="o">=</span><span class="n">self</span><span class="p">.</span><span class="n">silunorm</span><span class="p">),</span>
            <span class="n">qkv</span><span class="p">[</span><span class="mi">2</span><span class="p">],</span>
        <span class="p">)</span>

        <span class="n">a_i</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">a_i</span><span class="p">(</span><span class="n">x</span><span class="p">).</span><span class="nf">transpose</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="o">-</span><span class="mi">2</span><span class="p">)</span>
        <span class="n">a_i</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">torch</span><span class="p">.</span><span class="n">nn</span><span class="p">.</span><span class="n">functional</span><span class="p">.</span><span class="nf">sigmoid</span><span class="p">(</span><span class="n">a_i</span><span class="p">)</span> <span class="o">+</span> <span class="mf">1e-7</span><span class="p">)</span>

        <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">mask_type</span> <span class="o">==</span> <span class="sh">"</span><span class="s">Selective</span><span class="sh">"</span><span class="p">:</span>
            <span class="n">m</span> <span class="o">=</span> <span class="nf">selective_mask</span><span class="p">(</span><span class="n">a_i</span><span class="p">)</span>

        <span class="k">elif</span> <span class="n">self</span><span class="p">.</span><span class="n">mask_type</span> <span class="o">==</span> <span class="sh">"</span><span class="s">Decay</span><span class="sh">"</span><span class="p">:</span>
            <span class="n">m</span> <span class="o">=</span> <span class="nf">decay_mask</span><span class="p">(</span><span class="n">a_i</span><span class="p">)</span>

        <span class="k">elif</span> <span class="n">self</span><span class="p">.</span><span class="n">mask_type</span> <span class="o">==</span> <span class="sh">"</span><span class="s">Lit</span><span class="sh">"</span><span class="p">:</span>
            <span class="n">m</span> <span class="o">=</span> <span class="mi">1</span>

        <span class="n">attn</span> <span class="o">=</span> <span class="p">(</span><span class="n">q</span> <span class="o">@</span> <span class="n">k</span><span class="p">.</span><span class="nf">transpose</span><span class="p">(</span><span class="o">-</span><span class="mi">2</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">))</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="n">scale</span>
        <span class="n">attn</span> <span class="o">=</span> <span class="n">m</span> <span class="o">*</span> <span class="n">attn</span>

        <span class="c1"># Scaling
</span>        <span class="n">attn</span> <span class="o">=</span> <span class="n">attn</span> <span class="o">/</span> <span class="n">attn</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">,</span> <span class="n">keepdim</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="n">attn</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">attn_drop</span><span class="p">(</span><span class="n">attn</span><span class="p">)</span>
        <span class="n">x</span> <span class="o">=</span> <span class="p">(</span><span class="n">attn</span> <span class="o">@</span> <span class="n">v</span><span class="p">).</span><span class="nf">transpose</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">).</span><span class="nf">reshape</span><span class="p">(</span><span class="n">b</span><span class="p">,</span> <span class="n">n</span><span class="p">,</span> <span class="n">c</span><span class="p">)</span>

        <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">proj</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">proj_drop</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">x</span>
</code></pre></div></div> <blockquote> <p><strong>Question:</strong> As seen above, the <strong>RNN</strong> is more efficient than the <strong>Transformer</strong> since it only requires storing the output for each token, resulting in a memory complexity of <strong>$\mathcal{O}(Ld)$</strong>, as opposed to storing the full Attention matrix, which requires <strong>$\mathcal{O}(L^2 d)$</strong>. Can we achieve a balance between the speed of Attention parallelism and the efficiency of an RNN?</p> </blockquote> <p>We will answer this question in our next section by introducing LION-Chunk.</p> <h2 id="next-up">Next Up</h2> <ul> <li> <p>In the next section of this series, we will describe how to apply a <strong>chunkwise parallel form</strong> for LION, allowing us to balance between the <em>RNN structure</em> and the <em>Attention-based</em> formulation.</p> </li> <li> <p>We show the numercial results and experiments on <a href="https://www.image-net.org/">Imagenet</a> and <a href="https://paperswithcode.com/dataset/c4">C4</a> dataset 😊.</p> </li> </ul> <p><a href="/blog/2025/lion-part3-chunk/">Continue reading to Part III - Chunkwise Parallel from of LION</a></p>]]></content><author><name>Arshia Afzal</name></author><summary type="html"><![CDATA[Deriving equivalent bi-directional RNN for Linear Attention]]></summary></entry><entry><title type="html">LION 🦁 Part III - Chunkwise Parallel Form of LION</title><link href="https://arshiaafzal.github.io/blog/2025/lion-part3-chunk/" rel="alternate" type="text/html" title="LION 🦁 Part III - Chunkwise Parallel Form of LION"/><published>2025-02-24T00:00:00+00:00</published><updated>2025-02-24T00:00:00+00:00</updated><id>https://arshiaafzal.github.io/blog/2025/lion-part3-chunk</id><content type="html" xml:base="https://arshiaafzal.github.io/blog/2025/lion-part3-chunk/"><![CDATA[<p>[<a href="https://www.arxiv.org/abs/2502.16249">Paper</a>] [<a href="https://github.com/LIONS-EPFL/LION">Code</a>]</p> <ol> <li><a href="/blog/2025/lion-part1-model/">Part I - Full Linear Attention</a></li> <li><a href="/blog/2025/lion-part2-theory/">Part II - Bi-directional RNN</a></li> <li>Part III - Chunkwise Parallel from of LION</li> <li><a href="/blog/2025/lion-part4-results/">Part IV - Results</a></li> </ol> <p>Since we have now established the LION framework, which maps Full Linear Attention into a bi-directional RNN in <a href="/blog/2025/lion-part2-theory/">Part II</a> of this series, a key question arises:</p> <p>Given that RNNs are efficient and Attention is fast, can we strike a balance between them?</p> <p>For causal Transformers like DeltaNet <d-cite key="yang2024parallelizing"></d-cite> and GLA <d-cite key="yang2024gated"></d-cite>, as well as the SSD algorithm in Mamba2 <d-cite key="dao2024transformers"></d-cite>, a chunkwise parallel form of Full Linear Attention could be an effective solution. Additionally, in models like Hydra <d-cite key="hwang2025hydra"></d-cite>, this balance is achieved by applying two SSD algorithms. However, can we derive a unified framework for chunking Full Linear Attention, particularly for LION-S and LION-D, where the mask $\mathbf{M}$ structure is known? The aim of chunking Full Linear Attention in LION is to maintain a balance between efficiency and speed, particularly during inference. Since LION benefits from stable masks, it does <strong>not</strong> require chunking during training allowing for higher throughput, especially for short sequences, when compared to other models such as Hydra <d-cite key="hwang2025hydra"></d-cite>. While in <strong>Gated Linear Attention (GLA)</strong> <d-cite key="yang2024gated"></d-cite>, <strong>DeltaNet</strong> <d-cite key="yang2024parallelizing"></d-cite>, and the <strong>SSD algorithm of Mamba2</strong> <d-cite key="dao2024transformers"></d-cite> <em>causal-specific</em> chunking methods are employed, we extend this to the non-causal case as well.</p> <h2 id="lion-chunk">LION-Chunk</h2> <p>The key idea of chunking is that instead of processing the entire sequence of length $L$, we divide it into $N$ subsequences of length $C$, where $N \times C = L$.<br/> To achieve this, we start with the Full Linear Attention formulation:</p> \[\mathbf{Y} = (\mathbf{Q} \mathbf{K}^\top \odot \mathbf{M}) \mathbf{V}\] <p>we first chunk the queries, keys and values into submatrices</p> \[\mathbf{Q}_{[i]} , \mathbf{K}_{[i]}, \mathbf{V}_{[i]} \in \mathbb{R}^{C \times d}\] <p>Now, given the form \((\mathbf{A} \odot \mathbf{M})\), where \(\mathbf{A} = \mathbf{Q} \mathbf{K}^\top\) we can construct the chunkwise form in four parts</p> <ul> <li>Chunkwise \(\mathbf{A}_{[ij]}\)</li> <li>Chunkwise form for the scaling matrix $\mathbf{C}_{[ij]}$</li> <li>The chunked hidden state to shape the unscaled output $\mathbf{S}_{[i(j-1)]}$</li> <li>Finally the output of the chunk $i$ which is $\mathbf{Y}_{[i]}$</li> </ul> <p>using these chunked matrices we shape the full linear atteniton in chunk form as bellow:</p> <blockquote class="block-tip"> <p><strong>LION Chunk</strong></p> \[\begin{aligned} \mathbf{A}_{[ij]} &amp; = \mathbf{Q}_{[i]}\mathbf{K}_{[j]}^\top \odot \mathbf{M}_{[ij]}, \\ \mathbf{C}_{[ij]} &amp;= \mathbf{C}_{[i(j-1)]} + \text{Sum} (\mathbf{A}_{[ij]}), \\ \mathbf{S}_{[ij]} &amp; =\mathbf{S}_{[i(j-1)]} + \mathbf{A}_{[ij]} \mathbf{V}_{[j]} , \\ \mathbf{Y}_{[i]} &amp; = \frac{\mathbf{S}_{[iN]}}{\mathbf{C}_{[iN]}} \end{aligned}\] </blockquote> <p>where $\text{Sum}$ operations applies summation over the row of the input matrix. And $\mathbf{M}_{[ij]}$ corresponds to a submatrix of the full maks $\mathbf{M}$ at chunk $ij$ like:</p> \[\mathbf{M}_{[ij]} = \mathbf{M}_{iC+1:i(C+1),jC+1:j(C+1)} \in \mathbb{R}^{C \times C}.\] <p>Let’s start with an example, chunking the Attention matrix $\mathbf{A}$ for a sequence of $L=9$ with $C=3$ chunk size in detail below:</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/att_chunk.svg" sizes="95vw"/> <img src="/assets/img/att_chunk.svg" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>Chunking simply involves computing the queries and keys for each boxed sub-matrix, as illustrated for the upper, lower, and diagonal chunks. For every Attention matrix chunk $[ij]$, the computation follows the same pattern, multiplying the corresponding queries and keys for that chunk.</p> <p>But does the same approach apply to Selective and Fixed masks?</p> <p>In reality, chunking the Attention mask is slightly different and even more critical than chunking Attention itself due to its unique structure. Below, we provide a detailed explanation of how to chunk the Attention mask for LION-D and LION-S.</p> <p>🚀 <strong>Note:</strong> The chunking visualization and details of this part are exclusively on the blogpost version.</p> <h3 id="lion-d-chunk">LION-D Chunk</h3> <p>Let’s start with the decay mask, as it is simpler and easier to visualize. For LION-D, the final mask is a Toeplitz mask constructed using the scalar decay factor $\lambda$. We can visualize how the mask is structured.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/mascdecchunk.svg" sizes="95vw"/> <img src="/assets/img/mascdecchunk.svg" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>The full mask of LION-D (or full RetNet mask) is constructed simply by the submatrix of $\Gamma$, which is a <a href="https://en.wikipedia.org/wiki/Toeplitz_matrix">Toeplitz matrix</a> itself. Regardless of where the chunk is located, whether in the upper or lower part of the mask matrix $\mathbf{M}$, it retains the same property of being a fraction of the Toeplitz matrix $\Gamma$ as bellow:</p> \[\mathbf{M}_{[ij]} = \Gamma \lambda^{|i-j|}\] <p>A pytorch implementation for LION-D Chunk Mask is provided below:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">mask_decay_partial</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">length</span><span class="p">,</span> <span class="n">start</span><span class="p">,</span> <span class="n">end</span><span class="p">):</span>
    <span class="n">idx</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="n">length</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="n">a</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>
    <span class="n">i</span><span class="p">,</span> <span class="n">j</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">meshgrid</span><span class="p">(</span><span class="n">idx</span><span class="p">,</span> <span class="n">idx</span><span class="p">[</span><span class="n">start</span><span class="p">:</span><span class="n">end</span><span class="p">],</span> <span class="n">indexing</span><span class="o">=</span><span class="sh">"</span><span class="s">ij</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">e</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">abs</span><span class="p">((</span><span class="n">i</span> <span class="o">-</span> <span class="n">j</span><span class="p">)).</span><span class="nf">float</span><span class="p">().</span><span class="nf">view</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">length</span><span class="p">,</span> <span class="nf">len</span><span class="p">(</span><span class="n">idx</span><span class="p">[</span><span class="n">start</span><span class="p">:</span><span class="n">end</span><span class="p">]))</span>
    <span class="n">m</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sigmoid</span><span class="p">(</span><span class="n">a</span><span class="p">).</span><span class="nf">view</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span> <span class="o">**</span> <span class="n">e</span>
    <span class="k">return</span> <span class="n">m</span>
</code></pre></div></div> <h3 id="lion-s-chunk">LION-S Chunk</h3> <p>The full mask of LION-S is more tricky than LION-D since the upper lower and the diagonal part of the mask are shaped differently:</p> <ul> <li>The <span style="background-color: rgb(255, 248, 203); padding: 3px; color:black">Upper</span> part is influenced only by the decay factors applied from the end to the beginning of the sequence.</li> <li>The <span style="background-color: rgb(254, 200, 201); padding: 3px; color:black">Diagonal</span> part incorporates contributions from both directions, spanning from the start to the end and from the end to the start.</li> <li>The <span style="background-color: rgb(208, 243, 248); padding: 3px; color:black">Lower</span> part is influenced only by the decay factors applied from the beginning to the end of the sequence.</li> </ul> <p>Let’s visualize LION-S mask as well:</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/masksel_chunk.svg" sizes="95vw"/> <img src="/assets/img/masksel_chunk.svg" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>For example, the chunk [1,3] has only the cumulative decay factors multiplied from the beginning up to the last three sequence elements, while the chunk [3,1] has only the decay factors multiplied from the end up to the first three sequence elements. This is the reason for using the matrices $\mathbf{L}^F$ and $\mathbf{L}^B$ to compute the cumulative products of the decay factors, progressing from the beginning to the end of the sequence and in reverse which can be created simply by <code class="language-plaintext highlighter-rouge">L^F = cumprod(a)</code> and <code class="language-plaintext highlighter-rouge">L^B = cumprod(flip(a))</code>.</p> <h3 id="the-code-for-lion-s-chunk-mask">The code for LION-S Chunk Mask</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">mask_forward</span><span class="p">(</span><span class="n">tensor</span><span class="p">,</span> <span class="n">chunk_index</span><span class="p">,</span> <span class="n">chunk_length</span><span class="p">):</span>
    <span class="n">cumprod</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">clamp</span><span class="p">(</span><span class="n">tensor</span><span class="p">.</span><span class="nf">cumprod</span><span class="p">(</span><span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">),</span> <span class="mf">1e-6</span><span class="p">)</span>
    <span class="n">a</span> <span class="o">=</span> <span class="p">(</span>
        <span class="n">cumprod</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>
        <span class="o">/</span> <span class="n">cumprod</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">2</span><span class="p">)[</span>
            <span class="p">...,</span> <span class="n">chunk_index</span> <span class="o">*</span> <span class="n">chunk_length</span> <span class="p">:</span> <span class="p">(</span><span class="n">chunk_index</span> <span class="o">+</span> <span class="mi">1</span><span class="p">)</span> <span class="o">*</span> <span class="n">chunk_length</span>
        <span class="p">]</span>
    <span class="p">)</span>
    <span class="k">return</span> <span class="n">torch</span><span class="p">.</span><span class="nf">tril</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">diagonal</span><span class="o">=-</span><span class="n">chunk_index</span> <span class="o">*</span> <span class="n">chunk_length</span><span class="p">)</span>


<span class="k">def</span> <span class="nf">mask_backward</span><span class="p">(</span><span class="n">tensor</span><span class="p">,</span> <span class="n">chunk_index</span><span class="p">,</span> <span class="n">chunk_length</span><span class="p">):</span>
    <span class="n">cumprod</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">clamp</span><span class="p">(</span><span class="n">tensor</span><span class="p">.</span><span class="nf">cumprod</span><span class="p">(</span><span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">),</span> <span class="mf">1e-6</span><span class="p">)</span>
    <span class="n">a</span> <span class="o">=</span> <span class="n">cumprod</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)[</span>
        <span class="p">...,</span> <span class="n">chunk_index</span> <span class="o">*</span> <span class="n">chunk_length</span> <span class="p">:</span> <span class="p">(</span><span class="n">chunk_index</span> <span class="o">+</span> <span class="mi">1</span><span class="p">)</span> <span class="o">*</span> <span class="n">chunk_length</span><span class="p">,</span> <span class="p">:</span>
    <span class="p">]</span> <span class="o">/</span> <span class="n">cumprod</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">2</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">torch</span><span class="p">.</span><span class="nf">triu</span><span class="p">(</span><span class="n">a</span><span class="p">.</span><span class="nf">transpose</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="o">-</span><span class="mi">2</span><span class="p">),</span> <span class="n">diagonal</span><span class="o">=-</span><span class="n">chunk_index</span> <span class="o">*</span> <span class="n">chunk_length</span><span class="p">)</span>


<span class="k">def</span> <span class="nf">mask_selective_partial</span><span class="p">(</span><span class="n">vec</span><span class="p">,</span> <span class="n">chunk_index</span><span class="p">,</span> <span class="n">chunk_length</span><span class="p">):</span>
    <span class="n">b</span><span class="p">,</span> <span class="n">h</span><span class="p">,</span> <span class="n">l</span> <span class="o">=</span> <span class="n">vec</span><span class="p">.</span><span class="n">shape</span>
    <span class="n">a_for</span> <span class="o">=</span> <span class="nf">create_matrix_from_tensor_forward</span><span class="p">(</span>
        <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">((</span><span class="n">torch</span><span class="p">.</span><span class="nf">ones_like</span><span class="p">(</span><span class="n">vec</span><span class="p">[...,</span> <span class="p">:</span><span class="mi">2</span><span class="p">]),</span> <span class="n">vec</span><span class="p">[...,</span> <span class="mi">1</span><span class="p">:</span><span class="o">-</span><span class="mi">1</span><span class="p">]),</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">),</span>
        <span class="n">chunk_index</span><span class="p">,</span>
        <span class="n">chunk_length</span><span class="p">,</span>
    <span class="p">)</span>
    <span class="n">a_back</span> <span class="o">=</span> <span class="nf">create_matrix_from_tensor_backward</span><span class="p">(</span>
        <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">((</span><span class="n">torch</span><span class="p">.</span><span class="nf">ones_like</span><span class="p">(</span><span class="n">vec</span><span class="p">[...,</span> <span class="p">:</span><span class="mi">1</span><span class="p">]),</span> <span class="n">vec</span><span class="p">[...,</span> <span class="mi">1</span><span class="p">:]),</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">),</span>
        <span class="n">chunk_index</span><span class="p">,</span>
        <span class="n">chunk_length</span><span class="p">,</span>
    <span class="p">)</span>
    <span class="n">i</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">diag_embed</span><span class="p">(</span>
        <span class="n">torch</span><span class="p">.</span><span class="nf">ones</span><span class="p">((</span><span class="n">b</span><span class="p">,</span> <span class="n">h</span><span class="p">,</span> <span class="n">l</span> <span class="o">-</span> <span class="n">chunk_index</span> <span class="o">*</span> <span class="n">chunk_length</span><span class="p">)),</span>
        <span class="n">offset</span><span class="o">=-</span><span class="n">chunk_index</span> <span class="o">*</span> <span class="n">chunk_length</span><span class="p">,</span>
    <span class="p">)[...,</span> <span class="p">:</span> <span class="n">a_for</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">]]</span>
    <span class="k">return</span> <span class="n">a_for</span> <span class="o">+</span> <span class="n">a_back</span> <span class="o">-</span> <span class="n">i</span><span class="p">.</span><span class="nf">to</span><span class="p">(</span><span class="n">a_for</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>
</code></pre></div></div> <p>Now that we have all elements in place let’s see how these models are working in practice on real-world datasets for masked language modeling and image classification.</p> <h2 id="next-up">Next Up</h2> <p>In the <a href="/blog/2025/lion-part4-results/">final part of this series</a>, we present the advantages of using LION compared to other methods for training SSMs or Linear Transformers.</p> <p>We also present the trade-offs for different LION 🦁 models and compare them with other well-known SSMs and Softmax Transformers.</p> <p><a href="/blog/2025/lion-part4-results/">Continue reading to Part IV - Results</a></p>]]></content><author><name>Arshia Afzal</name></author><summary type="html"><![CDATA[Explaining LION-Chunk for Balancing Memory-Speed Tradeoffs During Inference]]></summary></entry><entry><title type="html">LION 🦁 Part IV - Results</title><link href="https://arshiaafzal.github.io/blog/2025/lion-part4-results/" rel="alternate" type="text/html" title="LION 🦁 Part IV - Results"/><published>2025-02-24T00:00:00+00:00</published><updated>2025-02-24T00:00:00+00:00</updated><id>https://arshiaafzal.github.io/blog/2025/lion-part4-results</id><content type="html" xml:base="https://arshiaafzal.github.io/blog/2025/lion-part4-results/"><![CDATA[<p>[<a href="https://www.arxiv.org/abs/2502.16249">Paper</a>] [<a href="https://github.com/LIONS-EPFL/LION">Code</a>]</p> <ol> <li><a href="/blog/2025/lion-part1-model/">Part I - Full Linear Attention</a></li> <li><a href="/blog/2025/lion-part2-theory/">Part II - Bi-directional RNN</a></li> <li><a href="/blog/2025/lion-part3-chunk/">Part III - Chunkwise Parallel from of LION</a></li> <li>Part IV - Results</li> </ol> <p>In the final part of our LION series, we will present and discuss a selection of experimental results across various domains, including vision tasks, masked language modeling (MLM), and different LION architectures. These results not only highlight LION’s versatility and efficiency across diverse applications but also serve as a preview of the comprehensive findings detailed in the full paper.</p> <h2 id="image-classification-performance-overview">Image Classification Performance Overview</h2> <h3 id="model-comparisons">Model Comparisons</h3> <p>We evaluated LION’s performance, efficiency, and training times against state-of-the-art SSMs and Transformers for image classification. The results demonstrate that LION achieves competitive performance while offering significant advantages in training speed and efficiency.</p> <table> <thead> <tr> <th>Model</th> <th>#Param</th> <th>Imagenet Top-1 Acc.</th> <th>Train. time</th> </tr> </thead> <tbody> <tr> <td>$\text{ViT}$</td> <td>86M</td> <td>$77.9$</td> <td>$\times 1$</td> </tr> <tr> <td>$\text{DeiT}$</td> <td>86M</td> <td>$\underline{81.8}$</td> <td>$\times 1$</td> </tr> <tr> <td>$\text{Hydra}$</td> <td>104M</td> <td>$81.0$</td> <td>$\times 2.51$</td> </tr> <tr> <td>$\text{Vim}$</td> <td>98M</td> <td>$\mathbf{81.9}$</td> <td>$\times 10.86$</td> </tr> <tr> <td>$\text{LION-}\text{🔥}$</td> <td>86M</td> <td>$74.7$</td> <td>$\mathbf{\times 0.73}$</td> </tr> <tr> <td>$\text{LION-D}$</td> <td>86M</td> <td>$77.8$</td> <td>$\times \underline{1.39}$</td> </tr> <tr> <td>$\text{LION-D}^{\natural}$</td> <td>86M</td> <td>$80.2$</td> <td>$\times 1.48$</td> </tr> <tr> <td>$\text{LION-S}$</td> <td>86M</td> <td>$76.3$</td> <td>$\times 1.46$</td> </tr> <tr> <td>$\text{LION-S}^{\natural}$</td> <td>86M</td> <td>$79.9$</td> <td>$\times 1.68$</td> </tr> </tbody> </table> <div class="caption" style="color: #666666; margin-top: 1px;"> Model performance comparison on ImageNet classification, showing parameter count, top-1 accuracy, and relative training time. </div> <p>As shown in the table above, LION models achieve competitive performance with vision-specific SSMs like Vim, while being significantly faster during training. LION-D performs comparably to Vim and surpasses Hydra <d-cite key="hwang2025hydra"></d-cite>, while training approximately 7x faster than Vim <d-cite key="zhu2024vision"></d-cite>. Notably, LION-🔥 demonstrates the highest training speed across all models, showing that training with Full Linear Attention is significantly faster than chunkwise parallel training (used in Hydra) and considerably faster than the scan algorithm, even with optimized GPU kernels (as used in Vim). \(LION-S^{\natural}\) and \(LION-D^{\natural}\) modify the order of patches in an image to better capture the locality inherent in spatial patterns. By rearranging the patch sequence, these models enhance their understanding of local structures while still leveraging the efficiency of Linear Attention mechanisms similar to xLSTM <d-cite key="alkin2024vision"></d-cite>.</p> <h3 id="memory-efficiency">Memory Efficiency</h3> <p>The LION family demonstrates excellent memory efficiency across both vision and language tasks. Figure below shows inference memory usage with a batch size of 64 across different image resolutions, LION models (RNN form) maintain reasonable memory consumption even at high resolutions up to 2496 pixels, while adding minimal training overhead in BERT-style language modeling scenarios. In contrast, baseline models like ViT and DeiT run out of memory (OOM) at much lower resolutions.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/fig1_plot.svg" sizes="95vw"/> <img src="/assets/img/fig1_plot.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Memory Usage Comparison" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Memory usage during inference across different architectures with batch size 64. LION models (RNN form) maintain reasonable memory consumption at high resolutions while other models run out of memory.</figcaption> </figure> </div> </div> <h3 id="training-time-analysis">Training Time Analysis</h3> <p>The LION family demonstrates remarkable training efficiency across both vision and language tasks. As shown in the table below, LION variants add minimal training overhead compared to SSMs.</p> <table> <thead> <tr> <th>Task</th> <th><span style="background-color: rgb(230, 255, 230); padding: 3px; color:black">LION-🔥 </span></th> <th><span style="background-color: rgb(229, 204, 230); padding: 3px; color:black">LION-D </span></th> <th><span style="background-color: rgb(255, 233, 211) ; padding: 3px; color:black">LION-S </span></th> <th>Hydra</th> <th>Vim</th> </tr> </thead> <tbody> <tr> <td>Vision</td> <td>$\times 0.73$</td> <td>$\times 1.39$</td> <td>$\times 1.46$</td> <td>$\times 2.51$</td> <td>$\times 10.86$</td> </tr> <tr> <td>MLM</td> <td>$\times 0.95$</td> <td>$\times 1.10$</td> <td>$\times 1.32$</td> <td>$\times 3.13$</td> <td>✗</td> </tr> </tbody> </table> <div class="caption" style="color: #666666; margin-top: 1px;"> Training Times (relative to Transformer) ↓ </div> <p>For vision tasks, LION-🔥 achieves remarkable speed, training 27% faster than standard vision Transformers <d-cite key="dosovitskiy2020image"></d-cite>. Even the more complex LION variants maintain competitive training times, with LION-D and LION-S training only ~1.4x slower than Transformers. This is significantly better than competing approaches like Hydra (2.51x slower) and Vim (10.86x slower).</p> <p>In MLM tasks, the efficiency gains are even more pronounced. LION-🔥 nearly matches Transformer training speed at just 0.95x, while LION-D adds only 10% overhead. Even LION-S remains efficient at 1.32x. All LION variants significantly outperform Hydra’s 3.13x slowdown, while Vim is not applicable to MLM tasks (marked as ✗).</p> <h2 id="mlm-results">MLM Results</h2> <p>For masked language modeling (MLM) tasks, we evaluated LION models against BERT <d-cite key="devlin2018bert"></d-cite> and Hydra on both MLM pretraining and GLUE benchmark finetuning. The results show that LION variants achieve competitive performance while maintaining good training efficiency.</p> <table> <thead> <tr> <th>Model</th> <th>MLM Acc.</th> <th>GLUE</th> <th>Train. time</th> </tr> </thead> <tbody> <tr> <td>BERT</td> <td>$\underline{69.88}$</td> <td>$\mathbf{82.95}$</td> <td>$\times 1$</td> </tr> <tr> <td>Hydra</td> <td>$\mathbf{71.18}$</td> <td>$\underline{81.77}$</td> <td>$\times 3.13$</td> </tr> <tr> <td><span style="background-color: rgb(230, 255, 230); padding: 3px; color:black">LION-🔥 </span></td> <td>$67.11$</td> <td>$80.76$</td> <td>$\times \mathbf{0.95}$</td> </tr> <tr> <td><span style="background-color: rgb(229, 204, 230); padding: 3px; color:black">LION-D </span></td> <td>$68.64$</td> <td>$81.34$</td> <td>$\times \underline{1.10}$</td> </tr> <tr> <td><span style="background-color: rgb(255, 233, 211) ; padding: 3px; color:black">LION-S </span></td> <td>$69.16$</td> <td>$81.58$</td> <td>$\times 1.32$</td> </tr> </tbody> </table> <div class="caption" style="color: #666666; margin-top: 1px;"> C4 MLM and GLUE results for the LARGE scale (334M). For each dataset, the best and second best results are highlighted with bold and underline respectively. </div> <h2 id="lion-architecture-variants-and-trade-offs">LION Architecture Variants and Trade-offs</h2> <p>Let’s explore how different LION variants handle the trade-off between memory usage and inference speed. We will look at three key approaches:</p> <ol> <li>Full Linear Attention - The standard approach using the Full Attention matrix.</li> <li>Bidirectional RNN - Our memory-efficient RNN formulation.</li> <li>LION Chunk - A balanced approach using chunked computation.</li> </ol> <h3 id="memory-vs-speed-trade-offs">Memory vs Speed Trade-offs</h3> <p>The first plot below shows how these approaches compare in terms of memory efficiency and inference speed in LION-D. The RNN approach proves to be the most memory-efficient, while Full Attention uses the most memory. LION Chunk provides a nice middle ground - it uses less memory than Full Attention while actually achieving faster inference speeds than both alternatives. This makes it particularly attractive when you need to balance performance with resource constraints.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/fig3_plot.svg" sizes="95vw"/> <img src="/assets/img/fig3_plot.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Impact of Chunk Size" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Analysis of how chunk size affects model performance across different LION-D variants.</figcaption> </figure> </div> </div> <p>For LION-🔥, we see a similar pattern, but the chunking approach is even more pronounced.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/linear_chunking.svg" sizes="95vw"/> <img src="/assets/img/linear_chunking.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Linear Chunking Analysis" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Evaluation of linear chunking strategies and their impact on model efficiency of LION-🔥.</figcaption> </figure> </div> </div> <p>Lastly for LION-S, we see that the chunking approach is only faster at lower resolutions - at higher resolutions, the overhead from mask calculations starts to slow it down.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/selective_chunking.svg" sizes="95vw"/> <img src="/assets/img/selective_chunking.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Selective Chunking Analysis" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Performance comparison of selective chunking approaches across different sequence lengths with LION-S.</figcaption> </figure> </div> </div> <h2 id="future-directions">Future Directions</h2> <ul> <li> <p><strong>Expanding LION’s Potential:</strong> Our experiments focused on three main mask choices, but LION has the potential to accelerate other Linear Transformer variants for bidirectional tasks.</p> </li> <li> <p><strong>Optimizing Chunkwise Parallelism:</strong> The chunkwise parallel implementation during inference was done in PyTorch, with room for optimization through GPU kernel programming to reduce I/O overhead and improve speed.</p> </li> <li> <p><strong>Stabilizing Hydra and Mamba with LION:</strong> Hydra <d-cite key="hwang2025hydra"></d-cite> and Mamba <d-cite key="gu2023mamba"></d-cite> activations led to unstable training under Full Attention, suggesting LION could be used to stabilize these variants in the future.</p> </li> </ul> <h1 id="last-points">Last Points</h1> <p>We encourage the readers of this blog post to read the full <a href="https://www.arxiv.org/abs/2502.16249">paper</a> for more details about the LION framework and experimental setups. The implementation details are available in the <a href="https://github.com/LIONS-EPFL/LION">code repository</a>.</p> <p>If you use this work, please consider citing the paper:</p> <div class="language-bibtex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">@article</span><span class="p">{</span><span class="nl">afzal2025linear</span><span class="p">,</span>
  <span class="na">title</span><span class="p">=</span><span class="s">{Linear Attention for Efficient Bidirectional Sequence Modeling}</span><span class="p">,</span>
  <span class="na">author</span><span class="p">=</span><span class="s">{Afzal, Arshia and Abad Rocamora, Elias and Candogan, Leyla Naz and Puigdemont, Pol and Tonin, Francesco and Wu, Yongtao and Shoaran, Mahsa and Cevher, Volkan}</span><span class="p">,</span>
  <span class="na">journal</span><span class="p">=</span><span class="s">{arXiv preprint arXiv:2502.16249}</span><span class="p">,</span>
  <span class="na">year</span><span class="p">=</span><span class="s">{2025}</span><span class="p">,</span>
  <span class="na">url</span><span class="p">=</span><span class="s">{https://arxiv.org/abs/2502.16249}</span><span class="p">,</span>
  <span class="na">doi</span><span class="p">=</span><span class="s">{10.48550/arXiv.2502.16249}</span>
<span class="p">}</span>
</code></pre></div></div>]]></content><author><name>Arshia Afzal</name></author><category term="math"/><category term="ai"/><summary type="html"><![CDATA[Comprehensive results on Vision, MLM and more LION variants]]></summary></entry></feed>